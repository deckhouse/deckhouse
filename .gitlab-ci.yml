stages:
  - .pre
  - validate
  - build
  - test
  - deploy
  - .post

include:
  - .gitlab/**.yml

variables:
  CI_IMAGES_TAG: "v7"

default:
  tags:
    - k8s-intteam-cloud-d8-ssdlc-runner
  interruptible: true
