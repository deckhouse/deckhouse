#!/bin/bash -e

if [[ "$1" == "--config" ]]; then
    echo "
{
    \"afterHelm\": 1
}
"
else
    cat << 'EOF' > "$DYNAMIC_VALUES_JSON_MERGE_PATH"
{
    "module": {
        "afterHelm": "value"
    }
}
EOF
fi
