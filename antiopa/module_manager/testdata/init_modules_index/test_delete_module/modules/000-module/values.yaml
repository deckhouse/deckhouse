# Default values for 000-module.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

module:
  replicaCount: 1

  image:
    repository: nginx
    tag: stable
    pullPolicy: IfNotPresent
