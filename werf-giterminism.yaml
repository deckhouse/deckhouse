giterminismConfigVersion: 1
config:
  goTemplateRendering:	# The rules for the Go-template functions
    allowEnvVariables: [ /TF_.+/, /BASE_.+/, /CI_.+/, /REPO_MCM_.+/ ]
    allowUncommittedFiles: [ "modules/images_tags.json" ]
  stapel:
    mount:
     allowBuildDir: true
     allowFromPaths: [ "modules/images_tags.json" ]

