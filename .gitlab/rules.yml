# Pipeline creation rules.
# Pipeline runs for: tags, main, merge requests, release-x.xx, schedule,
# and manual/API e2e (web|api).
workflow:
  # Cancel previous pipeline jobs for the same ref when a new commit arrives
  # (MR pushes, main pushes). Jobs with interruptible: false are kept.
  auto_cancel:
    on_new_commit: interruptible
  rules:
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api)$/
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH =~ /^release-\d+\.\d+$/
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - when: never

# Reusable job-level rules (use via: extends: .rules:...)

# Skip regular CI jobs when the pipeline is an e2e run (web/api).
.rules:skip_e2e_run: &skip_e2e_run
  if: $CI_PIPELINE_SOURCE =~ /^(web|api)$/
  when: never

# E2E jobs: only for manual (web) or API pipelines.
.rules:on_e2e:
  rules:
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api)$/

# --- single-context -----------------------------------------------------------

.rules:on_tag:
  rules:
    - *skip_e2e_run
    - if: $CI_COMMIT_TAG

.rules:on_main:
  rules:
    - *skip_e2e_run
    - if: $CI_COMMIT_BRANCH == "main"

.rules:on_merge_request:
  rules:
    - *skip_e2e_run
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

.rules:on_release_branch:
  rules:
    - *skip_e2e_run
    - if: $CI_COMMIT_BRANCH =~ /^release-\d+\.\d+$/

.rules:on_schedule_cve_scans:
  rules:
    - *skip_e2e_run
    - if: $CI_PIPELINE_SOURCE == "schedule" && $DKP_CVE_SCANS_ENABLE == "true"

# Manual edition builds (MR / main). Release-branch editions are automatic.
.rules:on_main_manual:
  rules:
    - *skip_e2e_run
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual

.rules:on_merge_request_manual:
  rules:
    - *skip_e2e_run
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: manual

# --- composites ---------------------------------------------------------------

# PR + main + release-x.xx (validate, unit/lint, tests, doc_web).
.rules:on_validate:
  rules:
    - *skip_e2e_run
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH =~ /^release-\d+\.\d+$/

# PR + main only (security_scan, main_web_build).
.rules:on_pr_main:
  rules:
    - *skip_e2e_run
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# --- path-filtered (MR only) --------------------------------------------------

.rules:on_merge_request_node_controller:
  rules:
    - *skip_e2e_run
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - modules/040-node-manager/images/node-controller/**/*

.rules:on_merge_request_docs:
  rules:
    - *skip_e2e_run
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - "**/*.md"
        - "docs/**/*"
        - "**/crds/**/*"
        - "**/openapi/*config-values.yaml"
        - "candi/**/openapi/**/*"
        - "ee/candi/**/openapi/**/*"
