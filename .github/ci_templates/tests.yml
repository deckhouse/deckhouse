{!{/* tests-controller covers ./deckhouse-controller/... and tests-modules the
     module hooks and templates. Neither crosses a nested go.mod boundary, so a
     tool module under pkg/ needs naming here explicitly or its tests never run
     on a pull request -- test-crd-enricher is the crd-enricher module's. */}!}
{!{ define "unit_run_args" }!}
# <template: unit_run_args>
setup_cmd: 'pm install git'
args: 'sh -c "git config --global --add safe.directory /deckhouse && make tests-controller tests-modules test-crd-enricher"'
docker_options: '-w /deckhouse -v ${{github.workspace}}:/deckhouse --tmpfs /tmp:mode=1777,exec -e HOME=/tmp -e "TERM=xterm-256color" -v ~/go-pkg-cache:/go/pkg'
exec_options: '-u $(id -u):$(id -g) -e HOME=/tmp -w /deckhouse'
# </template: unit_run_args>
{!{- end -}!}

{!{ define "dhctl_run_args" }!}
# <template: dhctl_run_args>
args: 'make ci'
docker_options: '-u 0:0 -w /deckhouse/dhctl -v ~/go-pkg-cache:/go/pkg --tmpfs /tmp:mode=1777,exec -e HOME=/tmp'
# </template: dhctl_run_args>
{!{- end -}!}

{!{/* Args for the "Go Lint Warning" job. Lints only Go modules touched by
     the diff between $DIFF_BASE (set as job-level env) and HEAD; nested
     go.mod modules handled via longest-prefix matching in
     tools/lint-changed.sh. Runs inside the project's `tests` image which
     already has golangci-lint at /usr/local/bin/golangci-lint (built from
     the golangci-lint-artifact werf target), so no install dance needed. */}!}
{!{ define "golint_diff_run_args" }!}
# <template: golint_diff_run_args>
args: 'sh -c "git config --global --add safe.directory /deckhouse && make lint-changed"'
docker_options: '-u $(id -u):$(id -g) -w /deckhouse -v ${{github.workspace}}:/deckhouse --tmpfs /tmp:mode=1777,exec -e HOME=/tmp -e "TERM=xterm-256color" -e "DIFF_BASE=${DIFF_BASE}" -v ~/go-pkg-cache:/go/pkg -v ~/go-pkg-cache:/root/go/pkg -v ~/go-build-cache:/root/.cache/go-build -v ~/golangci-lint-cache:/root/.cache/golangci-lint'
# </template: golint_diff_run_args>
{!{- end -}!}

{!{ define "openapi_test_cases_run_args" }!}
# <template: openapi_test_cases_run_args>
args: 'ginkgo -vet=off ./testing/openapi_cases/'
docker_options: '-u $(id -u):$(id -g) -v ${{github.workspace}}:/deckhouse -w /deckhouse --tmpfs /tmp:mode=1777,exec -e HOME=/tmp -v ~/go-pkg-cache:/go/pkg'
# </template: openapi_test_cases_run_args>
{!{- end -}!}

{!{ define "validators_run_args" }!}
# <template: validators_run_args>
args: 'go run gotest.tools/gotestsum@latest -- -tags=validation -run Validation -timeout=${{env.TEST_TIMEOUT}} ./testing/...'
docker_options: '-u $(id -u):$(id -g) -w /deckhouse --tmpfs /tmp:mode=1777,exec -e HOME=/tmp -v ~/go-pkg-cache:/go/pkg'
# </template: validators_run_args>
{!{- end -}!}

{!{ define "tests_template" }!}
# <template: tests_template>
{!{- $ctx       := index . 0 }!}
{!{- $args_name := index . 1 }!}
{!{- $build_job := index . 2 }!}
{!{- $args_tmpl := printf "%s_run_args" $args_name }!}
{!{- $default   := dict "image" "tests" "args" "echo no args" "docker_options" "" }!}
{!{- $run       := coll.Merge (tmpl.Exec $args_tmpl | yaml) $default }!}
runs-on: [self-hosted, regular]
steps:
{!{ tmpl.Exec "started_at_output"       $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "checkout_full_step"      $ctx | strings.Indent 2 }!}
{!{- $secrets := slice -}!}
{!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/registry_host" "DECKHOUSE_DEV_REGISTRY_HOST" "DECKHOUSE_DEV_REGISTRY_HOST" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "login" "DECKHOUSE_DEV_REGISTRY_USER" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "password" "DECKHOUSE_DEV_REGISTRY_PASSWORD" ) $secrets -}!}
{!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 2 }!}
{!{ tmpl.Exec "login_dev_registry_step" $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "login_rw_registry_step"  $ctx | strings.Indent 2 }!}
  - name: Run tests
    env:
      TESTS_IMAGE_NAME: ${{needs.{!{$build_job}!}.outputs.tests_image_name}}
    run: |
      if [[ -z ${TESTS_IMAGE_NAME} ]] ; then
        echo "TESTS_IMAGE_NAME is empty"
        exit 1
      fi

      # Decode image name from gzip+base64.
      TESTS_IMAGE_NAME=$(echo ${TESTS_IMAGE_NAME} | base64 -d | gunzip)

      # Print image name in uppercase to prevent hiding non-secret registry host stored in secret.
      echo "Tests image name: '${TESTS_IMAGE_NAME}'" | tr '[:lower:]' '[:upper:]'
      echo "⚓️ 📥 [$(date -u)] Pull 'tests' image..."
      docker pull ${TESTS_IMAGE_NAME}
      echo "⚓️ 🏎 [$(date -u)] Run tests..."
{!{- if has $run "setup_cmd" }!}
      CONTAINER_ID=$(docker run -d {!{ $run.docker_options }!} ${TESTS_IMAGE_NAME} sleep infinity)
      docker exec -u 0:0 ${CONTAINER_ID} {!{ $run.setup_cmd }!}
      docker exec {!{ $run.exec_options }!} ${CONTAINER_ID} {!{ $run.args }!}
      docker rm -f ${CONTAINER_ID}
{!{- else }!}
      docker run {!{ $run.docker_options }!} ${TESTS_IMAGE_NAME} {!{ $run.args }!}
{!{- end }!}
# </template: tests_template>
{!{- end -}!}

{!{/* TODO change args in tests_template and merge two templates */}!}
{!{ define "tests_before_build_template" }!}
# <template: tests_before_build_template>
{!{- $ctx       := index . 0 }!}
{!{- $args_name := index . 1 }!}
{!{- $args_tmpl := printf "%s_run_args" $args_name }!}
{!{- $default   := dict "image" "tests" "args" "echo no args" "docker_options" "" }!}
{!{- $run       := coll.Merge (tmpl.Exec $args_tmpl | yaml) $default }!}
runs-on: [self-hosted, regular]
steps:
{!{ tmpl.Exec "started_at_output"       $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "checkout_full_step"      $ctx | strings.Indent 2 }!}
{!{- $secrets := slice -}!}
{!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/registry_host" "DECKHOUSE_DEV_REGISTRY_HOST" "DECKHOUSE_DEV_REGISTRY_HOST" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "login" "DECKHOUSE_DEV_REGISTRY_USER" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "password" "DECKHOUSE_DEV_REGISTRY_PASSWORD" ) $secrets -}!}
{!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 2 }!}
{!{ tmpl.Exec "login_dev_registry_step" $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "login_rw_registry_step"  $ctx | strings.Indent 2 }!}
  - name: Run tests
    env:
      TESTS_IMAGE_NAME: {!{ printf "%s@%s" (datasource "alt_base_images").REGISTRY_PATH (index (datasource "alt_base_images") "builder/golang-1.26") | quote }!}
    run: |
      # Print image name in uppercase to prevent hiding non-secret registry host stored in secret.
      echo "Tests image name: '${TESTS_IMAGE_NAME}'" | tr '[:lower:]' '[:upper:]'
      echo "⚓️ 📥 [$(date -u)] Pull 'tests' image..."
      docker pull ${TESTS_IMAGE_NAME}
      echo "⚓️ 🏎 [$(date -u)] Run tests..."
{!{- if has $run "setup_cmd" }!}
      CONTAINER_ID=$(docker run -d {!{ $run.docker_options }!} ${TESTS_IMAGE_NAME} sleep infinity)
      docker exec -u 0:0 ${CONTAINER_ID} {!{ $run.setup_cmd }!}
      docker exec {!{ $run.exec_options }!} ${CONTAINER_ID} {!{ $run.args }!}
      docker rm -f ${CONTAINER_ID}
{!{- else }!}
      docker run {!{ $run.docker_options }!} ${TESTS_IMAGE_NAME} {!{ $run.args }!}
{!{- end }!}
# </template: tests_before_build_template>
{!{- end -}!}

{!{ define "tests_registry_images_template" }!}
# <template: tests_registry_images_template>
{!{- $ctx := index . 0 }!}
runs-on: [self-hosted, regular]
steps:
{!{ tmpl.Exec "started_at_output"       $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "checkout_full_step"      $ctx | strings.Indent 2 }!}
{!{- $secrets := slice -}!}
{!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/registry_host" "DECKHOUSE_DEV_REGISTRY_HOST" "DECKHOUSE_DEV_REGISTRY_HOST" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "login" "DECKHOUSE_DEV_REGISTRY_USER" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "password" "DECKHOUSE_DEV_REGISTRY_PASSWORD" ) $secrets -}!}
{!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 2 }!}
{!{ tmpl.Exec "login_dev_registry_step" $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "login_rw_registry_step"  $ctx | strings.Indent 2 }!}
  - name: Run registry image tests
    env:
      TESTS_IMAGE_NAME: {!{ printf "%s@%s" (datasource "alt_base_images").REGISTRY_PATH (index (datasource "alt_base_images") "builder/golang-1.26") | quote }!}
    run: |
      # Print image name in uppercase to prevent hiding non-secret registry host stored in secret.
      echo "Tests image name: '${TESTS_IMAGE_NAME}'" | tr '[:lower:]' '[:upper:]'
      echo "⚓️ 📥 [$(date -u)] Pull base golang image..."
      docker pull ${TESTS_IMAGE_NAME}
      # Dedicated on-runner caches, created up front so the bind mounts are owned by the runner user
      # (Docker would otherwise create a missing host path as root, making it unwritable in-container).
      # Not the shared ~/go-pkg-cache: root jobs populate that with root-owned entries.
      mkdir -p ~/go-build-cache-registry ~/go-mod-cache-registry
      for entry in "go-build|$HOME/go-build-cache-registry" "go-mod|$HOME/go-mod-cache-registry"; do
        name="${entry%%|*}"; path="${entry#*|}";
        if [ -e "$path" ] && [ -n "$(ls -A "$path" 2>/dev/null)" ]; then
          echo "::notice::cache HIT  ${name}";
        else
          echo "::warning::cache MISS ${name} — will download/recompile";
        fi;
      done
      echo "⚓️ 🏎 [$(date -u)] Run registry image tests..."
      # Every binary of the registry module lives under its own go.mod, so `make tests-modules`
      # (TESTS_PATH = ./modules/...) never reaches them: the pattern stops at a nested module
      # boundary. This job is the only thing that runs them.
      #
      # docker-distribution is deliberately not in the list. Its suite is upstream's, and three of
      # its cases do not pass unattended on any machine: the inmemory driver suite runs past Go's
      # 10-minute limit, TestGracefulShutdown binds a fixed port, and one client test expects an
      # error string the code no longer produces. What this module changed there is covered by the
      # tests of the code that renders its configuration.
      #
      # GOFLAGS=-buildvcs=false: the tests need no git stamp, so go does not shell out to git.
      CONTAINER_ID=$(docker run -d -w /deckhouse --tmpfs /tmp:mode=1777,exec -v ${{github.workspace}}:/deckhouse -e HOME=/tmp -e "TERM=xterm-256color" -e GOFLAGS=-buildvcs=false -e GOCACHE=/go-build-cache -v ~/go-mod-cache-registry:/go/pkg -v ~/go-build-cache-registry:/go-build-cache ${TESTS_IMAGE_NAME} sleep infinity)
      for module in registry-agent registry-controller registry-syncer ; do
        echo "::group::${module}"
        docker exec -u $(id -u):$(id -g) -e HOME=/tmp -w /deckhouse/modules/038-registry/images/${module}/src ${CONTAINER_ID} go test -race -timeout 15m ./... || exit 1
        echo "::endgroup::"
      done
      docker rm -f ${CONTAINER_ID}
# </template: tests_registry_images_template>
{!{ end }!}

{!{ define "tests_node_controller_envtest_template" }!}
# <template: tests_node_controller_envtest_template>
{!{- $ctx := index . 0 }!}
runs-on: [self-hosted, regular]
steps:
{!{ tmpl.Exec "started_at_output"       $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "checkout_full_step"      $ctx | strings.Indent 2 }!}
{!{- $secrets := slice -}!}
{!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/registry_host" "DECKHOUSE_DEV_REGISTRY_HOST" "DECKHOUSE_DEV_REGISTRY_HOST" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "login" "DECKHOUSE_DEV_REGISTRY_USER" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/dev-registry/writetoken" "password" "DECKHOUSE_DEV_REGISTRY_PASSWORD" ) $secrets -}!}
{!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 2 }!}
{!{ tmpl.Exec "login_dev_registry_step" $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "login_rw_registry_step"  $ctx | strings.Indent 2 }!}
  - name: Run node-controller tests
    env:
      TESTS_IMAGE_NAME: {!{ printf "%s@%s" (datasource "alt_base_images").REGISTRY_PATH (index (datasource "alt_base_images") "builder/golang-1.26") | quote }!}
    run: |
      # Print image name in uppercase to prevent hiding non-secret registry host stored in secret.
      echo "Tests image name: '${TESTS_IMAGE_NAME}'" | tr '[:lower:]' '[:upper:]'
      echo "⚓️ 📥 [$(date -u)] Pull base golang image..."
      docker pull ${TESTS_IMAGE_NAME}
      # Persistent on-runner caches, created up front so the bind mounts are owned by the runner
      # user (Docker would otherwise create a missing host path as root, making it unwritable
      # in-container):
      #   envtest-assets                 - kubebuilder assets (etcd, kube-apiserver, kubectl) + setup-envtest + gotestfmt
      #   go-build-cache-node-controller - GOCACHE, so test binaries are not recompiled from scratch every run
      #   go-mod-cache-node-controller   - GOMODCACHE (dedicated, not the shared ~/go-pkg-cache which root
      #                                    jobs populate with root-owned entries that break our non-root tool installs)
      mkdir -p ~/envtest-assets ~/go-build-cache-node-controller ~/go-mod-cache-node-controller
      # On-runner cache status — MISS means this run will download/recompile (debugging aid;
      # emitted as GitHub annotations so a cold runner is easy to spot).
      for entry in "envtest-assets|$HOME/envtest-assets/k8s" "setup-envtest|$HOME/envtest-assets/setup-envtest" "gotestfmt|$HOME/envtest-assets/gotestfmt" "go-build|$HOME/go-build-cache-node-controller" "go-mod|$HOME/go-mod-cache-node-controller"; do
        name="${entry%%|*}"; path="${entry#*|}";
        if [ -e "$path" ] && [ -n "$(ls -A "$path" 2>/dev/null)" ]; then
          echo "::notice::cache HIT  ${name}";
        else
          echo "::warning::cache MISS ${name} — will download/recompile";
        fi;
      done
      echo "⚓️ 🏎 [$(date -u)] Run node-controller tests (unit + envtest)..."
      # GOFLAGS=-buildvcs=false: the tests don't need a git stamp, so go won't shell out to git —
      # this lets us skip installing git into the image (saves ~30s/run) and the safe.directory dance.
      CONTAINER_ID=$(docker run -d -w /deckhouse --tmpfs /tmp:mode=1777,exec -v ${{github.workspace}}:/deckhouse -e HOME=/tmp -e "TERM=xterm-256color" -e GOFLAGS=-buildvcs=false -e GOCACHE=/go-build-cache -v ~/go-mod-cache-node-controller:/go/pkg -v ~/envtest-assets:/envtest-assets -v ~/go-build-cache-node-controller:/go-build-cache ${TESTS_IMAGE_NAME} sleep infinity)
      docker exec -u $(id -u):$(id -g) -e HOME=/tmp -w /deckhouse ${CONTAINER_ID} sh -c "cd modules/040-node-manager/images/node-controller/src && make test LOCALBIN=/envtest-assets GOTESTFMT_CI=github"
      docker rm -f ${CONTAINER_ID}
# </template: tests_node_controller_envtest_template>
{!{- end -}!}

{!{ define "tests_webhooks_template" }!}
# <template: tests_webhooks_template>
{!{- $ctx       := index . 0 }!}
runs-on: [self-hosted, regular]
steps:
{!{ tmpl.Exec "started_at_output"       $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "checkout_full_step"      $ctx | strings.Indent 2 }!}
{!{- $secrets := slice -}!}
{!{- $secrets = append ( slice "projects/data/b050f3bd-733f-4746-9640-9df80d484074/proxy" "DISTRO_PACKAGES_PROXY" "DISTRO_PACKAGES_PROXY" ) $secrets -}!}
{!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 2 }!}
  - name: Run python webhook tests
    env:
      DISTRO_PACKAGES_PROXY: "${{ steps.secrets.outputs.DISTRO_PACKAGES_PROXY }}"
    run: |
      make tests-webhooks
# </template: tests_webhooks_template>
{!{- end -}!}

{!{ define "tests_dmt_lint" }!}
# <template: tests_dmt_lint>
{!{- $ctx       := index . 0 }!}
runs-on: [self-hosted, regular]
steps:
{!{ tmpl.Exec "started_at_output"       $ctx | strings.Indent 2 }!}
{!{ tmpl.Exec "checkout_full_step"      $ctx | strings.Indent 2 }!}
{!{- $secrets := slice -}!}
{!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/dmt_metrics" "url" "DMT_METRICS_URL" ) $secrets -}!}
{!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/dmt_metrics" "token" "DMT_METRICS_TOKEN" ) $secrets -}!}
{!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 2 }!}
  - name: DMT lint
    env:
      DMT_METRICS_URL: "${{ steps.secrets.outputs.DMT_METRICS_URL }}"
      DMT_METRICS_TOKEN: "${{ steps.secrets.outputs.DMT_METRICS_TOKEN }}"
    run: |
      make dmt-lint
# </template: tests_dmt_lint>
{!{- end -}!}
