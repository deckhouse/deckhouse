# ModuleSource without annotation and generation
apiVersion: deckhouse.io/v1alpha1
kind: ModuleSource
metadata:
  name: test-source-1
spec:
  registry:
    dockerCfg: YXNiCg==
    repo: dev-registry.deckhouse.io/deckhouse/modules
    scheme: HTTPS
