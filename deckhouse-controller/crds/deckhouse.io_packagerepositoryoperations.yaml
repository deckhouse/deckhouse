---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: packagerepositoryoperations.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: PackageRepositoryOperation
    listKind: PackageRepositoryOperationList
    plural: packagerepositoryoperations
    singular: packagerepositoryoperation
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PackageRepositoryOperation represents an operation to scan/update
          a package repository.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the behavior of a PackageRepositoryOperation.
            properties:
              packageRepository:
                type: string
              type:
                type: string
              update:
                properties:
                  fullScan:
                    type: boolean
                  timeout:
                    type: string
                type: object
            required:
            - packageRepository
            - type
            type: object
          status:
            description: Status of a PackageRepositoryOperation.
            properties:
              completionTime:
                format: date-time
                type: string
              conditions:
                description: List of status conditions to indicate the status of a PackageRepositoryOperation.
                items:
                  description: PackageRepositoryOperationStatusCondition contains condition information for a PackageRepositoryOperation.
                  properties:
                    lastProbeTime:
                      description: Last time the condition was probed.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of (`True`, `False`, `Unknown`).
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              packages:
                properties:
                  discovered:
                    type: integer
                  processed:
                    type: integer
                  total:
                    type: integer
                type: object
              packagesToProcess:
                description: List of packages that need to be processed.
                items:
                  description: PackageRepositoryOperationStatusPackageQueue represents a package in the processing queue.
                  properties:
                    name:
                      type: string
                    type:
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              phase:
                type: string
              startTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
