spec:
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: |
            Определяет состояние и параметры применения конкретного релиза (версии) Deckhouse Platform (DP) в кластере.

            Объекты DeckhouseRelease создаются DP автоматически, при обнаружении новых версий DP на выбранном [канале обновлений](/products/kubernetes-platform/documentation/latest/reference/release-channels.html). Изменение DeckhouseRelease позволяет управлять процессом применения соответствующей версии DP. Подробнее о настройке обновлений DP можно почитать в [документации](../../admin/configuration/update/configuration.html).

            Актуальные версии DP и модулей по каналам обновлений можно узнать на [releases.deckhouse.ru](https://releases.deckhouse.ru).
          properties:
            approved:
              description: |
                Разрешает или запрещает ручное обновление.

                Используется только если в конфигурации [модуля `deckhouse`](/modules/deckhouse/) установлен ручной режим обновлений (параметр [update.mode](/modules/deckhouse/configuration.html#parameters-update-mode) установлен в `Manual`).
                Игнорируется, если установлен режим обновления `Auto` или `AutoPatch`.

                Подробнее о ручном подтверждении обновлений можно почитать [в документации](../../admin/configuration/update/configuration.html#ручное-подтверждение-обновлений).
            spec:
              properties:
                version:
                  description: Версия DP.
                applyAfter:
                  description: Время, до которого отложено обновление, если релиз является частью [canary-release](../../user/network/canary-deployment.html). Если релиз ожидает применения, уточнить причину можно в поле [`.status.message`](../../admin/configuration/update/notifications.html#формат-уведомлений) объекта DeckhouseRelease.
                requirements:
                  description: |
                    Структура, содержащая список требований для установки релиза. Используется ядром DP. Если требования не выполняются, релиз может быть пропущен или заблокирован для установки.

                    Сообщения о невыполненных требованиях, можно найти в поле [`.status.message`](../../admin/configuration/update/notifications.html#формат-уведомлений) объекта DeckhouseRelease.
                disruptions:
                  x-doc-deprecated: true
                  description: Изменения в релизе, которые могут привести к кратковременному простою в работе системных компонентов.
                changelog:
                  description: Структура, содержащая список изменений DP (и модулей, входящих в состав DP) данного релиза. Подробнее об истории изменений в релизах DP можно почитать в [разделе «Обновление»](../../architecture/updating.html#получение-истории-изменений-changelog).
                changelogLink:
                  description: Ссылка на страницу со всеми изменениями данного релиза.
            status:
              properties:
                phase:
                  description: |
                    Текущий статус релиза.

                    Типичные значения:

                    - `Pending` — релиз создан, но еще не применен (ожидание ручного подтверждения, окон обновлений, canary-release, минимального времени уведомления);
                    - `Deployed` — DP переключился на версию релиза (обновление модулей и компонентов может продолжаться асинхронно);
                    - `Superseded` — релиз устарел и больше не используется;
                    - `Suspended` — релиз отменен (как правило, до применения);
                    - `Skipped` — релиз пропущен из-за невыполненных требований (`requirements`).

                    Подробнее о статусах релизов DP можно почитать в [документации модуля `deckhouse`](/modules/deckhouse/#обновление-релизов-deckhouse).
                message:
                  description: Детальное сообщение об ошибке или статусе релиза.
                transitionTime:
                  description: Время изменения статуса релиза.
                approved:
                  description: |
                    Статус готовности релиза к обновлению. Используется только для режима обновления [`Manual`](/modules/deckhouse/#закрепление-релиза) (`update.mode: Manual`).
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: phase
          jsonPath: .status.phase
          type: string
          description: 'Показывает текущий статус релиза.'
        - name: transitionTime
          jsonPath: .status.transitionTime
          type: date
          description: 'Показывает, когда статус релиза изменился.'
        - name: message
          jsonPath: .status.message
          type: string
          description: 'Детали статуса релиза (почему релиз ожидает или пропущен).'
