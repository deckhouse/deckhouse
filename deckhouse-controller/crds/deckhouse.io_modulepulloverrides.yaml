---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: modulepulloverrides.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: ModulePullOverride
    listKind: ModulePullOverrideList
    plural: modulepulloverrides
    singular: modulepulloverride
  scope: Cluster
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: ModulePullOverride object
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the behavior of an ModulePullOverride.
            properties:
              imageTag:
                type: string
              rollback:
                type: boolean
              scanInterval:
                format: date-time
                type: string
            required:
            - imageTag
            - rollback
            - scanInterval
            type: object
          status:
            description: Status of an ModulePullOverride.
            properties:
              imageDigest:
                type: string
              message:
                type: string
              updatedAt:
                format: date-time
                type: string
              weight:
                format: int32
                type: integer
            required:
            - imageDigest
            - message
            - updatedAt
            - weight
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
