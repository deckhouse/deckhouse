---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: modulepackages.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: ModulePackage
    listKind: ModulePackageList
    plural: modulepackages
    singular: modulepackage
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.usedByCount
      name: UsedBy
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModulePackage represents information about available module package.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            description: Status of a ModulePackage.
            properties:
              availableRepositories:
                description: List of repository names where this module package is
                  available.
                items:
                  type: string
                type: array
              usedBy:
                description: Information about modules using this package.
                items:
                  properties:
                    name:
                      description: Name of the module instance.
                      type: string
                    namespace:
                      description: Namespace where the module is installed.
                      type: string
                    version:
                      description: Version of the package used by this module.
                      type: string
                  type: object
                type: array
              usedByCount:
                description: Number of modules using this package.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
