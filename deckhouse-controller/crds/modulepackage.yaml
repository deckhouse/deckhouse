---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: modulepackages.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: ModulePackage
    listKind: ModulePackageList
    plural: modulepackages
    shortNames:
    - mp
    singular: modulepackage
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModulePackage represents information about available module package.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.

              More info [in the Kubernetes documentation](https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources)
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.

              More info [in the Kubernetes documentation](https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds)
            type: string
          metadata:
            type: object
          status:
            description: Status of a ModulePackage.
            properties:
              availableRepositories:
                description: List of repository names where this module package is
                  available.
                items:
                  type: string
                type: array
              releaseChannels:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Versions the release channels of each repository point to, keyed by repository name and then
                  by release channel name.
                type: object
              usedBy:
                description: Information about module using this package.
                properties:
                  version:
                    description: Version of the package used by this module.
                    type: string
                type: object
            type: object
        type: object
        x-doc-examples:
        - apiVersion: deckhouse.io/v1alpha1
          kind: ModulePackage
          metadata:
            name: example
    served: true
    storage: true
    subresources:
      status: {}
