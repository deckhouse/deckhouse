apiVersion: v1
kind: Service
metadata:
  name: kube-lego-nginx
  namespace: kube-lego
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: kube-lego
