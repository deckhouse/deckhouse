type: object # TODO
properties:
  podNetworkMode:
    description: |
      Режим работы.
      > **ВНИМАНИЕ!!!** При переключении между `HostGW` и `VXLAN` требуется **перезагрузка всех узлов** кластера!!! Изменять параметр можно **только** при использовании модуля **в bare metal-кластерах**.
