---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: vertical-pod-autoscaler:exporter
{{ include "helm_lib_module_labels" (list . (dict "app" "vpa-exporter")) | indent 2 }}
rules:
- apiGroups: [""]
  resources:
  - namespaces
  - pods
  verbs:
  - list
  - watch
  - get
- apiGroups: ["extensions"]
  resources:
  - deployments
  - daemonsets
  verbs:
  - list
  - watch
  - get
- apiGroups: ["batch"]
  resources:
  - jobs
  verbs:
  - list
  - watch
  - get
- apiGroups: ["apps"]
  resources:
  - statefulsets
  verbs:
  - list
  - watch
  - get
- apiGroups:
  - "autoscaling.k8s.io"
  resources:
  - verticalpodautoscalers
  verbs:
  - get
  - list
  - watch
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vpa-exporter
  namespace: kube-system
{{ include "helm_lib_module_labels" (list . (dict "app" "vpa-exporter")) | indent 2 }}
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: vertical-pod-autoscaler:exporter
{{ include "helm_lib_module_labels" (list . (dict "app" "vpa-exporter")) | indent 2 }}
subjects:
- kind: ServiceAccount
  name: vpa-exporter
  namespace: kube-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: vertical-pod-autoscaler:exporter
