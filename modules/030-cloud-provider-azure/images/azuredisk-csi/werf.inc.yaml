---
image: {{ $.ModuleName }}/{{ $.ImageName }}
fromImage: common/distroless
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
  add: /src/_output/amd64/azurediskplugin
  to: /azurediskplugin
  before: setup
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-util-linux-artifact
  add: /
  to: /
  before: setup
  includePaths:
  - etc/mke2fs.conf
  - bin/mount
  - bin/mkdir
  - bin/rmdir
  - bin/umount
  - bin/findmnt
  - bin/dmesg
  - bin/lsblk
  - sbin/badblocks
  - sbin/blockdev
  - 'sbin/blk*'
  - 'sbin/btrfs*'
  - sbin/debugfs
  - sbin/dumpe2fs
  - 'sbin/e2*'
  - sbin/findfs
  - 'sbin/fsck*'
  - 'sbin/fsfreeze'
  - 'sbin/fstrim'
  - sbin/mke2fs
  - 'sbin/mkfs*'
  - sbin/resize2fs
  - sbin/tune2fs
  - sbin/xfs_repair
  - sbin/wipefs
  - usr/bin/chattr
  - usr/bin/lsattr
  - usr/sbin/e2freefrag
  - usr/sbin/e4crypt
  - usr/sbin/e4defrag
  - usr/sbin/filefrag
  - usr/sbin/nvme
  - usr/sbin/xfs*
  - 'lib64/lib*'
  - 'lib64/ld*'
docker:
  ENTRYPOINT: ["/azurediskplugin"]
---
{{ $csiBuildPath := "/src" }}
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
from: {{ $.Images.BASE_GOLANG_20_ALPINE }}
mount:
- fromPath: ~/go-pkg-cache
  to: /go/pkg
shell:
  beforeInstall:
  - |
    apk add --no-cache ca-certificates make git \
      util-linux udev\
      e2fsprogs e2fsprogs-extra \
      xfsprogs xfsprogs-extra \
      btrfs-progs btrfs-progs-extra
  install:
  - export GO_VERSION=${GOLANG_VERSION}
  - export GOPROXY={{ $.GOPROXY }}
  - mkdir -p {{ $csiBuildPath }}
  - git clone --depth 1 --branch v1.17.0 https://github.com/kubernetes-sigs/azuredisk-csi-driver.git {{ $csiBuildPath }}
  - cd {{ $csiBuildPath }}
  - make azuredisk
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-util-linux-artifact
from: {{ $.Images.BASE_ALT }}
shell:
  beforeInstall:
    - apt-get update
    - |
      apt-get install -y ca-certificates \
                      util-linux \
                      e2fsprogs xfsprogs \
                      mount btrfs-progs