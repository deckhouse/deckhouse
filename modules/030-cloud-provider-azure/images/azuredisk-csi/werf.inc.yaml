---
image: {{ $.ModuleName }}/{{ $.ImageName }}
fromImage: common/distroless
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
  add: /src/_output/amd64/azurediskplugin
  to: /azurediskplugin
  before: setup
docker:
  ENTRYPOINT: ["/azurediskplugin"]
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
from: {{ $.Images.BASE_GOLANG_20_ALPINE }}
mount:
- fromPath: ~/go-pkg-cache
  to: /go/pkg
shell:
  beforeInstall:
  - apk add --no-cache make bash git mercurial patch rsync
  install:
  - mkdir /src
  - wget https://github.com/kubernetes-sigs/azuredisk-csi-driver/archive/refs/tags/v1.17.0.tar.gz -O - | tar -xz --strip-components=1 -C /src/
  - cd /src
  - make azuredisk
  - chown 64535:64535 _output/amd64/azurediskplugin
  - chmod 0755 _output/amd64/azurediskplugin
