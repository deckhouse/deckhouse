type: object
properties:
  podNetworkMode:
    description: |
      Режим работы.
      > **ВНИМАНИЕ!!!** При переключении между `host-gw` и `vxlan` требуется **перезагрузка всех узлов** кластера!!! Изменять параметр можно **только** при использовании модуля **в bare-metal-кластерах**.
