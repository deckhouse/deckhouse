apiVersion: v1
kind: Pod
metadata:
  name: "linstor-test-schedule"
  namespace: d8-{{ .Chart.Name }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-weight: "6"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  # Smoke test: just test the scheduler without volumes
  schedulerName: linstor
  containers:
    - name: wget
      image: busybox
      command: []
  restartPolicy: Never
