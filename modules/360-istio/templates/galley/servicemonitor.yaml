{{- if (.Values.global.enabledModules | has "prometheus") }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: istio-galley
  namespace: kube-prometheus
{{ include "helm_lib_module_labels" (list . (dict "app" "galley" "prometheus" "main")) | indent 2 }}
spec:
  jobLabel: prometheus-target
  selector:
    matchLabels:
      prometheus-target: istio-galley
  namespaceSelector:
    matchNames:
    - d8-{{ $.Chart.Name }}
  endpoints:
  - port: http-metrics
    relabelings:
    - regex: endpoint
      action: labeldrop
{{- end }}
