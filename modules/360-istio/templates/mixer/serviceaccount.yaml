{{- if not .Values.istio.disableMixerTelemetry }}
apiVersion: v1
kind: ServiceAccount
metadata:
  # Это имя захардкожено в цитадели и много где.
  name: istio-mixer-service-account
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: mixer
{{- end }}
