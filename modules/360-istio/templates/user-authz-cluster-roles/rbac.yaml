---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    user-authz.deckhouse.io/access-level: User
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
  name: kube-{{ .Chart.Name }}:user-authz:rbac:user
rules:
- apiGroups:
  - rbac.istio.io
  resources:
  - rbacconfigs
  - serviceroles
  - servicerolebindings
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    user-authz.deckhouse.io/access-level: Deploy
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
  name: kube-{{ .Chart.Name }}:user-authz:rbac:deploy
rules:
- apiGroups:
  - rbac.istio.io
  resources:
  - rbacconfigs
  - serviceroles
  - servicerolebindings
  verbs:
  - create
  - delete
  - deletecollection
  - patch
  - update
