---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    user-authz.deckhouse.io/access-level: User
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
  name: kube-{{ .Chart.Name }}:user-authz:authentication:user
rules:
- apiGroups:
  - authentication.istio.io
  resources:
  - policies
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    user-authz.deckhouse.io/access-level: Deploy
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
  name: kube-{{ .Chart.Name }}:user-authz:authentication:deploy
rules:
- apiGroups:
  - authentication.istio.io
  resources:
  - policies
  verbs:
  - create
  - delete
  - deletecollection
  - patch
  - update
