---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: {{ .Chart.Name }}
spec:
  clusterIP: None
  ports:
  - name: http
    port: 8000
    protocol: TCP
  selector:
    app: {{ .Chart.Name }}
