#!/bin/bash

source /antiopa/shell_lib.sh

function __config__() {
  jo -p beforeHelm=10
}

function __main__() {
  if ! values::has openvpn.adminPassword ; then
    values::set --config openvpn.adminPassword $(values::generate_password)
  fi
}

hook::run $@
