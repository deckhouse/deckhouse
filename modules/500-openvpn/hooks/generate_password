#!/bin/bash

source /deckhouse/shell_lib_legacy.sh

function __config__() {
  jo -p beforeHelm=15
}

function __main__() {
  if ! values::has openvpn.externalAuthentication ; then
    if ! values::has openvpn.password ; then
      values::set --config openvpn.password $(values::generate_password)
    fi
  else
    values::unset --config openvpn.password
  fi
}

hook::run $@
