## Patches

### 000-update-go-libraries.patch

Update Go and Go dependencies to fix known CVEs (`golang.org/x/net`, `golang.org/x/sys`, `golang.org/x/text`, `golang.org/x/mod`, `golang.org/x/tools` and related transitive modules). The Go directive is bumped to `1.25.0`.

### 001-openvpn-genkey-cmd.patch

Update cmd line for openvpn genkey
