{{- $containerd_version := "1.7.24" }}
{{- $image_version := $containerd_version | replace "." "-" }}
{{- $runc_version := "1.2.2" }}
---
image: {{ .ModuleName }}/{{ .ImageName }}-src-artifact-{{ $image_version }}
final: false
fromImage: common/src-artifact
git:
- add: /{{ $.ModulePath }}modules/007-{{ $.ModuleName }}/images/{{ $.ImageName }}/scripts
  to: /src/scripts
  stageDependencies:
    install:
    - '**/*'
shell:
  install:
  - git clone --depth=1 --branch v{{ $containerd_version }} {{ $.SOURCE_REPO }}/containerd/containerd.git /src/containerd
#  - rm -rf /src/containerd/.git # https://github.com/containerd/containerd/blob/main/Makefile#L35-L36
  - git clone --depth=1 --branch v{{ $runc_version }} {{ $.SOURCE_REPO }}/opencontainers/runc /src/runc
  - rm -rf /src/runc/.git
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $image_version }}
from: {{ $.Images.BASE_SCRATCH }}
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
  add: /
  to: /
  includePaths:
  - containerd
  - containerd-shim
  - containerd-shim-runc-v1
  - containerd-shim-runc-v2
  - ctr
  - runc
  - install
  - uninstall
  - containerd.service
  before: setup
docker:
  LABEL:
    distro: all
    version: all
    containerd: {{ $containerd_version }}
    runc: {{ $runc_version }}
  USER: 64535
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
final: false
from: {{ $.Images.BASE_GOLANG_23_BULLSEYE }}
import:
- image: {{ .ModuleName }}/{{ .ImageName }}-src-artifact-{{ $image_version }}
  add: /src
  to: /src
  before: install
git:
- add: /{{ $.ModulePath }}modules/007-{{ $.ModuleName }}/images/{{ $.ImageName }}/scripts
  to: /
  stageDependencies:
    install:
      - '**/*'
mount:
- fromPath: ~/go-pkg-cache
  to: /go/pkg
shell:
  beforeInstall:
  {{- include "debian packages proxy" . | nindent 2 }}
  - apt-get update && apt-get install libseccomp-dev -y
  install:
  - export GOPROXY={{ $.GOPROXY }}
  - cd /src/containerd
  - make STATIC=1 all
  - mv bin/* /
  - cd /src/runc
  - make static
  - mv runc /runc
  - mv /src/scripts/* /
