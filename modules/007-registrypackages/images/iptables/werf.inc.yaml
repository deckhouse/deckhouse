{{- $version := "1.8.8" }}
{{- $image_version := $version | replace "." "-" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $image_version }}
from: {{ $.Images.BASE_SCRATCH }}
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
  add: /
  to: /
  includePaths:
  - xtables-legacy-multi
  - xtables-nft-multi
  - iptables-apply
  - install
  - uninstall
  before: setup
docker:
  LABEL:
    distro: all
    version: all
    iptables: {{ $version }}
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
from: {{ $.Images.BASE_ALT_DEV }}
git:
  - add: /{{ $.ModulePath }}modules/007-{{ $.ModuleName }}/images/{{ $.ImageName }}/scripts
    to: /
    stageDependencies:
      setup:
      - '**/*'
shell:
  setup:
    - git clone -b libmnl-1.0.5 --depth 1 git://git.netfilter.org/libmnl /src-libmnl
    - cd /src-libmnl
    - ./autogen.sh
    - ./configure --enable-static
    - make && make install
    - git clone -b libnftnl-1.2.6 --depth 1 git://git.netfilter.org/libnftnl /src-libnftnl
    - cd /src-libnftnl
    - ./autogen.sh
    - ./configure --enable-static
    - make && make install
    - git clone -b v{{ $version }} --depth 1 git://git.netfilter.org/iptables /src
    - cd /src
    - ./autogen.sh
    - ./configure --enable-static --disable-shared
    - make LLDFLAGS=-all-static
    - mv ./iptables/xtables-legacy-multi /xtables-legacy-multi && mv ./iptables/xtables-nft-multi /xtables-nft-multi && mv ./iptables/iptables-apply /iptables-apply
    - chmod +x /xtables-legacy-multi /xtables-nft-multi /iptables-apply  /install /uninstall
