{{- $version := "1.47.0" }}
{{- $image_version := $version | replace "." "-" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $image_version }}
from: {{ $.Images.BASE_SCRATCH }}
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
  add: /
  to: /
  includePaths:
  - mke2fs
  - fsck
  - blkid
  - tune2fs
  - install
  - uninstall
  before: setup
docker:
  LABEL:
    distro: all
    version: all
    mke2fs: {{ $version }}
    fsck: {{ $version }}
    blkid: {{ $version }}
    tune2fs: {{ $version }}
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
from: {{ $.Images.BASE_ALPINE_DEV }}
git:
  - add: /{{ $.ModulePath }}modules/007-{{ $.ModuleName }}/images/{{ $.ImageName }}/scripts
    to: /
    stageDependencies:
      setup:
      - '**/*'
shell:
  beforeInstall:
    - apk add --no-cache openssh-client
  setup:
    - mkdir -p ~/.ssh && echo "StrictHostKeyChecking accept-new" > ~/.ssh/config
    - git clone -b v{{ $version }} --depth 1 {{ $.SOURCE_REPO }}/ext2/e2fsprogs.git src
    - cd /src
    - CFLAGS='-static' LDFLAGS="-static" ./configure
    - make
    - strip ./misc/mke2fs && strip ./misc/fsck ./misc/blkid ./misc/tune2fs
    - mv ./misc/mke2fs /mke2fs && mv ./misc/fsck /fsck && mv ./misc/blkid /blkid && mv ./misc/tune2fs /tune2fs
    - chmod +x /mke2fs /fsck /blkid /tune2fs /install /uninstall
