{{- $version := "2.23" }}
{{- $image_version := $version | replace "." "-" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $image_version }}
fromImage: builder/scratch
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
  add: /
  to: /
  includePaths:
  - which
  - install
  - uninstall
  before: setup
imageSpec:
  config:
    labels: {"distro": "all", "version": "all", "which": "{{ $version }}" }
    clearUser: true
    clearWorkingDir: true
    clearCmd: true
    clearEntrypoint: true
    removeEnv: ["/.*/"]
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact-{{ $image_version }}
final: false
fromImage: common/src-artifact
git:
- add: /{{ $.ModulePath }}/modules/007-{{ $.ModuleName }}/images/{{ $.ImageName }}/scripts
  to: /src/scripts
  stageDependencies:
    install:
    - '**/*'
shell:
  install:
  - wget https://ftp.gnu.org/gnu/which/which-{{ $version }}.tar.gz -O /src/which.tar.gz
  - tar -xzf /src/which.tar.gz -C /src
  - mv /src/which-{{ $version }} /src/which
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
fromImage: builder/golang-alpine
final: false
mount:
{{ include "mount points for golang builds" . }}
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact-{{ $image_version }}
  add: /src
  to: /src
  before: setup
shell:
  install:
  {{- include "alpine packages proxy" . | nindent 2 }}
  - apk add --no-cache make build-base gcc binutils
  setup:
  - cd /src/which
  - export CFLAGS="-static -O2 -s"
  - export LDFLAGS="-static"
  - ./configure --disable-nls --disable-rpath
  - make
  - strip ./which
  - ./which --version
  - mv ./which /which
  - mv /src/scripts/* /
  - chmod +x /which /install /uninstall