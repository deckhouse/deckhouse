{{- $version := "2.40.1" }}
{{- $image_version := $version | replace "." "-" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $image_version }}
from: {{ $.Images.BASE_SCRATCH }}
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
  add: /
  to: /
  includePaths:
  - mount
  - umount
  - install
  - uninstall
  before: setup
docker:
  LABEL:
    distro: all
    version: all
    mount: {{ $version }}
    umount: {{ $version }}
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $image_version }}
from: {{ $.Images.BASE_ALT_DEV }}
git:
  - add: /{{ $.ModulePath }}modules/007-{{ $.ModuleName }}/images/{{ $.ImageName }}/scripts
    to: /
    stageDependencies:
      setup:
      - '**/*'
shell:
  setup:
    - git clone -b v{{ $version }} --depth 1 {{ $.SOURCE_REPO }}/util-linux/util-linux.git src
    - cd /src
    - ./autogen.sh 
    - ./configure  --enable-static-programs=mount,umount --disable-shared --disable-liblastlog2
    - make mount.static umount.static
    - strip ./mount.static && strip ./umount.static
    - mv ./mount.static /mount && mv ./umount.static /umount
    - chmod +x /mount /umount /install /uninstall
