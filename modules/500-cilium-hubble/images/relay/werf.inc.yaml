{{- $ciliumVersion := "1.14.14" }}
# Based on https://github.com/cilium/cilium/blob/v1.14.14/images/hubble-relay/Dockerfile
---
artifact: {{ .ModuleName }}/{{ .ImageName }}-src-artifact
fromArtifact: src-artifact
git:
- add: /{{ $.ModulePath }}modules/{{ $.ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/patches
  to: /patches
  stageDependencies:
    install:
      - '**/*'
shell:
  install:
  - git clone --depth 1 --branch v{{ $ciliumVersion }} {{ $.SOURCE_REPO }}/cilium/cilium.git /src/cilium
  - cd /src/cilium
  - find /patches -name '*.patch' | xargs git apply --verbose
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-build-artifact
from: {{ $.Images.BASE_GOLANG_23_ALPINE }}
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src
  to: /src
  before: install
mount:
- fromPath: ~/go-pkg-cache
  to: /go/pkg
shell:
  beforeInstall:
  {{- include "alpine packages proxy" . | nindent 2 }}
  - apk add --no-cache make git bash
  install:
  - export GO_VERSION=${GOLANG_VERSION}
  - export GOPROXY={{ $.GOPROXY }}
  - export GOOS=linux GOARCH=amd64
  - cd /src/cilium
  - go mod tidy && go mod vendor && go mod verify
  - cd /src/cilium/hubble-relay
  - make
  - chown 64535:64535 /src/cilium/hubble-relay/hubble-relay
  - chmod 0700 /src/cilium/hubble-relay/hubble-relay
---
# Distroless Main Image
---
image: {{ $.ModuleName }}/{{ $.ImageName }}
fromImage: common/distroless
import:
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-build-artifact
  add: /src/cilium/hubble-relay/hubble-relay
  to: /usr/local/bin/hubble-relay
  before: install
- artifact: cni-cilium/gops-artifact
  add: /out/linux/amd64/bin/gops
  to: /bin/gops
  before: install
docker:
  USER: 64535
  ENTRYPOINT: ["/usr/local/bin/hubble-relay", "serve"]
