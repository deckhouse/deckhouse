apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: cert-manager
  namespace: kube-cert-manager
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: cert-manager
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: cert-manager
