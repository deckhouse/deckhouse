apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: cainjector
  namespace: kube-cert-manager
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: cainjector
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: cainjector
