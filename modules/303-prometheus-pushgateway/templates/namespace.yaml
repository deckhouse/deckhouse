apiVersion: v1
kind: Namespace
metadata:
  name: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: {{ .Chart.Name }}
  annotations:
    extended-monitoring.flant.com/enabled: ""
