apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: prometheus-pushgateway
    rbac.deckhouse.io/aggregate-to-observability-as: manager
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: kube-prometheus-pushgateway
  name: d8:system-capability:prometheus-pushgateway:edit
  annotations:
    en.meta.deckhouse.io/title: "Module prometheus-pushgateway: edit configuration"
    ru.meta.deckhouse.io/title: "Модуль prometheus-pushgateway: управление конфигурацией"
    en.meta.deckhouse.io/description: "Manage the prometheus-pushgateway module configuration."
    ru.meta.deckhouse.io/description: "Управление конфигурацией модуля prometheus-pushgateway."
rules:
- apiGroups:
  - deckhouse.io
  resourceNames:
  - prometheus-pushgateway
  resources:
  - moduleconfigs
  verbs:
  - create
  - update
  - patch
  - delete
