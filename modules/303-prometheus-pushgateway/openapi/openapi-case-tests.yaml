positive:
  configValues:
  - instances: [ first, second ]
negative:
  configValues:
  - {}
  - instances: [false, true]
  - instances: ["Uppercase"]
  - instances: ["under_score"]
  - instances:
    - |-
      valid
      ---
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: pwned
        namespace: kube-prometheus-pushgateway
