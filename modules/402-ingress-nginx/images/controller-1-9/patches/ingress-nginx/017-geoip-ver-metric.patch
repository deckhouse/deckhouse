diff --git a/cmd/nginx/main.go b/cmd/nginx/main.go
index 781f3a8eb..2ecb238e9 100644
--- a/cmd/nginx/main.go
+++ b/cmd/nginx/main.go
@@ -55,6 +55,10 @@ func main() {
 
 	fmt.Println(version.String())
 
+	reg := prometheus.NewRegistry()
+	reg.MustRegister(nginx.GeoIPVersion)
+	nginx.GeoIPVersion.Set(1)
+
 	showVersion, conf, err := ingressflags.ParseFlags()
 	if showVersion {
 		os.Exit(0)
@@ -120,8 +124,6 @@ func main() {
 		klog.Fatalf("Unexpected error obtaining ingress-nginx pod: %v", err)
 	}
 
-	reg := prometheus.NewRegistry()
-
 	reg.MustRegister(collectors.NewGoCollector())
 	reg.MustRegister(collectors.NewProcessCollector(collectors.ProcessCollectorOpts{
 		PidFn:        func() (int, error) { return os.Getpid(), nil },
diff --git a/internal/nginx/maxmind.go b/internal/nginx/maxmind.go
index ba0ce42b4..4752199a4 100644
--- a/internal/nginx/maxmind.go
+++ b/internal/nginx/maxmind.go
@@ -30,6 +30,7 @@ import (
 	"syscall"
 	"time"
 
+	"github.com/prometheus/client_golang/prometheus"
 	"k8s.io/apimachinery/pkg/util/wait"
 	klog "k8s.io/klog/v2"
 )
@@ -55,6 +56,17 @@ var MaxmindRetriesTimeout = time.Second * 0
 // minimumRetriesCount minimum value of the MaxmindRetriesCount parameter. If MaxmindRetriesCount less than minimumRetriesCount, it will be set to minimumRetriesCount
 const minimumRetriesCount = 1
 
+// GeoIPVersion reflects which version of GeoIP DB is in use if any
+// 0 - no geoipdb
+// 1 - geoip v1 (predownloaded)
+// 2 - geoip v2 (downloaded)
+var GeoIPVersion = prometheus.NewGauge(
+	prometheus.GaugeOpts{
+		Name: "geoip_version",
+		Help: "The version of GeoIP DB",
+	},
+)
+
 const (
 	geoIPPath   = "/etc/ingress-controller/geoip"
 	dbExtension = ".mmdb"
@@ -122,6 +134,7 @@ func DownloadGeoLite2DB(attempts int, period time.Duration) error {
 
 		lastErr = dlError
 		if dlError == nil {
+			GeoIPVersion.Set(2)
 			klog.Infof("GeoLite2 DBs downloaded successfully")
 			return true, nil
 		}
