{{- $controllerBranch := "controller-v1.12.1" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
fromImage: common/src-artifact
fromCacheVersion: "2025-08-07.03"
final: false
git:
- add: /{{ $.ModulePath }}modules/{{ $.ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/patches
  to: /patches
  stageDependencies:
    install:
      - '**/*'
- add: /{{ $.ModulePath }}modules/{{ $.ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/rootfs
  to: /src/rootfs
  stageDependencies:
    install:
      - '**/*'
- add: /{{ $.ModulePath }}modules/{{ $.ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/curl-chroot-wrapper.sh
  to: /src/curl-chroot-wrapper.sh
  stageDependencies:
    install:
      - '**/*'
- add: /{{ $.ModulePath }}modules/{{ $.ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/nginx-chroot-wrapper.sh
  to: /src/nginx-chroot-wrapper.sh
  stageDependencies:
    install:
      - '**/*'
secrets:
- id: SOURCE_REPO
  value: {{ .SOURCE_REPO }}
shell:
  setup:
  - mkdir -p /src
  - cd /src
  - git clone --branch 0.5.1 --depth 1 $(cat /run/secrets/SOURCE_REPO)/starwing/lua-protobuf
  - git clone --branch 7-3 --depth 1 $(cat /run/secrets/SOURCE_REPO)/luarocks-sorces/lua-iconv
  #
  - git clone --branch {{ $controllerBranch }} --depth 1 $(cat /run/secrets/SOURCE_REPO)/kubernetes/ingress-nginx.git
  - cd /src/ingress-nginx
  - git apply /patches/*.patch --verbose
  - echo "export COMMIT_SHA=git-$(git rev-parse --short HEAD)" > .env_pass
  - echo "export REPO_INFO=$(git config --get remote.origin.url)" >> .env_pass
  - echo "export TAG=$(git describe --tags --always)" >> .env_pass
  #
  - mkdir -p /src/nginx-deps
  - git clone -b {{ $controllerBranch }} $(cat /run/secrets/SOURCE_REPO)/kubernetes/ingress-nginx-deps.git /src/nginx-deps
    # Dependency for opentelemetry-cpp (source: opentelemetry-cpp/cmake/nlohmann-json.cmakee)
  - mkdir -p /src/nginx-deps/opentelemetry-cpp/third_party/nlohmann-json
  - git clone -b v3.11.3 $(cat /run/secrets/SOURCE_REPO)/nlohmann/json.git /src/nginx-deps/opentelemetry-cpp/third_party/nlohmann-json
  #
  - cd /src/nginx-deps
  - git clone --depth=100 $(cat /run/secrets/SOURCE_REPO)/google/ngx_brotli.git
  - cd ngx_brotli
  - git reset --hard 63ca02abdcf79c9e788d2eedcc388d2335902e52
  - git submodule init
  - git submodule set-url deps/brotli "$(cat /run/secrets/SOURCE_REPO)/google/brotli.git"
  - git submodule update
  #
  - cd /src/nginx-deps
  - git clone --depth=1 $(cat /run/secrets/SOURCE_REPO)/ssdeep-project/ssdeep
  #
  - cd /src/nginx-deps
  - git clone -b v3.0.14 $(cat /run/secrets/SOURCE_REPO)/SpiderLabs/ModSecurity
  - cd ModSecurity/
  - git submodule init
  - git submodule set-url test/test-cases/secrules-language-tests "$(cat /run/secrets/SOURCE_REPO)/SpiderLabs/secrules-language-tests"
  - git submodule set-url others/libinjection "$(cat /run/secrets/SOURCE_REPO)/libinjection/libinjection.git"
  - git submodule set-url bindings/python "$(cat /run/secrets/SOURCE_REPO)/SpiderLabs/ModSecurity-Python-bindings.git"
  - git submodule set-url others/mbedtls "$(cat /run/secrets/SOURCE_REPO)/Mbed-TLS/mbedtls.git"
  - git submodule set-url others/mbedtls/framework "$(cat /run/secrets/SOURCE_REPO)/Mbed-TLS/mbedtls-framework"
  - git submodule set-url others/mbedtls/tf-psa-crypto "$(cat /run/secrets/SOURCE_REPO)/Mbed-TLS/TF-PSA-Crypto.git"
  - git submodule set-url others/mbedtls/tf-psa-crypto/framework "$(cat /run/secrets/SOURCE_REPO)/Mbed-TLS/mbedtls-framework"
  - git submodule update
  #
  - cd /src/nginx-deps
  - git clone $(cat /run/secrets/SOURCE_REPO)/open-telemetry/opentelemetry-cpp-contrib.git opentelemetry-cpp-contrib
  - cd /src/nginx-deps/opentelemetry-cpp-contrib
  - git reset --hard 8933841f0a7f8737f61404cf0a64acf6b079c8a5
  #
  - cd /src/nginx-deps
  - git clone -b v4.10.0 $(cat /run/secrets/SOURCE_REPO)/coreruleset/coreruleset
  - mv coreruleset owasp-modsecurity-crs
  #
  - rm -rf /src/lua-protobuf/.git
  - rm -rf /src/lua-iconv/.git
  - rm -rf /src/ingress-nginx/.git
  - rm -rf /src/nginx-deps/.git
  - rm -rf /src/nginx-deps/opentelemetry-cpp/third_party/nlohmann-json/.git
  - rm -rf /src/ngx_brotli/.git
  - rm -rf /src/ngx_brotli/deps/brotli/.git
  - rm -rf /src/ssdeep/.git
  - rm -rf /src/ModSecurity/.git
  - rm -rf /src/ModSecurity/test/test-cases/secrules-language-tests/.git
  - rm -rf /src/ModSecurity/others/libinjection/.git
  - rm -rf /src/ModSecurity/bindings/python/.git
  - rm -rf /src/ModSecurity/others/mbedtls/.git
  - rm -rf /src/ModSecurity/others/mbedtls/framework/.git
  - rm -rf /src/ModSecurity/others/mbedtls/tf-psa-crypto/.git
  - rm -rf /src/ModSecurity/others/mbedtls/tf-psa-crypto/framework.git
  - rm -rf /src/nginx-deps/opentelemetry-cpp-contrib/.git
  - rm -rf /src/owasp-modsecurity-crs/.git
---
image: {{ .ModuleName }}/{{ $.ImageName }}-controller-artifact
fromImage: builder/golang-alpine
fromCacheVersion: "2025-08-04.03"
final: false
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src/ingress-nginx
  to: /src
  before: install
mount:
{{ include "mount points for golang builds" . }}
secrets:
- id: GOPROXY
  value: {{ .GOPROXY }}
shell:
  install:
  {{- include "alpine packages proxy" . | nindent 2 }}
  - apk --no-cache add make bash
  setup:
  - export GOPROXY=$(cat /run/secrets/GOPROXY)
  - export GOARCH=amd64
  - export PKG="k8s.io/ingress-nginx"
  - cd /src/
  - go mod edit -go=1.23.6
  - go mod tidy
  - |
    source /src/.env_pass && \
    echo "Loaded: COMMIT_SHA=$COMMIT_SHA, REPO_INFO=$REPO_INFO, ARCH=$GOARCH, PKG=$PKG TAG=$TAG" && \
    make GO111MODULE=on USE_DOCKER=false build
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-abseil
# from: dev-registry.deckhouse.io/base_images@sha256:4639a3555de0fbd0855e417ad667d16b86b08fa5d21313144262682e8dd34a26
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-brotli
# from: dev-registry.deckhouse.io/base_images@sha256:5bd143b84d1dde8841b7cd1e2d621ea59fe6ef9938acc4b06922e1fe4928dc68
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-libffi
# from: dev-registry.deckhouse.io/base_images@sha256:c0781182bd2b1218d235b1f5d7cc09ac20e4ff4954e840ac4862fbbdfb213173
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-gmp
# from: dev-registry.deckhouse.io/base_images@sha256:6e3839c1ffdf3341bdf8e63f3c50f2920eff1dbdc76d6ad38d59b55779c1d511
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-libgcrypt
# from: dev-registry.deckhouse.io/base_images@sha256:f9d3a8784767faa3dbe48330f3267eec0737cdadf3ebf99e2c3f26a101724d77
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-libgpg-error
# from: dev-registry.deckhouse.io/base_images@sha256:27b93b6f2ca7fa88d39259e9adb8ad6c4d5561ff1e7e611196b9efe4c42d71c8
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-krb5
# from: dev-registry.deckhouse.io/base_images@sha256:54f8cd1e23c46cb0342dc70cd10333bacb60ed142e681062ff6f46f7c26a2bb1
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-pcre2
# from: dev-registry.deckhouse.io/base_images@sha256:fad2fd8fe87db24253398c3a3e03464936fa34862e6bd1e5853bd0bfefb169cb
# final: false
# ---
# image: {{ $.ModuleName }}/{{ $.ImageName }}-zstd
# from: dev-registry.deckhouse.io/base_images@sha256:85e8330e51b5c47ac58e489970eddb76842d86432e1b4ddb14f64ae4e9535451
# final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-protobuf
from: dev-registry.deckhouse.io/base_images@sha256:93d2460c69ad88a016ea2b4c059141ae5ce49eabc802aa444a8cf8832ec5e19e
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-nginx-artifact
fromImage: builder/alpine
fromCacheVersion: "2025-08-07.03"
final: false
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src/ingress-nginx/images/nginx/rootfs/
  to: /
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src/rootfs/etc
  to: /src/etc
  before: install
# - image: base/distroless
#   add: /usr
#   to: /chroot/usr
#   before: setup
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src/nginx-deps
  to: /src/nginx-deps
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-protobuf
  add: /
  to: /
  includePaths:
  - usr/include/*
  - usr/bin/*
  - usr/lib/*
  before: install
secrets:
- id: SOURCE_REPO
  value: {{ .SOURCE_REPO }}
shell:
  beforeInstall:
  {{- include "alpine packages proxy" . | nindent 2 }}
  - apk add --no-cache linux-headers autoconf automake libtool bash pkgconfig ninja
  - apk add --no-cache build-base g++ cmake # FIXME bison flex
  # Mandatory
  - apk add pcre-dev
  - apk add curl-dev
  - apk add libmaxminddb-dev
  - apk add grpc-dev
  # - apk add protobuf-dev
  # Maybe need (thinking)
  # - apk add libxml2
  # - apk add yajl-dev lmdb-dev
  # ca-certificates

# ModSecurity -  for Linux

# Mandatory dependencies
#   + libInjection                                  ....
#   + Mbed TLS                                      ....
#   + SecLang tests                                 ....

# Optional dependencies
#   + GeoIP/MaxMind                                 ....found
#      * (MaxMind) v1.9.1
#         -lmaxminddb, -DWITH_MAXMIND
#   + LibCURL                                       ....found v8.12.1
#      -lcurl,  -DWITH_CURL_SSLVERSION_TLSv1_2 -DWITH_CURL
#   + YAJL                                          ....not found
#   + LMDB                                          ....not found
#   + LibXML2                                       ....not found
#   + SSDEEP                                        ....found
#      -lfuzzy -L/usr/local/lib/, -DWITH_SSDEEP -I/usr/local/include
#   + LUA                                           ....not found
#   + PCRE2                                          ....not found

  # Optional
  # - apk add libxslt-dev
  # - apk add gd-dev
  # - apk add openssl-dev
  # - apk add libedit-dev
  # - apk add libaio-dev
  # - apk add perl-dev
  # - apk add libc-dev
  # - apk add zlib-dev
  # - apk add libxml2-dev
  # - apk add gtest-dev
  # - apk add c-ares-dev
  # - apk add re2-dev

  # - apk add patch
  # - apk add openssl
  # - apk add libprotobuf
  # - apk add python3
  # - apk add yaml-cpp
  # mercurial alpine-sdk findutils curl lmdb-tools wget doxygen bc unzip dos2unix coreutils
  install:
  # Add deckhouse user
  - echo "deckhouse:x:64535:64535:deckhouse:/:/sbin/nologin" >> /etc/passwd
  - echo "deckhouse:x:64535:" >> /etc/group
  - echo "deckhouse:!::0:::::" >> /etc/shadow
  #
  - cp -R /src/etc/* /etc/
  - cp -R /src/nginx-deps/owasp-modsecurity-crs /etc/nginx/

  # BUILD NGINX and components (source: images/nginx/rootfs/build.sh)
  - export NGINX_VERSION=1.25.5
  - export BUILD_PATH=/src/nginx-deps
  - mkdir -p /etc/nginx
  - cd "$BUILD_PATH"

  # Improve compilation times
  - CORES=$(($(grep -c ^processor /proc/cpuinfo) - 1))
  - export MAKEFLAGS=-j${CORES}
  - export CTEST_BUILD_FLAGS=${MAKEFLAGS}

  # Install luajit from openresty fork
  - export LUAJIT_LIB=/usr/local/lib
  - export LUA_LIB_DIR="$LUAJIT_LIB/lua"
  - export LUAJIT_INC=/usr/local/include/luajit-2.1

  - cd "$BUILD_PATH/luajit2"
  - make CCDEBUG=-g
  - make install

  - ln -s /usr/local/bin/luajit /usr/local/bin/lua
  - ln -s "$LUAJIT_INC" /usr/local/include/lua

  - cd "$BUILD_PATH/opentelemetry-cpp"
  - export CXXFLAGS="-DBENCHMARK_HAS_NO_INLINE_ASSEMBLY"

  - mkdir .git # A hack to trick the builder
  - cmake -B build -G Ninja -Wno-dev
    -DOTELCPP_PROTO_PATH="${BUILD_PATH}/opentelemetry-proto/"
    -DCMAKE_INSTALL_PREFIX=/usr
    -DBUILD_SHARED_LIBS=ON
    -DBUILD_TESTING="OFF"
    -DBUILD_W3CTRACECONTEXT_TEST="OFF"
    -DCMAKE_BUILD_TYPE=None
    -DWITH_ABSEIL=ON
    -DWITH_STL=ON
    -DWITH_EXAMPLES=OFF
    -DWITH_ZPAGES=OFF
    -DWITH_OTLP_GRPC=ON
    -DWITH_OTLP_HTTP=ON
    -DWITH_ZIPKIN=ON
    -DWITH_PROMETHEUS=OFF
    -DWITH_ASYNC_EXPORT_PREVIEW=OFF
    -DWITH_METRICS_EXEMPLAR_PREVIEW=OFF
  - cmake --build build
  - cmake --install build

  # Build ssdeep
  - cd "$BUILD_PATH"
  - cd ssdeep/
  - ./bootstrap
  - ./configure
  - make
  - make install

  # Build modsecurity library
  - cd "$BUILD_PATH"
  - cd ModSecurity/
  - sh build.sh

  # https://github.com/SpiderLabs/ModSecurity/issues/1909#issuecomment-465926762
  - sed -i '115i LUA_CFLAGS="${LUA_CFLAGS} -DWITH_LUA_JIT_2_1"' build/lua.m4
  - sed -i '117i AC_SUBST(LUA_CFLAGS)' build/lua.m4

  - ./configure
    --disable-doxygen-doc
    --disable-doxygen-html
    --disable-examples
  - make
  - make install

  - mkdir -p /etc/nginx/modsecurity
  - cp modsecurity.conf-recommended /etc/nginx/modsecurity/modsecurity.conf
  - cp unicode.mapping /etc/nginx/modsecurity/unicode.mapping

  # Replace serial logging with concurrent
  - sed -i 's|SecAuditLogType Serial|SecAuditLogType Concurrent|g' /etc/nginx/modsecurity/modsecurity.conf

  # Concurrent logging implies the log is stored in several files
  - echo "SecAuditLogStorageDir /var/log/audit/" >> /etc/nginx/modsecurity/modsecurity.conf

  # Prepare owasp modsecurity crs
  - cd /etc/nginx/owasp-modsecurity-crs
  - mv crs-setup.conf.example crs-setup.conf
  - mv rules/REQUEST-900-EXCLUSION-RULES-BEFORE-CRS.conf.example rules/REQUEST-900-EXCLUSION-RULES-BEFORE-CRS.conf
  - mv rules/RESPONSE-999-EXCLUSION-RULES-AFTER-CRS.conf.example rules/RESPONSE-999-EXCLUSION-RULES-AFTER-CRS.conf
  - cd ..

  # OWASP CRS v4 rules
  - |
    echo "
    Include /etc/nginx/owasp-modsecurity-crs/crs-setup.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-900-EXCLUSION-RULES-BEFORE-CRS.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-901-INITIALIZATION.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-905-COMMON-EXCEPTIONS.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-911-METHOD-ENFORCEMENT.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-913-SCANNER-DETECTION.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-920-PROTOCOL-ENFORCEMENT.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-921-PROTOCOL-ATTACK.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-922-MULTIPART-ATTACK.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-930-APPLICATION-ATTACK-LFI.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-931-APPLICATION-ATTACK-RFI.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-932-APPLICATION-ATTACK-RCE.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-933-APPLICATION-ATTACK-PHP.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-934-APPLICATION-ATTACK-GENERIC.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-941-APPLICATION-ATTACK-XSS.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-942-APPLICATION-ATTACK-SQLI.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-943-APPLICATION-ATTACK-SESSION-FIXATION.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-944-APPLICATION-ATTACK-JAVA.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/REQUEST-949-BLOCKING-EVALUATION.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-950-DATA-LEAKAGES.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-951-DATA-LEAKAGES-SQL.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-952-DATA-LEAKAGES-JAVA.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-953-DATA-LEAKAGES-PHP.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-954-DATA-LEAKAGES-IIS.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-955-WEB-SHELLS.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-959-BLOCKING-EVALUATION.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-980-CORRELATION.conf
    Include /etc/nginx/owasp-modsecurity-crs/rules/RESPONSE-999-EXCLUSION-RULES-AFTER-CRS.conf
    " > /etc/nginx/owasp-modsecurity-crs/nginx-modsecurity.conf

  - sed -i "s/ngx_feature_test='printf(\"hello\");'/ngx_feature_test='msc_init();'/" $BUILD_PATH/ModSecurity-nginx/config

  # build nginx
  - cd "$BUILD_PATH/nginx-$NGINX_VERSION"

  # apply nginx patches
  - |
    for PATCH in `ls /patches`;do
      echo "Patch: $PATCH"
      if [[ "$PATCH" == *.txt ]]; then
        patch -p0 < /patches/$PATCH
      else
        patch -p1 < /patches/$PATCH
      fi
    done

  - WITH_FLAGS="--with-debug
    --with-compat
    --with-pcre-jit
    --with-http_ssl_module
    --with-http_stub_status_module
    --with-http_realip_module
    --with-http_auth_request_module
    --with-http_addition_module
    --with-http_gzip_static_module
    --with-http_sub_module
    --with-http_v2_module
    --with-http_v3_module
    --with-stream
    --with-stream_ssl_module
    --with-stream_realip_module
    --with-stream_ssl_preread_module
    --with-threads
    --with-http_secure_link_module
    --with-http_gunzip_module"

  # "Combining -flto with -g is currently experimental and expected to produce unexpected results."
  # https://gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html
  - CC_OPT="-g -O2 -fPIE -fstack-protector-strong
    -Wformat
    -Werror=format-security
    -Wno-deprecated-declarations
    -fno-strict-aliasing
    -D_FORTIFY_SOURCE=2
    --param=ssp-buffer-size=4
    -DTCP_FASTOPEN=23
    -fPIC
    -Wno-cast-function-type"

  - LD_OPT="-fPIE -fPIC -pie -Wl,-z,relro -Wl,-z,now"
  - CC_OPT+=' -m64 -mtune=generic'

  - WITH_MODULES="
    --add-module=$BUILD_PATH/ngx_devel_kit
    --add-module=$BUILD_PATH/set-misc-nginx-module
    --add-module=$BUILD_PATH/headers-more-nginx-module
    --add-module=$BUILD_PATH/ngx_http_substitutions_filter_module
    --add-module=$BUILD_PATH/lua-nginx-module
    --add-module=$BUILD_PATH/stream-lua-nginx-module
    --add-module=$BUILD_PATH/lua-upstream-nginx-module
    --add-dynamic-module=$BUILD_PATH/nginx-http-auth-digest
    --add-dynamic-module=$BUILD_PATH/ModSecurity-nginx
    --add-dynamic-module=$BUILD_PATH/ngx_http_geoip2_module
    --add-dynamic-module=$BUILD_PATH/ngx_brotli"

  - ./configure
    --prefix=/usr/local/nginx
    --conf-path=/etc/nginx/nginx.conf
    --modules-path=/etc/nginx/modules
    --http-log-path=/var/log/nginx/access.log
    --error-log-path=/var/log/nginx/error.log
    --lock-path=/var/lock/nginx.lock
    --pid-path=/run/nginx.pid
    --http-client-body-temp-path=/var/lib/nginx/body
    --http-fastcgi-temp-path=/var/lib/nginx/fastcgi
    --http-proxy-temp-path=/var/lib/nginx/proxy
    --http-scgi-temp-path=/var/lib/nginx/scgi
    --http-uwsgi-temp-path=/var/lib/nginx/uwsgi
    ${WITH_FLAGS}
    --without-mail_pop3_module
    --without-mail_smtp_module
    --without-mail_imap_module
    --without-http_uwsgi_module
    --without-http_scgi_module
    --with-cc-opt="${CC_OPT}"
    --with-ld-opt="${LD_OPT}"
    --user=deckhouse
    --group=deckhouse
    ${WITH_MODULES}

  - make
  - make modules
  - make install

  - cd ${BUILD_PATH}/opentelemetry-cpp-contrib

  - export OTEL_TEMP_INSTALL=/tmp/otel
  - mkdir -p ${OTEL_TEMP_INSTALL}

  - cd ${BUILD_PATH}/opentelemetry-cpp-contrib/instrumentation/nginx
  # remove downloading nginx from cmake and clone it manually
  # by default cmake download nginx from internet
  # and change cmake NGINX_DIR
  - sed -e "35d" nginx.cmake | sed -e "35i set(NGINX_DIR \"${BUILD_PATH}/opentelemetry-cpp-contrib/instrumentation/nginx/build/nginx/\")" | sed -e "27d" | sed -e "27i \ \ SOURCE_DIR nginx" > nginx.cmake.tmp
  - rm -f nginx.cmake
  - mv nginx.cmake.tmp nginx.cmake
  - cat nginx.cmake
  - mkdir -p build
  - cp -R "${BUILD_PATH}/nginx-$NGINX_VERSION" build/nginx
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=Release
    -G Ninja
    -DCMAKE_CXX_STANDARD=17
    -DCMAKE_INSTALL_PREFIX=${OTEL_TEMP_INSTALL}
    -DBUILD_SHARED_LIBS=ON
    -DNGINX_VERSION=${NGINX_VERSION}
    ..

  - cmake --build . -j ${CORES} --target install

  - mkdir -p /etc/nginx/modules
  - cp ${OTEL_TEMP_INSTALL}/otel_ngx_module.so /etc/nginx/modules/otel_ngx_module.so


  - cd "$BUILD_PATH/lua-resty-core"
  - make install

  - cd "$BUILD_PATH/lua-resty-balancer"
  - make all
  - make install

  - export LUA_INCLUDE_DIR=/usr/local/include/luajit-2.1
  - ln -s $LUA_INCLUDE_DIR /usr/include/lua5.1

  - cd "$BUILD_PATH/lua-cjson"
  - make all
  - make install

  - cd "$BUILD_PATH/lua-resty-cookie"
  - make all
  - make install

  - cd "$BUILD_PATH/lua-resty-lrucache"
  - make install

  - cd "$BUILD_PATH/lua-resty-dns"
  - make install

  - cd "$BUILD_PATH/lua-resty-lock"
  - make install

  # required for OCSP verification
  - cd "$BUILD_PATH/lua-resty-http"
  - make install

  - cd "$BUILD_PATH/lua-resty-upload"
  - make install

  - cd "$BUILD_PATH/lua-resty-string"
  - make install

  - cd "$BUILD_PATH/lua-resty-memcached"
  - make install

  - cd "$BUILD_PATH/lua-resty-redis"
  - make install

  - cd "$BUILD_PATH/lua-resty-ipmatcher"
  - INST_LUADIR=/usr/local/lib/lua make install

  - cd "$BUILD_PATH/mimalloc"
  - mkdir -p out/release
  - cd out/release

  - cmake ../..

  - make
  - make install

  # update image permissions
  - |
    writeDirs=( \
      /etc/nginx \
      /usr/local/nginx \
      /opt/modsecurity/var/log \
      /opt/modsecurity/var/upload \
      /opt/modsecurity/var/audit \
      /var/log/audit \
      /var/log/nginx \
    );
    for dir in "${writeDirs[@]}"; do
      mkdir -p ${dir};
      chown -R deckhouse:deckhouse ${dir};
    done
  - rm -rf /etc/nginx/owasp-modsecurity-crs/.git
  - rm -rf /etc/nginx/owasp-modsecurity-crs/tests

  # remove .a files
  - find /usr/local -name "*.a" -print | xargs /bin/rm
  setup:
  # Source: rootfs/chroot.sh
  - |
    bash -eu -c '
    writeDirs=(
      /chroot/etc/nginx
      /chroot/usr/local
      /chroot/etc/ingress-controller
      /chroot/etc/ingress-controller/ssl
      /chroot/etc/ingress-controller/auth
      /chroot/etc/ingress-controller/telemetry
      /chroot/etc/ingress-controller/geoip
      /chroot/opt/modsecurity/var/log
      /chroot/opt/modsecurity/var/upload
      /chroot/opt/modsecurity/var/audit
      /chroot/var/log/audit
      /chroot/var/lib/nginx
      /chroot/var/log/nginx
      /chroot/var/lib/nginx/body
      /chroot/var/lib/nginx/fastcgi
      /chroot/var/lib/nginx/proxy
      /chroot/var/lib/nginx/scgi
      /chroot/var/lib/nginx/uwsgi
      /chroot/tmp/nginx
    );
    for dir in "${writeDirs[@]}"; do
      mkdir -p ${dir};
      chown -R deckhouse:deckhouse ${dir};
    done'
  # Create opentelemetry.toml file as it doesn't present in controller_image
  - mkdir -p /etc/nginx && touch /etc/nginx/opentelemetry.toml
  - mkdir -p /etc/ingress-controller/telemetry && touch /etc/ingress-controller/telemetry/opentelemetry.toml
  - chown -R deckhouse:deckhouse /etc/nginx/opentelemetry.toml /etc/ingress-controller/telemetry/opentelemetry.toml
  #
  - mkdir -p  /chroot/lib /chroot/proc /chroot/usr /chroot/bin /chroot/dev /chroot/run
  - cp /etc/passwd /etc/group /etc/hosts /chroot/etc/
  - cp -a /usr/* /chroot/usr/ # FIXME delete
  - cp -a /etc/nginx/* /chroot/etc/nginx/
  - cp -a /etc/ingress-controller/* /chroot/etc/ingress-controller/
  - cp /lib/ld-musl-* /chroot/lib/ # FIXME delete
  - cp /usr/lib/libcrypto* /usr/lib/libssl* /usr/lib/libz* /chroot/usr/lib/ # FIXME delete
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-lua-protobuf
from: dev-registry.deckhouse.io/base_images@sha256:bfd71542919fd099c875d4090e581ff1a4f8d35ed70f3e6689ba83cde3068c7e
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-lua-iconv
from: dev-registry.deckhouse.io/base_images@sha256:4d5dca4e0b7a5e9e544f05f911b09d2ea7e2777b77ffc13df399b4e893645e01
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-chroot-artifact
fromImage: builder/alpine
fromCacheVersion: "2025-08-07.05"
final: false
mount:
- from: build_dir
  to: /tmp/apps
import:
- image: tools/libcap
  add: /usr/sbin/setcap
  to: /tools/setcap
  before: install
- image: registrypackages/d8-curl-artifact-8-9-1 # FIXME rebuild!
  add: /d8-curl
  to: /chroot/usr/bin/curl
  owner: "64535"
  group: "64535"
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-nginx-artifact
  add: /
  to: /
  includePaths:
  - chroot
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-lua-iconv
  add: /
  to: /chroot
  includePaths:
  - lib/lua/5.1/iconv.so
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-lua-protobuf
  add: /
  to: /chroot
  includePaths:
  - lib/lua/5.1/pb.so
  - share/lua/5.1/protoc.lua
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-controller-artifact
  add: /src/rootfs/etc
  to: /src/rootfs/etc
  before: install
secrets:
- id: SOURCE_REPO
  value: {{ .SOURCE_REPO }}
shell:
  setup:
  # Add deckhouse user
  - echo "deckhouse:x:64535:64535:deckhouse:/:/sbin/nologin" >> /etc/passwd
  - echo "deckhouse:x:64535:" >> /etc/group
  - echo "deckhouse:!::0:::::" >> /etc/shadow

  # Source: rootfs/Dockerfile-chroot
  - cp -a /src/rootfs/etc/* /chroot/etc
  - chown -R deckhouse:deckhouse /chroot/etc
  - chown root:root /chroot/etc/passwd /chroot/etc/group

  - ln -s /usr/local/nginx/sbin/nginx /sbin/nginx

  - mkdir -p /var/log
  - chown -R deckhouse:deckhouse /var/log

  # LD_LIBRARY_PATH does not work so below is needed for  opentelemetry/other modules
  # Put libs of newer modules under `/modules_mount/<other>/lib` and add that path below
  # Could get complicated arch specific paths become a need
  - echo "/lib:/usr/lib:/usr/local/lib:/modules_mount/etc/nginx/modules/otel" > /chroot/etc/ld-musl-x86_64.path
  #
  - ln -sf /chroot/etc/nginx /etc/nginx
  - ln -sf /chroot/tmp/nginx /tmp/nginx
  - ln -sf /chroot/etc/ingress-controller /etc/ingress-controller
  - ln -sf /chroot/var/log/nginx /var/log/nginx
  - touch /chroot/var/log/nginx/access.log
  - chown deckhouse:deckhouse /chroot/var/log/nginx/access.log
  - echo "" > /chroot/etc/resolv.conf
  - chown -R deckhouse:deckhouse /chroot/var/log/nginx /chroot/etc/resolv.conf
  - mknod -m 0666 /chroot/dev/null c 1 3
  - mknod -m 0666 /chroot/dev/random c 1 8
  - mknod -m 0666 /chroot/dev/urandom c 1 9
  - mknod -m 0666 /chroot/dev/full c 1 7
  - mknod -m 0666 /chroot/dev/ptmx c 5 2
  - mknod -m 0666 /chroot/dev/zero c 1 5
  - mknod -m 0666 /chroot/dev/tty c 5 0

  - mkdir -p /chroot/modules_mount
  - mkdir -p modules_mount
  - ln -s /modules_mount /chroot/modules_mount

  # Utils for final image
  - cp -a /tools/setcap /tmp/apps
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-bash # FIXME delete
from: dev-registry.deckhouse.io/base_images@sha256:7cd24a890378d0c527ad3baf555c2ce59bd34dea2835a0ab831e00e715c59f43
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-procps # FIXME delete
from: dev-registry.deckhouse.io/base_images@sha256:053f0fe0e754b2d5bbb8df9e7f81fd346f61e80c94aeb6c94687876316d99b5d
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-diffutils # FIXME delete
from: dev-registry.deckhouse.io/base_images@sha256:5dbcc20e9deb7d020388e16d726475c1faa1270f1b7800cb0348d1c5c54d9763
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-dumb-init # FIXME delete
from: dev-registry.deckhouse.io/base_images@sha256:2c249890508acd50a61cb216cb1ca8e9263601547a88ee93de814bce0a4525bb
final: false
---
image: {{ $.ModuleName }}/{{ $.ImageName }}
fromImage: builder/alpine # FIXME replace!
# fromImage: common/distroless
fromCacheVersion: "2025-08-07.04"
mount:
- from: build_dir
  to: /tmp/apps
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-chroot-artifact
  add: /
  to: /
  includePaths:
  - chroot
  - modules_mount
  - var/log
  - etc/nginx
  - tmp/nginx
  - etc/ingress-controller
  - var/log/nginx
  - sbin/nginx
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-controller-artifact
  add: /src/rootfs/bin/amd64/dbg
  to: /dbg
  owner: "64535"
  group: "64535"
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-controller-artifact
  add: /src/rootfs/bin/amd64/nginx-ingress-controller
  to: /nginx-ingress-controller
  owner: "64535"
  group: "64535"
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-controller-artifact
  add: /src/rootfs/bin/amd64/wait-shutdown
  to: /wait-shutdown
  owner: "64535"
  group: "64535"
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src/curl-chroot-wrapper.sh
  to: /usr/bin/curl
  owner: "64535"
  group: "64535"
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src/nginx-chroot-wrapper.sh
  to: /usr/bin/nginx
  owner: "64535"
  group: "64535"
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-dumb-init
  add: /dumb-init
  to: /usr/bin/dumb-init
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-bash
  add: /usr
  to: /
  includePaths:
  - bin/bash
  - bin/sh
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-procps
  add: /
  to: /
  includePaths:
  - bin/pkill
  - bin/pgrep
  before: install
- image: {{ $.ModuleName }}/{{ $.ImageName }}-diffutils
  add: /usr/bin/diff
  to: /usr/bin/diff
  before: install
- image: tools/util-linux
  add: /usr/bin/unshare
  to: /usr/bin/unshare
  before: install
- image: tools/openssl
  add: /
  to: /
  includePaths:
  - usr/lib/libcrypto.so*
  - usr/lib/libssl.so*
  - usr/lib/engines-3
  - usr/lib/ossl-modules
  - usr/bin/openssl
  - usr/lib/libc.so
  - lib/ld-musl-x86_64.so.1
  before: install
- image: tools/coreutils
  add: /
  to: /
  includePaths:
  - usr/bin/coreutils
  - usr/bin/test
  - usr/bin/chroot
  before: install
shell:
  setup:
  - /tmp/apps/setcap    cap_sys_chroot,cap_net_bind_service=+ep /nginx-ingress-controller
  - /tmp/apps/setcap -v cap_sys_chroot,cap_net_bind_service=+ep /nginx-ingress-controller
  - /tmp/apps/setcap    cap_sys_chroot,cap_net_bind_service=+ep /usr/bin/unshare
  - /tmp/apps/setcap -v cap_sys_chroot,cap_net_bind_service=+ep /usr/bin/unshare
  - /tmp/apps/setcap    cap_net_bind_service=+ep /chroot/usr/local/nginx/sbin/nginx
  - /tmp/apps/setcap -v cap_net_bind_service=+ep /chroot/usr/local/nginx/sbin/nginx
  - /tmp/apps/setcap    cap_sys_chroot,cap_net_bind_service=+ep /usr/bin/dumb-init
  - /tmp/apps/setcap -v cap_sys_chroot,cap_net_bind_service=+ep /usr/bin/dumb-init
imageSpec:
  config:
    workingDir: /chroot/etc/nginx
    user: "64535"
    expose: ["80", "443"]
    entrypoint: ["/usr/bin/dumb-init", "--"]
    cmd: ["/nginx-ingress-controller"]
    env: { "PATH": "$PATH:/usr/local/luajit/bin:/usr/local/nginx/sbin:/usr/local/nginx/bin", "LUA_PATH": "/usr/local/share/luajit-2.1.0-beta3/?.lua;/usr/local/share/lua/5.1/?.lua;/usr/local/lib/lua/?.lua;;", "LUA_CPATH": "/usr/local/lib/lua/?/?.so;/usr/local/lib/lua/?.so;;" }
