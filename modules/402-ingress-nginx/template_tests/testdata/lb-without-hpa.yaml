apiVersion: deckhouse.io/v2
kind: IngressNginxController
metadata:
  name: test-without-hpa
spec:
  inlet: LoadBalancer
  ingressClass: nginx
  controllerVersion: "1.15"
  maxReplicas: 3
  minReplicas: 3
