# This file is auto-generated by openapigen. Do not edit manually.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    heritage: deckhouse
    module: cloud-provider-yandex
  name: yandexinstanceclasses.deckhouse.io
spec:
  group: deckhouse.io
  names:
    categories:
    - cloudinstanceclasses
    kind: YandexInstanceClass
    listKind: YandexInstanceClassList
    plural: yandexinstanceclasses
    singular: yandexinstanceclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: NodeGroups which use this instance class.
      jsonPath: .status.nodeGroupConsumers
      name: Node Groups
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
            x-doc-skip: true
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
            x-doc-skip: true
          metadata:
            type: object
          spec:
            description: InstanceClassSpec defines the desired state of the YandexInstanceClass.
            properties:
              additionalLabels:
                additionalProperties:
                  type: string
                description: Additional labels to be attached to the Yandex Compute
                  Instance resource.
                type: object
                x-doc-examples:
                - '{project: cms-production, severity: critical}'
              additionalSubnets:
                description: |-
                  Subnet IDs that VirtualMachines' secondary NICs will connect to.

                  Each subnet listed here translates into one additional network interface.
                items:
                  type: string
                type: array
                x-doc-examples:
                - '[b0csh41c1or82vuch89v, e2lgddi5svochh5fbq96]'
              assignPublicIPAddress:
                default: false
                description: Should a public external IPv4 address be assigned to
                  a provisioned Yandex Compute Instance.
                type: boolean
                x-doc-default: "false"
                x-doc-examples:
                - "false"
              coreFraction:
                default: 100
                description: |-
                  Percent of reserved CPU capacity on a Yandex Compute Instance.

                  [Details...](https://cloud.yandex.com/en/docs/compute/concepts/performance-levels)
                enum:
                - 5
                - 20
                - 50
                - 100
                type: integer
                x-doc-default: "100"
                x-doc-examples:
                - "20"
              cores:
                description: Amount of CPU cores to provision on a Yandex Compute
                  Instance.
                type: integer
                x-doc-examples:
                - "4"
              diskSizeGB:
                default: 50
                description: Yandex Compute Instance disk size in gibibytes.
                type: integer
                x-doc-default: "50"
                x-doc-examples:
                - "20"
              diskType:
                default: network-hdd
                description: |-
                  Instance [disk type](https://cloud.yandex.com/en-ru/docs/compute/concepts/disk#disks_types).

                  Size of `network-ssd-nonreplicated` and `network-ssd-io-m3` disks must be a multiple of 93 GB.
                enum:
                - network-ssd
                - network-hdd
                - network-ssd-nonreplicated
                - network-ssd-io-m3
                type: string
                x-doc-default: '"network-hdd"'
                x-doc-examples:
                - network-hdd
              gpus:
                default: 0
                description: Number of GPUs on a Yandex Compute Instance.
                type: integer
                x-doc-default: "0"
                x-doc-examples:
                - "4"
              imageID:
                description: |-
                  Image ID to use while provisioning Yandex Compute Instances.

                  The [masterNodeGroup.instanceClass.imageID](cluster_configuration.html#yandexclusterconfiguration-masternodegroup-instanceclass-imageid) parameter will be used by default.
                type: string
                x-doc-examples:
                - fd83ica41cade1mj35sr
              mainSubnet:
                description: |-
                  Subnet ID that VirtualMachines' primary NIC will connect to.

                  If the parameter is not specified, the main network is determined automatically according to the following logic:
                  if a list of networks is set in the [existingZoneToSubnetIDMap](cluster_configuration.html#yandexclusterconfiguration-existingzonetosubnetidmap) parameter,
                  then the network is selected from the specified list; otherwise, the created Deckhouse network is used.
                type: string
                x-doc-examples:
                - e9bnc7g9mu9mper9clk4
              memory:
                description: Amount of primary memory in MB provision on a Yandex
                  Compute Instance.
                type: integer
                x-doc-examples:
                - "8192"
              networkType:
                default: Standard
                description: 'Network type: `Standard` or [`SoftwareAccelerated`](https://cloud.yandex.com/en/docs/vpc/concepts/software-accelerated-network).'
                enum:
                - Standard
                - SoftwareAccelerated
                type: string
              platformID:
                default: standard-v3
                description: |-
                  Platform ID.

                  [List of available platforms...](https://cloud.yandex.com/en-ru/docs/compute/concepts/vm-platforms)
                type: string
                x-doc-default: standard-v3
              preemptible:
                default: false
                description: |-
                  Should a provisioned Yandex Compute Instance be preemptible.

                  For more information about preemptible virtual machines, read the [provider's documentation](https://cloud.yandex.com/en/docs/compute/concepts/preemptible-vm).
                type: boolean
                x-doc-default: "false"
            required:
            - cores
            - imageID
            - memory
            type: object
          status:
            description: YandexInstanceClassStatus stores information about the YandexInstanceClass
              resource.
            properties:
              nodeGroupConsumers:
                description: NodeGroupConsumers lists the names of NodeGroups that
                  use this instance class.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
            x-doc-skip: true
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
            x-doc-skip: true
          metadata:
            type: object
          spec:
            description: InstanceClassSpec defines the desired state of the YandexInstanceClass.
            properties:
              additionalLabels:
                additionalProperties:
                  type: string
                description: Additional labels to be attached to the Yandex Compute
                  Instance resource.
                type: object
                x-doc-examples:
                - '{project: cms-production, severity: critical}'
              additionalSubnets:
                description: |-
                  Subnet IDs that VirtualMachines' secondary NICs will connect to.

                  Each subnet listed here translates into one additional network interface.
                items:
                  type: string
                type: array
                x-doc-examples:
                - '[b0csh41c1or82vuch89v, e2lgddi5svochh5fbq96]'
              assignPublicIPAddress:
                default: false
                description: Should a public external IPv4 address be assigned to
                  a provisioned Yandex Compute Instance.
                type: boolean
                x-doc-default: "false"
                x-doc-examples:
                - "false"
              coreFraction:
                default: 100
                description: |-
                  Percent of reserved CPU capacity on a Yandex Compute Instance.

                  [Details...](https://cloud.yandex.com/en/docs/compute/concepts/performance-levels)
                enum:
                - 5
                - 20
                - 50
                - 100
                type: integer
                x-doc-default: "100"
                x-doc-examples:
                - "20"
              cores:
                description: Amount of CPU cores to provision on a Yandex Compute
                  Instance.
                type: integer
                x-doc-examples:
                - "4"
              diskSizeGB:
                default: 20
                description: Yandex Compute Instance disk size in gibibytes.
                type: integer
                x-doc-default: "20"
                x-doc-examples:
                - "50"
              diskType:
                default: network-hdd
                description: |-
                  Instance disk type.

                  Size of `network-ssd-nonreplicated` and `network-ssd-io-m3` disks must be a multiple of 93 GB.

                  For more information about possible disk types, read the [provider's documentation](https://cloud.yandex.com/en-ru/docs/compute/concepts/disk#disks_types).
                enum:
                - network-ssd
                - network-hdd
                - network-ssd-nonreplicated
                type: string
                x-doc-default: '"network-hdd"'
                x-doc-examples:
                - network-hdd
              gpus:
                default: 0
                description: Number of GPUs on a Yandex Compute Instance.
                type: integer
                x-doc-default: "0"
                x-doc-examples:
                - "4"
              imageID:
                description: |-
                  Image ID to use while provisioning Yandex Compute Instances.

                  The [masterNodeGroup.instanceClass.imageID](cluster_configuration.html#yandexclusterconfiguration-masternodegroup-instanceclass-imageid) parameter will be used by default.
                type: string
                x-doc-examples:
                - fd83ica41cade1mj35sr
              mainSubnet:
                description: |-
                  Subnet ID that VirtualMachines' primary NIC will connect to.

                  If the parameter is not specified, the main network is determined automatically according to the following logic:
                  if a list of networks is set in the [existingZoneToSubnetIDMap](cluster_configuration.html#yandexclusterconfiguration-existingzonetosubnetidmap) parameter,
                  then the network is selected from the specified list; otherwise, the created Deckhouse network is used.
                type: string
                x-doc-examples:
                - e9bnc7g9mu9mper9clk4
              memory:
                description: Amount of primary memory in MB provision on a Yandex
                  Compute Instance.
                type: integer
                x-doc-examples:
                - "8192"
              networkType:
                default: STANDARD
                description: 'Network type: STANDARD or SOFTWARE_ACCELERATED'
                enum:
                - STANDARD
                - SOFTWARE_ACCELERATED
                type: string
              platformID:
                default: standard-v2
                description: |-
                  Platform ID.

                  [List of available platforms...](https://cloud.yandex.com/en-ru/docs/compute/concepts/vm-platforms)
                type: string
                x-doc-default: standard-v2
              preemptible:
                default: false
                description: |-
                  Should a provisioned Yandex Compute Instance be preemptible.

                  For more information about preemptible virtual machines, read the [provider's documentation](https://cloud.yandex.com/en/docs/compute/concepts/preemptible-vm).
                type: boolean
                x-doc-default: "false"
            required:
            - cores
            - imageID
            - memory
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
