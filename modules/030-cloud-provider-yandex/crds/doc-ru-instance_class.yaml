# This file is auto-generated by openapigen. Do not edit manually.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    heritage: deckhouse
    module: cloud-provider-yandex
  name: yandexinstanceclasses.deckhouse.io
spec:
  group: deckhouse.io
  names:
    categories:
    - cloudinstanceclasses
    kind: YandexInstanceClass
    listKind: YandexInstanceClassList
    plural: yandexinstanceclasses
    singular: yandexinstanceclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: NodeGroups which use this instance class.
      jsonPath: .status.nodeGroupConsumers
      name: Node Groups
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: |
              Определяет параметры виртуальной машины Yandex Compute Cloud.
            properties:
              additionalLabels:
                additionalProperties:
                  type: string
                description: |
                  Дополнительные лейблы, которые будут присвоены созданным виртуальным машинам.
                type: object
              additionalSubnets:
                description: |
                  Список дополнительных подсетей, которые будут подключены к виртуальной машине.

                  Для узлов типа `CloudEphemeral` каждая подсеть из списка подключается как отдельный сетевой интерфейс.

                  Для узлов типа `CloudPermanent` из списка выбирается одна подсеть по индексу узла, поэтому список должен содержать не меньше подсетей, чем узлов в группе.
                items:
                  type: string
                type: array
              assignPublicIPAddress:
                description: |
                  Необходимость присвоения публичных IP-адресов виртуальным машинам.
                type: boolean
              coreFraction:
                description: |
                  Базовый уровень производительности каждого ядра CPU у создаваемых виртуальных машин.

                  [Подробнее об уровнях производительности](https://cloud.yandex.ru/docs/compute/concepts/performance-levels).
                type: integer
              cores:
                description: |
                  Количество ядер CPU, выделяемых виртуальной машине.
                type: integer
              diskSizeGB:
                description: |
                  Размер диска у виртуальных машин. Значение указывается в `ГиБ`.
                type: integer
              diskType:
                description: |
                  Тип диска у виртуальных машин.

                  Размер дисков `network-ssd-nonreplicated` и `network-ssd-io-m3` должен быть кратен 93 GB.

                  Подробнее о возможных типах дисков можно узнать в [документации провайдера](https://cloud.yandex.com/docs/compute/concepts/disk#disks_types).
                type: string
              etcdDiskSizeGB:
                description: |
                  Размер диска для etcd, устанавливается только для InstanceClass, предназначенных для группы мастер-узлов. Значение указывается в `ГиБ`.
                nullable: true
                type: integer
              gpus:
                description: |
                  Количество графических адаптеров у создаваемых виртуальных машин.
                type: integer
              imageID:
                description: |
                  Идентификатор образа, который будет установлен в заказанные виртуальные машины.

                  По умолчанию используется образ, указанный в параметре [masterNodeGroup.instanceClass.imageID](cluster_configuration.html#yandexclusterconfiguration-masternodegroup-instanceclass-imageid).
                type: string
              mainSubnet:
                description: |
                  Имя основной сети (ID), к которой будет подключен основной сетевой интерфейс виртуальной машины.

                  Если параметр не задан, то основная сеть определяется автоматически согласно следующей логике: если задан список сетей в параметре [existingZoneToSubnetIDMap](cluster_configuration.html#yandexclusterconfiguration-existingzonetosubnetidmap), то сеть определяется из указанного списка, иначе используется созданная Deckhouse сеть.
                type: string
              memory:
                description: |
                  Количество оперативной памяти (в мегабайтах) у создаваемых виртуальных машин.
                type: integer
              networkType:
                description: |
                  Тип сети: обычная или [программно-ускоренная](https://cloud.yandex.ru/docs/vpc/concepts/software-accelerated-network).
                type: string
              platformID:
                description: |
                  ID платформы.

                  [Список существующих платформ](https://cloud.yandex.com/docs/compute/concepts/vm-platforms).
                type: string
              preemptible:
                description: |
                  Необходимость заказа прерываемых виртуальных машин (preemptible-инстансов).

                  Подробнее о прерываемых виртуальных машинах можно узнать в [документации провайдера](https://cloud.yandex.ru/docs/compute/concepts/preemptible-vm).
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: |
              Определяет параметры виртуальной машины Yandex Compute Cloud.
            properties:
              additionalLabels:
                additionalProperties:
                  type: string
                description: |
                  Дополнительные лейблы, которые будут присвоены созданным виртуальным машинам.
                type: object
              additionalSubnets:
                description: |
                  Список дополнительных подсетей, которые будут подключены к виртуальной машине.

                  Для узлов типа `CloudEphemeral` каждая подсеть из списка подключается как отдельный сетевой интерфейс.

                  Для узлов типа `CloudPermanent` из списка выбирается одна подсеть по индексу узла, поэтому список должен содержать не меньше подсетей, чем узлов в группе.
                items:
                  type: string
                type: array
              assignPublicIPAddress:
                description: |
                  Необходимость присвоения публичных IP-адресов виртуальным машинам.
                type: boolean
              coreFraction:
                description: |
                  Базовый уровень производительности каждого ядра CPU у создаваемых виртуальных машин.

                  [Подробнее об уровнях производительности](https://cloud.yandex.ru/docs/compute/concepts/performance-levels).
                type: integer
              cores:
                description: |
                  Количество ядер CPU, выделяемых виртуальной машине.
                type: integer
              diskSizeGB:
                description: |
                  Размер диска у виртуальных машин. Значение указывается в `ГиБ`.
                type: integer
              diskType:
                description: |
                  Тип диска у виртуальных машин.

                  Размер дисков `network-ssd-nonreplicated` и `network-ssd-io-m3` должен быть кратен 93 GB.

                  Подробнее о возможных типах дисков можно узнать в [документации провайдера](https://cloud.yandex.com/docs/compute/concepts/disk#disks_types).
                type: string
              gpus:
                description: |
                  Количество графических адаптеров у создаваемых виртуальных машин.
                type: integer
              imageID:
                description: |
                  Идентификатор образа, который будет установлен в заказанные виртуальные машины.

                  По умолчанию используется образ, указанный в параметре [masterNodeGroup.instanceClass.imageID](cluster_configuration.html#yandexclusterconfiguration-masternodegroup-instanceclass-imageid).
                type: string
              mainSubnet:
                description: |
                  Имя основной сети (ID), к которой будет подключен основной сетевой интерфейс виртуальной машины.

                  Если параметр не задан, то основная сеть определяется автоматически согласно следующей логике: если задан список сетей в параметре [existingZoneToSubnetIDMap](cluster_configuration.html#yandexclusterconfiguration-existingzonetosubnetidmap), то сеть определяется из указанного списка, иначе используется созданная Deckhouse сеть.
                type: string
              memory:
                description: |
                  Количество оперативной памяти (в мегабайтах) у создаваемых виртуальных машин.
                type: integer
              networkType:
                description: |
                  Тип сети: обычная или [программно-ускоренная](https://cloud.yandex.ru/docs/vpc/concepts/software-accelerated-network).
                type: string
              platformID:
                description: |
                  ID платформы.

                  [Список существующих платформ](https://cloud.yandex.com/docs/compute/concepts/vm-platforms).
                type: string
              preemptible:
                description: |
                  Необходимость заказа прерываемых виртуальных машин (preemptible-инстансов).

                  Подробнее о прерываемых виртуальных машинах можно узнать в [документации провайдера](https://cloud.yandex.ru/docs/compute/concepts/preemptible-vm).
                type: boolean
            type: object
        type: object
    served: true
    storage: false
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
