diff --git a/api/v1alpha1/types.go b/api/v1alpha1/types.go
index d26fd22..42b923c 100644
--- a/api/v1alpha1/types.go
+++ b/api/v1alpha1/types.go
@@ -1,7 +1,7 @@
 package v1alpha1
 
 import (
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 )
 
 // InstanceStatus describes the status of a Yandex Cloud Compute instance.
@@ -38,13 +38,13 @@ const (
 	// ConditionStatusProvisioning is the string representing an instance in a provisioning state.
 	ConditionStatusProvisioning clusterv1.ConditionType = "PROVISIONING"
 	// ConditionStatusRunning is the string representing an instance in a running state.
-	ConditionStatusRunning = "RUNNING"
+	ConditionStatusRunning clusterv1.ConditionType = "RUNNING"
 	// ConditionStatusReady is the string representing an instance in a ready state.
-	ConditionStatusReady = "READY"
+	ConditionStatusReady clusterv1.ConditionType = "READY"
 	// ConditionStatusError is the string representing an instance in a error state.
 	ConditionStatusError = "ERROR"
 	// ConditionStatusNotfound used when the instance couldn't be retrieved.
-	ConditionStatusNotfound = "NOTFOUND"
+	ConditionStatusNotfound clusterv1.ConditionType = "NOTFOUND"
 )
 
 const (
diff --git a/api/v1alpha1/yandexcluster_mutatingwebhook.go b/api/v1alpha1/yandexcluster_mutatingwebhook.go
index 79eab05..993b9d4 100644
--- a/api/v1alpha1/yandexcluster_mutatingwebhook.go
+++ b/api/v1alpha1/yandexcluster_mutatingwebhook.go
@@ -14,11 +14,11 @@ import (
 //+kubebuilder:webhook:verbs=create;update,path=/mutate-infrastructure-cluster-x-k8s-io-v1alpha1-yandexcluster-identitylink,mutating=true,failurePolicy=fail,matchPolicy=Equivalent,groups=infrastructure.cluster.x-k8s.io,resources=yandexclusters,versions=v1alpha1,name=mutation.yandexclusters.infrastructure.cluster.x-k8s.io,sideEffects=None,admissionReviewVersions=v1beta1
 
 type yandexClusterAdmitter struct {
-	decoder *admission.Decoder
+	decoder admission.Decoder
 }
 
 // NewYandexClusterAdmitter returns a new admission.Handler that adds identityRef labels to YandexCluster objects.
-func NewYandexClusterAdmitter(decoder *admission.Decoder) admission.Handler {
+func NewYandexClusterAdmitter(decoder admission.Decoder) admission.Handler {
 	return &yandexClusterAdmitter{decoder: decoder}
 }
 
diff --git a/api/v1alpha1/yandexcluster_types.go b/api/v1alpha1/yandexcluster_types.go
index 45871b5..0cb692a 100644
--- a/api/v1alpha1/yandexcluster_types.go
+++ b/api/v1alpha1/yandexcluster_types.go
@@ -19,7 +19,7 @@ package v1alpha1
 import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 )
 
 const (
@@ -58,7 +58,7 @@ type YandexClusterSpec struct {
 	// Do not set it manually when creating YandexCluster as CAPY will set this for you
 	// after creating load balancer based on LoadBalancer specification.
 	// +optional
-	ControlPlaneEndpoint clusterv1.APIEndpoint `json:"controlPlaneEndpoint"`
+	ControlPlaneEndpoint clusterv1.APIEndpoint `json:"controlPlaneEndpoint,omitempty,omitzero"`
 
 	// FolderID is the identifier of YandexCloud folder to deploy the cluster to.
 	// +required
@@ -190,13 +190,21 @@ func (ir *IdentityReference) NamespacedName() types.NamespacedName {
 	}
 }
 
+// ClusterInitializationStatus provides observations of the YandexCluster initialization process.
+type ClusterInitializationStatus struct {
+	// Provisioned denotes that the cluster infrastructure provisioning is completed.
+	Provisioned bool `json:"provisioned,omitempty"`
+}
+
 // YandexClusterStatus defines the observed state of YandexCluster.
 type YandexClusterStatus struct {
 	// Ready is true when the provider resource is ready.
 	// +kubebuilder:default=false
-	Ready        bool                 `json:"ready"`
-	LoadBalancer LoadBalancerStatus   `json:"loadBalancerStatus,omitempty"`
-	Conditions   clusterv1.Conditions `json:"conditions,omitempty"`
+	Ready bool `json:"ready"`
+	// +optional
+	Initialization ClusterInitializationStatus `json:"initialization,omitempty"`
+	LoadBalancer   LoadBalancerStatus          `json:"loadBalancerStatus,omitempty"`
+	Conditions     []metav1.Condition          `json:"conditions,omitempty"`
 }
 
 // LoadBalancerStatus encapsulates load balancer resources.
@@ -242,13 +250,13 @@ type YandexClusterList struct {
 }
 
 // GetConditions returns the list of conditions for an YandexCluster API object.
-func (yc *YandexCluster) GetConditions() clusterv1.Conditions {
-	return yc.Status.Conditions
+func (c *YandexCluster) GetConditions() []metav1.Condition {
+	return c.Status.Conditions
 }
 
 // SetConditions will set the given conditions on an YandexCluster API object.
-func (yc *YandexCluster) SetConditions(conditions clusterv1.Conditions) {
-	yc.Status.Conditions = conditions
+func (c *YandexCluster) SetConditions(conditions []metav1.Condition) {
+	c.Status.Conditions = conditions
 }
 
 func init() {
diff --git a/api/v1alpha1/yandexcluster_webhook.go b/api/v1alpha1/yandexcluster_webhook.go
index 356cf94..32c7672 100644
--- a/api/v1alpha1/yandexcluster_webhook.go
+++ b/api/v1alpha1/yandexcluster_webhook.go
@@ -24,10 +24,9 @@ import (
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/validation/field"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	logf "sigs.k8s.io/controller-runtime/pkg/log"
-	"sigs.k8s.io/controller-runtime/pkg/webhook"
 	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
 )
 
@@ -55,10 +54,10 @@ func (c *YandexCluster) SetupWebhookWithManager(mgr ctrl.Manager) error {
 //nolint:lll // controller-gen marker
 //+kubebuilder:webhook:verbs=create;update,path=/validate-infrastructure-cluster-x-k8s-io-v1alpha1-yandexcluster,mutating=false,failurePolicy=fail,matchPolicy=Equivalent,groups=infrastructure.cluster.x-k8s.io,resources=yandexclusters,versions=v1alpha1,name=validation.yandexclusters.infrastructure.cluster.x-k8s.io,sideEffects=None,admissionReviewVersions=v1beta1
 
-var (
-	_ webhook.Defaulter = &YandexCluster{}
-	_ webhook.Validator = &YandexCluster{}
-)
+// var (
+// 	_ webhook.Defaulter = &YandexCluster{}
+// 	_ webhook.Validator = &YandexCluster{}
+// )
 
 // Default implements webhook.Defaulter so a webhook will be registered for the type.
 func (c *YandexCluster) Default() {
@@ -84,7 +83,7 @@ func (c *YandexCluster) ValidateCreate() (admission.Warnings, error) {
 		)
 	}
 
-	if !reflect.DeepEqual(c.Spec.ControlPlaneEndpoint, clusterv1.APIEndpoint{}) {
+	if !c.Spec.ControlPlaneEndpoint.IsZero() {
 		allErrs = append(allErrs, isControlPlaneEndpointValid(c.Spec.ControlPlaneEndpoint)...)
 	}
 
@@ -113,7 +112,7 @@ func (c *YandexCluster) ValidateUpdate(oldRaw runtime.Object) (admission.Warning
 	// We allow you to change the ControlPlaneEndpoint only if this field has not been set before.
 	// In all other cases, this field is immutable.
 	if !reflect.DeepEqual(c.Spec.ControlPlaneEndpoint, old.Spec.ControlPlaneEndpoint) {
-		if reflect.DeepEqual(old.Spec.ControlPlaneEndpoint, clusterv1.APIEndpoint{}) {
+		if old.Spec.ControlPlaneEndpoint.IsZero() {
 			allErrs = append(allErrs, isControlPlaneEndpointValid(c.Spec.ControlPlaneEndpoint)...)
 		} else {
 			allErrs = append(allErrs,
diff --git a/api/v1alpha1/yandexidentity_mutatingwebhook.go b/api/v1alpha1/yandexidentity_mutatingwebhook.go
index ca9c07a..c875232 100644
--- a/api/v1alpha1/yandexidentity_mutatingwebhook.go
+++ b/api/v1alpha1/yandexidentity_mutatingwebhook.go
@@ -15,12 +15,12 @@ import (
 
 type yandexIdentityAdmitter struct {
 	platformClient client.Client
-	decoder        *admission.Decoder
+	decoder        admission.Decoder
 }
 
 // NewYandexIdentityDeletionBlocker returns a new admission.Handler that prevents deletion of YandexIdentity objects
 // that are linked to YandexCluster objects.
-func NewYandexIdentityDeletionBlocker(platformClient client.Client, decoder *admission.Decoder) admission.Handler {
+func NewYandexIdentityDeletionBlocker(platformClient client.Client, decoder admission.Decoder) admission.Handler {
 	return &yandexIdentityAdmitter{
 		platformClient: platformClient,
 		decoder:        decoder,
diff --git a/api/v1alpha1/yandexidentity_types.go b/api/v1alpha1/yandexidentity_types.go
index 80c6dbd..a89f25c 100644
--- a/api/v1alpha1/yandexidentity_types.go
+++ b/api/v1alpha1/yandexidentity_types.go
@@ -18,7 +18,6 @@ package v1alpha1
 
 import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
 )
 
 const (
@@ -53,7 +52,7 @@ type YandexIdentityStatus struct {
 
 	// Conditions is a list of conditions and their status.
 	// +optional
-	Conditions clusterv1.Conditions `json:"conditions,omitempty"`
+	Conditions []metav1.Condition `json:"conditions,omitempty"`
 
 	// Linked clusters
 	// +optional
@@ -87,12 +86,12 @@ func (i *YandexIdentity) GenerateSecretFinalizer() string {
 }
 
 // GetConditions returns the list of conditions for an YandexIdentity API object.
-func (i *YandexIdentity) GetConditions() clusterv1.Conditions {
+func (i *YandexIdentity) GetConditions() []metav1.Condition {
 	return i.Status.Conditions
 }
 
 // SetConditions will set the given conditions on an YandexIdentity API object.
-func (i *YandexIdentity) SetConditions(conditions clusterv1.Conditions) {
+func (i *YandexIdentity) SetConditions(conditions []metav1.Condition) {
 	i.Status.Conditions = conditions
 }
 
diff --git a/api/v1alpha1/yandexmachine_types.go b/api/v1alpha1/yandexmachine_types.go
index b40a107..c07671f 100644
--- a/api/v1alpha1/yandexmachine_types.go
+++ b/api/v1alpha1/yandexmachine_types.go
@@ -21,7 +21,6 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 
 	"k8s.io/apimachinery/pkg/api/resource"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
 	"sigs.k8s.io/cluster-api/errors"
 )
 
@@ -65,6 +64,12 @@ type YandexMachineSpec struct {
 	NetworkInterfaces []NetworkInterface `json:"networkInterfaces"`
 }
 
+// InitializationStatus provides observations of the YandexMachine initialization process.
+type InitializationStatus struct {
+	// Provisioned denotes that the machine infrastructure provisioning is completed.
+	Provisioned bool `json:"provisioned,omitempty"`
+}
+
 // NetworkInterface defines the network interface configuration of YandexCloud VM.
 type NetworkInterface struct {
 	// SubnetID is the identifier of subnetwork to use for this instance.
@@ -124,6 +129,10 @@ type YandexMachineStatus struct {
 	// +optional
 	Ready bool `json:"ready"`
 
+	// Initialization provides observations of the YandexMachine initialization process.
+	// +optional
+	Initialization InitializationStatus `json:"initialization,omitempty"`
+
 	// Addresses contains the YandexCloud instance associated addresses.
 	// +optional
 	Addresses []corev1.NodeAddress `json:"addresses,omitempty"`
@@ -172,7 +181,7 @@ type YandexMachineStatus struct {
 
 	// Conditions defines current service state of the YandexMachine.
 	// +optional
-	Conditions clusterv1.Conditions `json:"conditions,omitempty"`
+	Conditions []metav1.Condition `json:"conditions,omitempty"`
 }
 
 //+kubebuilder:object:root=true
@@ -197,12 +206,12 @@ type YandexMachineList struct {
 }
 
 // GetConditions returns the list of conditions for an Yandex Machine API object.
-func (ym *YandexMachine) GetConditions() clusterv1.Conditions {
+func (ym *YandexMachine) GetConditions() []metav1.Condition {
 	return ym.Status.Conditions
 }
 
 // SetConditions will set the given conditions on an Yandex Machine API object.
-func (ym *YandexMachine) SetConditions(conditions clusterv1.Conditions) {
+func (ym *YandexMachine) SetConditions(conditions []metav1.Condition) {
 	ym.Status.Conditions = conditions
 }
 
diff --git a/api/v1alpha1/yandexmachine_webhook.go b/api/v1alpha1/yandexmachine_webhook.go
index 5a56d6c..6b25dcb 100644
--- a/api/v1alpha1/yandexmachine_webhook.go
+++ b/api/v1alpha1/yandexmachine_webhook.go
@@ -25,7 +25,6 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	ctrl "sigs.k8s.io/controller-runtime"
 	logf "sigs.k8s.io/controller-runtime/pkg/log"
-	"sigs.k8s.io/controller-runtime/pkg/webhook"
 	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
 )
 
@@ -42,10 +41,10 @@ func (ym *YandexMachine) SetupWebhookWithManager(mgr ctrl.Manager) error {
 //nolint:lll // controller-gen marker
 //+kubebuilder:webhook:path=/validate-infrastructure-cluster-x-k8s-io-v1alpha1-yandexmachine,mutating=false,failurePolicy=fail,sideEffects=None,groups=infrastructure.cluster.x-k8s.io,resources=yandexmachines,verbs=create;update,versions=v1alpha1,name=validation.yandexmachines.infrastructure.cluster.x-k8s.io,admissionReviewVersions=v1beta1
 
-var (
-	_ webhook.Defaulter = &YandexMachine{}
-	_ webhook.Validator = &YandexMachine{}
-)
+// var (
+// 	_ webhook.Defaulter = &YandexMachine{}
+// 	_ webhook.Validator = &YandexMachine{}
+// )
 
 // Default implements webhook.Defaulter so a webhook will be registered for the type.
 func (ym *YandexMachine) Default() {
diff --git a/api/v1alpha1/yandexmachinetemplate_types.go b/api/v1alpha1/yandexmachinetemplate_types.go
index 4b38863..747db8a 100644
--- a/api/v1alpha1/yandexmachinetemplate_types.go
+++ b/api/v1alpha1/yandexmachinetemplate_types.go
@@ -18,7 +18,7 @@ package v1alpha1
 
 import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 )
 
 // YandexMachineTemplateResource describes the data needed to create am YandexMachine from a template.
diff --git a/api/v1alpha1/yandexmachinetemplate_webhook.go b/api/v1alpha1/yandexmachinetemplate_webhook.go
index 05bf8e1..4f9e32a 100644
--- a/api/v1alpha1/yandexmachinetemplate_webhook.go
+++ b/api/v1alpha1/yandexmachinetemplate_webhook.go
@@ -26,7 +26,6 @@ import (
 	"k8s.io/apimachinery/pkg/util/validation/field"
 	ctrl "sigs.k8s.io/controller-runtime"
 	logf "sigs.k8s.io/controller-runtime/pkg/log"
-	"sigs.k8s.io/controller-runtime/pkg/webhook"
 	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
 )
 
@@ -50,10 +49,10 @@ func (t *YandexMachineTemplate) SetupWebhookWithManager(mgr ctrl.Manager) error
 
 //+kubebuilder:webhook:path=/validate-infrastructure-cluster-x-k8s-io-v1alpha1-yandexmachinetemplate,mutating=false,failurePolicy=fail,sideEffects=None,groups=infrastructure.cluster.x-k8s.io,resources=yandexmachinetemplates,verbs=create;update,versions=v1alpha1,name=validation.yandexmachinetemplates.infrastructure.cluster.x-k8s.io,admissionReviewVersions=v1beta1
 
-var (
-	_ webhook.Defaulter = &YandexMachineTemplate{}
-	_ webhook.Validator = &YandexMachineTemplate{}
-)
+// var (
+// 	_ webhook.Defaulter = &YandexMachineTemplate{}
+// 	_ webhook.Validator = &YandexMachineTemplate{}
+// )
 
 // Default implements webhook.Defaulter so a webhook will be registered for the type.
 func (t *YandexMachineTemplate) Default() {
diff --git a/api/v1alpha1/zz_generated.deepcopy.go b/api/v1alpha1/zz_generated.deepcopy.go
index 72dab6b..ab51216 100644
--- a/api/v1alpha1/zz_generated.deepcopy.go
+++ b/api/v1alpha1/zz_generated.deepcopy.go
@@ -22,8 +22,8 @@ package v1alpha1
 
 import (
 	"k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/runtime"
-	"sigs.k8s.io/cluster-api/api/v1beta1"
 	"sigs.k8s.io/cluster-api/errors"
 )
 
@@ -323,7 +323,7 @@ func (in *YandexClusterStatus) DeepCopyInto(out *YandexClusterStatus) {
 	out.LoadBalancer = in.LoadBalancer
 	if in.Conditions != nil {
 		in, out := &in.Conditions, &out.Conditions
-		*out = make(v1beta1.Conditions, len(*in))
+		*out = make([]metav1.Condition, len(*in))
 		for i := range *in {
 			(*in)[i].DeepCopyInto(&(*out)[i])
 		}
@@ -419,7 +419,7 @@ func (in *YandexIdentityStatus) DeepCopyInto(out *YandexIdentityStatus) {
 	*out = *in
 	if in.Conditions != nil {
 		in, out := &in.Conditions, &out.Conditions
-		*out = make(v1beta1.Conditions, len(*in))
+		*out = make([]metav1.Condition, len(*in))
 		for i := range *in {
 			(*in)[i].DeepCopyInto(&(*out)[i])
 		}
@@ -575,7 +575,7 @@ func (in *YandexMachineStatus) DeepCopyInto(out *YandexMachineStatus) {
 	}
 	if in.Conditions != nil {
 		in, out := &in.Conditions, &out.Conditions
-		*out = make(v1beta1.Conditions, len(*in))
+		*out = make([]metav1.Condition, len(*in))
 		for i := range *in {
 			(*in)[i].DeepCopyInto(&(*out)[i])
 		}
diff --git a/controllers/yandexcluster_controller.go b/controllers/yandexcluster_controller.go
index 5922de1..0262125 100644
--- a/controllers/yandexcluster_controller.go
+++ b/controllers/yandexcluster_controller.go
@@ -23,19 +23,20 @@ import (
 	"strconv"
 
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
-	"k8s.io/client-go/rest"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/runtime"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	"k8s.io/client-go/rest"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 	"sigs.k8s.io/cluster-api/util"
 	"sigs.k8s.io/cluster-api/util/annotations"
 	"sigs.k8s.io/cluster-api/util/conditions"
 	"sigs.k8s.io/cluster-api/util/predicates"
 	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/controller/controllerutil"
 	"sigs.k8s.io/controller-runtime/pkg/handler"
 	"sigs.k8s.io/controller-runtime/pkg/log"
-	"sigs.k8s.io/controller-runtime/pkg/source"
 
 	infrav1 "github.com/yandex-cloud/cluster-api-provider-yandex/api/v1alpha1"
 	yandex "github.com/yandex-cloud/cluster-api-provider-yandex/internal/pkg/client"
@@ -44,6 +45,16 @@ import (
 	"github.com/yandex-cloud/cluster-api-provider-yandex/internal/pkg/options"
 )
 
+func setLoadBalancerReadyCondition(cluster *infrav1.YandexCluster, status metav1.ConditionStatus, reason, message string) {
+	conditions.Set(cluster, metav1.Condition{
+		Type:               string(infrav1.LoadBalancerReadyCondition),
+		Status:             status,
+		Reason:             reason,
+		Message:            message,
+		LastTransitionTime: metav1.Now(),
+	})
+}
+
 // YandexClusterReconciler reconciles a YandexCluster object.
 type YandexClusterReconciler struct {
 	client.Client
@@ -142,7 +153,7 @@ func (r *YandexClusterReconciler) reconcile(ctx context.Context, clusterScope *s
 			Port: int32(port),
 		}
 
-		conditions.MarkTrue(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition)
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionTrue, "ExternalControlPlaneEndpointReady", "Control plane endpoint is managed externally")
 		clusterScope.SetReady()
 		return ctrl.Result{}, nil
 	case "", options.ControlPlaneEndpointManagementModeManagedLoadBalancer:
@@ -153,15 +164,13 @@ func (r *YandexClusterReconciler) reconcile(ctx context.Context, clusterScope *s
 	// Get loadbalancer service and reconcile load balancer.
 	lb := loadbalancer.New(clusterScope)
 	if err := lb.Reconcile(ctx); err != nil {
-		conditions.MarkFalse(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition,
-			"load balancer reconcile error", clusterv1.ConditionSeverityError, "%s", err.Error())
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionFalse, infrav1.LoadBalancerFailedReason, err.Error())
 		return ctrl.Result{}, fmt.Errorf("error reconciling load balancer: %w", err)
 	}
 
 	active, err := lb.IsActive(ctx)
 	if err != nil {
-		conditions.MarkFalse(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition,
-			"load balancer reconcile error", clusterv1.ConditionSeverityError, "%s", err.Error())
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionFalse, infrav1.LoadBalancerFailedReason, err.Error())
 		return ctrl.Result{}, fmt.Errorf("error reconciling load balancer: %w", err)
 	}
 	if !active {
@@ -173,14 +182,13 @@ func (r *YandexClusterReconciler) reconcile(ctx context.Context, clusterScope *s
 	// and YandexCluster status.
 	state, err := lb.Describe(ctx)
 	if err != nil {
-		conditions.MarkFalse(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition,
-			"load balancer reconcile error", clusterv1.ConditionSeverityError, "%s", err.Error())
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionFalse, infrav1.LoadBalancerFailedReason, err.Error())
 		return ctrl.Result{}, fmt.Errorf("error reconciling load balancer: %w", err)
 	}
 
 	switch {
 	// The load balancer has been created.
-	case clusterScope.YandexCluster.Spec.ControlPlaneEndpoint.Host == "" && clusterScope.YandexCluster.Spec.ControlPlaneEndpoint.Port == 0:
+	case clusterScope.YandexCluster.Spec.ControlPlaneEndpoint.IsZero():
 		clusterScope.YandexCluster.Spec.ControlPlaneEndpoint = clusterv1.APIEndpoint{
 			Host: state.ListenerAddress,
 			Port: state.ListenerPort,
@@ -189,7 +197,7 @@ func (r *YandexClusterReconciler) reconcile(ctx context.Context, clusterScope *s
 			ListenerAddress: state.ListenerAddress,
 			ListenerPort:    state.ListenerPort,
 		}
-		conditions.MarkTrue(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition)
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionTrue, "LoadBalancerReady", "Control plane load balancer is ready")
 		clusterScope.SetReady()
 		return ctrl.Result{}, nil
 
@@ -199,7 +207,7 @@ func (r *YandexClusterReconciler) reconcile(ctx context.Context, clusterScope *s
 			ListenerAddress: state.ListenerAddress,
 			ListenerPort:    state.ListenerPort,
 		}
-		conditions.MarkTrue(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition)
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionTrue, "LoadBalancerReady", "Control plane load balancer is ready")
 		clusterScope.SetReady()
 		return ctrl.Result{}, nil
 
@@ -231,8 +239,7 @@ func (r *YandexClusterReconciler) reconcileDelete(ctx context.Context, clusterSc
 	lb := loadbalancer.New(clusterScope)
 	deleted, err := lb.Delete(ctx)
 	if err != nil {
-		conditions.MarkFalse(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition,
-			clusterv1.DeletionFailedReason, clusterv1.ConditionSeverityWarning, "")
+		setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionFalse, "DeletionFailed", err.Error())
 		return ctrl.Result{}, fmt.Errorf("error deleting load balancer  %w", err)
 	}
 
@@ -243,31 +250,26 @@ func (r *YandexClusterReconciler) reconcileDelete(ctx context.Context, clusterSc
 	}
 
 	logger.V(1).Info("load balancer is being deleted, requeueing")
-	conditions.MarkFalse(clusterScope.YandexCluster, infrav1.LoadBalancerReadyCondition,
-		clusterv1.DeletingReason, clusterv1.ConditionSeverityInfo, "")
+	setLoadBalancerReadyCondition(clusterScope.YandexCluster, metav1.ConditionFalse, "Deleting", "Load balancer is being deleted")
 	return ctrl.Result{RequeueAfter: RequeueDuration}, nil
 }
 
 // SetupWithManager sets up the controller with the Manager.
 func (r *YandexClusterReconciler) SetupWithManager(ctx context.Context, mgr ctrl.Manager) error {
-	c, err := ctrl.NewControllerManagedBy(mgr).
+	return ctrl.NewControllerManagedBy(mgr).
 		For(&infrav1.YandexCluster{}).
-		WithEventFilter(predicates.ResourceNotPaused(ctrl.LoggerFrom(ctx))).
-		Build(r)
-	if err != nil {
-		return err
-	}
-
-	return c.Watch(
-		source.Kind(mgr.GetCache(), &clusterv1.Cluster{}),
-		handler.EnqueueRequestsFromMapFunc(
-			util.ClusterToInfrastructureMapFunc(
-				ctx,
-				infrav1.GroupVersion.WithKind("YandexCluster"),
-				mgr.GetClient(),
-				&infrav1.YandexCluster{},
+		Watches(
+			&clusterv1.Cluster{},
+			handler.EnqueueRequestsFromMapFunc(
+				util.ClusterToInfrastructureMapFunc(
+					ctx,
+					infrav1.GroupVersion.WithKind("YandexCluster"),
+					mgr.GetClient(),
+					&infrav1.YandexCluster{},
+				),
 			),
-		),
-		predicates.ClusterUnpaused(ctrl.LoggerFrom(ctx)),
-	)
+			builder.WithPredicates(predicates.ClusterUnpaused(r.Scheme, ctrl.LoggerFrom(ctx))),
+		).
+		WithEventFilter(predicates.ResourceNotPaused(r.Scheme, ctrl.LoggerFrom(ctx))).
+		Complete(r)
 }
diff --git a/controllers/yandexidentity_controller.go b/controllers/yandexidentity_controller.go
index 05cf831..a7477a4 100644
--- a/controllers/yandexidentity_controller.go
+++ b/controllers/yandexidentity_controller.go
@@ -21,8 +21,8 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/runtime"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
 	"sigs.k8s.io/cluster-api/util/conditions"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/builder"
@@ -41,6 +41,16 @@ import (
 	"github.com/yandex-cloud/cluster-api-provider-yandex/internal/pkg/options"
 )
 
+func setIdentityCondition(identity *infrav1.YandexIdentity, conditionType string, status metav1.ConditionStatus, reason, message string) {
+	conditions.Set(identity, metav1.Condition{
+		Type:               conditionType,
+		Status:             status,
+		Reason:             reason,
+		Message:            message,
+		LastTransitionTime: metav1.Now(),
+	})
+}
+
 // YandexIdentityReconciler reconciles a YandexIdentity object.
 type YandexIdentityReconciler struct {
 	client.Client
@@ -104,79 +114,49 @@ func (r *YandexIdentityReconciler) reconcile(ctx context.Context, identityScope
 	defer func() {
 		if rerr == nil {
 			identityScope.Identity.Status.Ready = true
-			conditions.MarkTrue(identityScope.Identity, infrav1.IdentityReadyCondition)
+			setIdentityCondition(identityScope.Identity, string(infrav1.IdentityReadyCondition), metav1.ConditionTrue, "IdentityReady", "Identity reconciliation completed successfully")
 		} else {
 			identityScope.Identity.Status.Ready = false
-			conditions.MarkFalse(identityScope.Identity,
-				infrav1.IdentityReadyCondition,
-				"ReconciliationError",
-				clusterv1.ConditionSeverityError,
-				"%s",
-				rerr.Error())
+			setIdentityCondition(identityScope.Identity, string(infrav1.IdentityReadyCondition), metav1.ConditionFalse, "ReconciliationError", rerr.Error())
 		}
 	}()
 
 	secretChanged, err := identityScope.IsSecretChanged(ctx)
 	if err != nil {
-		conditions.MarkFalse(identityScope.Identity,
-			infrav1.IdentityValidCondition,
-			"identity secret validation error",
-			clusterv1.ConditionSeverityError,
-			"%s",
-			err.Error())
+		setIdentityCondition(identityScope.Identity, string(infrav1.IdentityValidCondition), metav1.ConditionFalse, "IdentitySecretValidationError", err.Error())
 		return ctrl.Result{}, errors.Wrap(err, "failed to check identity key")
 	}
 
 	if secretChanged {
 		if err := identityScope.CheckConnectWithIdentity(ctx); err != nil {
-			conditions.MarkFalse(identityScope.Identity,
-				infrav1.IdentityValidCondition,
-				"identity check error",
-				clusterv1.ConditionSeverityError,
-				"%s",
-				err.Error())
+			setIdentityCondition(identityScope.Identity, string(infrav1.IdentityValidCondition), metav1.ConditionFalse, "IdentityCheckError", err.Error())
 			return ctrl.Result{}, errors.Wrap(err, "failed to check connection with identity")
 		}
 
 		if err := identityScope.SetSecretHash(ctx); err != nil {
-			conditions.MarkFalse(identityScope.Identity,
-				infrav1.IdentityValidCondition,
-				"identity key hash update error",
-				clusterv1.ConditionSeverityError,
-				"%s",
-				err.Error())
+			setIdentityCondition(identityScope.Identity, string(infrav1.IdentityValidCondition), metav1.ConditionFalse, "IdentityKeyHashUpdateError", err.Error())
 			return ctrl.Result{}, errors.Wrap(err, "failed to set key hash")
 		}
 	}
 
 	// set IdentityValidCondition to true
-	conditions.MarkTrue(identityScope.Identity, infrav1.IdentityValidCondition)
+	setIdentityCondition(identityScope.Identity, string(infrav1.IdentityValidCondition), metav1.ConditionTrue, "IdentityValid", "Identity credentials are valid")
 
 	if err := identityScope.SetSecretFinalizerAndOwner(ctx); err != nil {
-		conditions.MarkFalse(identityScope.Identity,
-			infrav1.IdentitySecretUpdatedCondition,
-			"secret update error",
-			clusterv1.ConditionSeverityError,
-			"%s",
-			err.Error())
+		setIdentityCondition(identityScope.Identity, string(infrav1.IdentitySecretUpdatedCondition), metav1.ConditionFalse, "SecretUpdateError", err.Error())
 		return ctrl.Result{}, errors.Wrap(err, "failed to set secret finalizer")
 	}
 
 	// set IdentitySecretUpdatedCondition to true
-	conditions.MarkTrue(identityScope.Identity, infrav1.IdentitySecretUpdatedCondition)
+	setIdentityCondition(identityScope.Identity, string(infrav1.IdentitySecretUpdatedCondition), metav1.ConditionTrue, "IdentitySecretUpdated", "Identity secret finalizer and owner references are up to date")
 
 	if err := identityScope.UpdateLinkedClusters(ctx); err != nil {
-		conditions.MarkFalse(identityScope.Identity,
-			infrav1.IdentityLinkedClustersUpdatedCondition,
-			"linked clusters update error",
-			clusterv1.ConditionSeverityError,
-			"%s",
-			err.Error())
+		setIdentityCondition(identityScope.Identity, string(infrav1.IdentityLinkedClustersUpdatedCondition), metav1.ConditionFalse, "LinkedClustersUpdateError", err.Error())
 		return ctrl.Result{}, errors.Wrap(err, "failed to update linked clusters")
 	}
 
 	// set IdentityLinkedClustersUpdatedCondition to true
-	conditions.MarkTrue(identityScope.Identity, infrav1.IdentityLinkedClustersUpdatedCondition)
+	setIdentityCondition(identityScope.Identity, string(infrav1.IdentityLinkedClustersUpdatedCondition), metav1.ConditionTrue, "LinkedClustersUpdated", "Linked clusters list is up to date")
 
 	return ctrl.Result{}, nil
 }
diff --git a/controllers/yandexmachine_controller.go b/controllers/yandexmachine_controller.go
index 6ac791c..3a7b227 100644
--- a/controllers/yandexmachine_controller.go
+++ b/controllers/yandexmachine_controller.go
@@ -24,17 +24,17 @@ import (
 	"github.com/pkg/errors"
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
 	"k8s.io/apimachinery/pkg/runtime"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 	capierrors "sigs.k8s.io/cluster-api/errors"
 	"sigs.k8s.io/cluster-api/util"
 	"sigs.k8s.io/cluster-api/util/annotations"
 	"sigs.k8s.io/cluster-api/util/predicates"
 	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/controller/controllerutil"
 	"sigs.k8s.io/controller-runtime/pkg/handler"
 	"sigs.k8s.io/controller-runtime/pkg/log"
-	"sigs.k8s.io/controller-runtime/pkg/source"
 
 	infrav1 "github.com/yandex-cloud/cluster-api-provider-yandex/api/v1alpha1"
 	yandex "github.com/yandex-cloud/cluster-api-provider-yandex/internal/pkg/client"
@@ -201,9 +201,14 @@ func (r *YandexMachineReconciler) reconcileDelete(ctx context.Context, machineSc
 
 // SetupWithManager sets up the controller with the Manager.
 func (r *YandexMachineReconciler) SetupWithManager(ctx context.Context, mgr ctrl.Manager) error {
-	c, err := ctrl.NewControllerManagedBy(mgr).
+	clusterToObjectFunc, err := util.ClusterToTypedObjectsMapper(r.Client, &infrav1.YandexMachineList{}, mgr.GetScheme())
+	if err != nil {
+		return errors.Wrapf(err, "failed to create mapper for Cluster to YandexMachines")
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
 		For(&infrav1.YandexMachine{}).
-		WithEventFilter(predicates.ResourceNotPaused(ctrl.LoggerFrom(ctx))).
+		WithEventFilter(predicates.ResourceNotPaused(r.Scheme, ctrl.LoggerFrom(ctx))).
 		Watches(
 			&clusterv1.Machine{},
 			handler.EnqueueRequestsFromMapFunc(util.MachineToInfrastructureMapFunc(infrav1.GroupVersion.WithKind("YandexMachine"))),
@@ -212,26 +217,13 @@ func (r *YandexMachineReconciler) SetupWithManager(ctx context.Context, mgr ctrl
 			&infrav1.YandexCluster{},
 			handler.EnqueueRequestsFromMapFunc(r.YandexClusterToYandexMachines(ctx)),
 		).
-		Build(r)
-	if err != nil {
-		return errors.Wrapf(err, "error creating controller")
-	}
-
-	clusterToObjectFunc, err := util.ClusterToTypedObjectsMapper(r.Client, &infrav1.YandexMachineList{}, mgr.GetScheme())
-	if err != nil {
-		return errors.Wrapf(err, "failed to create mapper for Cluster to YandexMachines")
-	}
-
-	// add a watch on clusterv1.Cluster object for unpause & ready notifications.
-	if err := c.Watch(
-		source.Kind(mgr.GetCache(), &clusterv1.Cluster{}),
-		handler.EnqueueRequestsFromMapFunc(clusterToObjectFunc),
-		predicates.ClusterUnpausedAndInfrastructureReady(ctrl.LoggerFrom(ctx)),
-	); err != nil {
-		return errors.Wrapf(err, "failed adding a watch for ready clusters")
-	}
-
-	return nil
+		Watches(
+			&clusterv1.Cluster{},
+			handler.EnqueueRequestsFromMapFunc(clusterToObjectFunc),
+			builder.WithPredicates(
+				predicates.ClusterUnpausedAndInfrastructureProvisioned(r.Scheme, ctrl.LoggerFrom(ctx))),
+		).
+		Complete(r)
 }
 
 // YandexClusterToYandexMachines is a handler.ToRequestsFunc to be used to enqeue requests for reconciliation
diff --git a/internal/pkg/cloud/scope/cluster.go b/internal/pkg/cloud/scope/cluster.go
index 8d85c03..697b8a9 100644
--- a/internal/pkg/cloud/scope/cluster.go
+++ b/internal/pkg/cloud/scope/cluster.go
@@ -10,7 +10,7 @@ import (
 	infrav1 "github.com/yandex-cloud/cluster-api-provider-yandex/api/v1alpha1"
 	yandex "github.com/yandex-cloud/cluster-api-provider-yandex/internal/pkg/client"
 
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 	"sigs.k8s.io/cluster-api/util/patch"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 )
@@ -77,7 +77,10 @@ type ClusterScope struct {
 
 // PatchObject persists the cluster configuration and status.
 func (c *ClusterScope) PatchObject(ctx context.Context) error {
-	return c.patchHelper.Patch(ctx, c.YandexCluster)
+	normalizeLegacyConditions(c.YandexCluster)
+	return c.patchHelper.Patch(ctx, c.YandexCluster, patch.WithOwnedConditions{Conditions: []string{
+		string(infrav1.LoadBalancerReadyCondition),
+	}})
 }
 
 // Close closes the current scope persisting the cluster configuration and status.
@@ -99,6 +102,7 @@ func (c *ClusterScope) Name() string {
 // SetReady sets the YandexCluster Ready Status.
 func (c *ClusterScope) SetReady() {
 	c.YandexCluster.Status.Ready = true
+	c.YandexCluster.Status.Initialization.Provisioned = true
 }
 
 // GetClient gets client for YandexCloud API.
diff --git a/internal/pkg/cloud/scope/conditions.go b/internal/pkg/cloud/scope/conditions.go
new file mode 100644
index 0000000..7457234
--- /dev/null
+++ b/internal/pkg/cloud/scope/conditions.go
@@ -0,0 +1,45 @@
+/*
+Copyright 2024.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scope
+
+import metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+type conditionsAware interface {
+	GetConditions() []metav1.Condition
+	SetConditions([]metav1.Condition)
+}
+
+func normalizeLegacyConditions(obj conditionsAware) {
+	conditions := obj.GetConditions()
+	if len(conditions) == 0 {
+		return
+	}
+
+	for i := range conditions {
+		if conditions[i].Reason == "" {
+			conditions[i].Reason = "MigratedFromBeta1"
+		}
+		if conditions[i].Message == "" {
+			conditions[i].Message = "Condition restored during migration to v1beta2"
+		}
+		if conditions[i].LastTransitionTime.IsZero() {
+			conditions[i].LastTransitionTime = metav1.Now()
+		}
+	}
+
+	obj.SetConditions(conditions)
+}
diff --git a/internal/pkg/cloud/scope/identity.go b/internal/pkg/cloud/scope/identity.go
index d38db02..90fa7dd 100644
--- a/internal/pkg/cloud/scope/identity.go
+++ b/internal/pkg/cloud/scope/identity.go
@@ -195,7 +195,13 @@ func (s *IdentityScope) generateIdentityLabelSelector() client.MatchingLabels {
 
 // PersistIndentityChanges persists the identity changes to the API server.
 func (s *IdentityScope) PersistIndentityChanges(ctx context.Context) error {
-	return s.patchHelper.Patch(ctx, s.Identity)
+	normalizeLegacyConditions(s.Identity)
+	return s.patchHelper.Patch(ctx, s.Identity, patch.WithOwnedConditions{Conditions: []string{
+		string(infrav1.IdentityReadyCondition),
+		string(infrav1.IdentityValidCondition),
+		string(infrav1.IdentitySecretUpdatedCondition),
+		string(infrav1.IdentityLinkedClustersUpdatedCondition),
+	}})
 }
 
 // IsSecretChanged checks if the secret of the identity has changed.
diff --git a/internal/pkg/cloud/scope/labels.go b/internal/pkg/cloud/scope/labels.go
index 667a776..e78c9c1 100644
--- a/internal/pkg/cloud/scope/labels.go
+++ b/internal/pkg/cloud/scope/labels.go
@@ -6,7 +6,7 @@ import (
 	"encoding/hex"
 	"strings"
 
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 )
 
 // labels mostly used in Yandex analytics to differentiate VMs created by CAPI
diff --git a/internal/pkg/cloud/scope/machine.go b/internal/pkg/cloud/scope/machine.go
index 9bd75e1..0bfa233 100644
--- a/internal/pkg/cloud/scope/machine.go
+++ b/internal/pkg/cloud/scope/machine.go
@@ -13,7 +13,7 @@ import (
 	corev1 "k8s.io/api/core/v1"
 	"k8s.io/apimachinery/pkg/types"
 	"k8s.io/utils/ptr"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 	capierrors "sigs.k8s.io/cluster-api/errors"
 	"sigs.k8s.io/cluster-api/util"
 	"sigs.k8s.io/cluster-api/util/patch"
@@ -77,7 +77,12 @@ type MachineScope struct {
 
 // PatchObject persists the cluster configuration and status.
 func (m *MachineScope) PatchObject() error {
-	return m.patchHelper.Patch(context.TODO(), m.YandexMachine)
+	normalizeLegacyConditions(m.YandexMachine)
+	return m.patchHelper.Patch(context.TODO(), m.YandexMachine, patch.WithOwnedConditions{Conditions: []string{
+		string(infrav1.ConditionStatusProvisioning),
+		string(infrav1.ConditionStatusRunning),
+		string(infrav1.ConditionStatusNotfound),
+	}})
 }
 
 // Close closes the current scope persisting the cluster configuration and status.
@@ -103,6 +108,7 @@ func (m *MachineScope) IsControlPlane() bool {
 // SetReady sets the YandexMachine Ready Status.
 func (m *MachineScope) SetReady() {
 	m.YandexMachine.Status.Ready = true
+	m.YandexMachine.Status.Initialization.Provisioned = true
 }
 
 // GetProviderID returns the YandexMachine providerID from the spec.
@@ -230,11 +236,11 @@ func (m *MachineScope) GetInstanceReq() (*compute.CreateInstanceRequest, error)
 	metadata["user-data"] = bootstrapData
 
 	return &compute.CreateInstanceRequest{
-		FolderId:   m.ClusterGetter.GetFolderID(),
-		Name:       m.YandexMachine.GetName(),
-		ZoneId:     *m.YandexMachine.Spec.ZoneID,
-		PlatformId: *m.YandexMachine.Spec.PlatformID,
-		Metadata:   metadata,
+		FolderId:      m.ClusterGetter.GetFolderID(),
+		Name:          m.YandexMachine.GetName(),
+		ZoneId:        *m.YandexMachine.Spec.ZoneID,
+		PlatformId:    *m.YandexMachine.Spec.PlatformID,
+		Metadata:      metadata,
 		Labels:        m.getMachineLabels(),
 		Hostname:      m.YandexMachine.GetName(),
 		ResourcesSpec: resourcesSpec,
diff --git a/internal/pkg/cloud/services/compute/reconcile.go b/internal/pkg/cloud/services/compute/reconcile.go
index f07b7e3..299827b 100644
--- a/internal/pkg/cloud/services/compute/reconcile.go
+++ b/internal/pkg/cloud/services/compute/reconcile.go
@@ -9,7 +9,7 @@ import (
 	"github.com/yandex-cloud/cluster-api-provider-yandex/internal/pkg/cloud/ycerrors"
 	yandex_compute "github.com/yandex-cloud/go-genproto/yandex/cloud/compute/v1"
 	corev1 "k8s.io/api/core/v1"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"sigs.k8s.io/cluster-api/util/conditions"
 	"sigs.k8s.io/controller-runtime/pkg/log"
 )
@@ -19,6 +19,16 @@ const (
 	instanceNotDeleted bool = false
 )
 
+func setMachineCondition(machine *infrav1.YandexMachine, conditionType, reason, message string, status metav1.ConditionStatus) {
+	conditions.Set(machine, metav1.Condition{
+		Type:               conditionType,
+		Status:             status,
+		Reason:             reason,
+		Message:            message,
+		LastTransitionTime: metav1.Now(),
+	})
+}
+
 // Reconcile reconciles compute instance.
 func (s *Service) Reconcile(ctx context.Context) error {
 	logger := log.FromContext(ctx)
@@ -31,8 +41,7 @@ func (s *Service) Reconcile(ctx context.Context) error {
 	if instanceID == "" {
 		newInstanceID, err := s.createComputeInstance(ctx, client)
 		if err != nil {
-			conditions.MarkFalse(s.scope.YandexMachine,
-				infrav1.ConditionStatusRunning, infrav1.ConditionStatusNotfound, clusterv1.ConditionSeverityError, "%s", err.Error())
+			setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusRunning), string(infrav1.ConditionStatusNotfound), err.Error(), metav1.ConditionFalse)
 			return err
 		}
 
@@ -46,39 +55,22 @@ func (s *Service) Reconcile(ctx context.Context) error {
 		if ycerrors.IsNotFound(err) {
 			logger.Info("unable to find compute instance in yandex cloud")
 			s.scope.SetInstanceStatus(infrav1.InstanceStatusDeleted)
-			conditions.MarkTrue(s.scope.YandexMachine, infrav1.ConditionStatusNotfound)
-			conditions.MarkFalse(s.scope.YandexMachine,
-				infrav1.ConditionStatusProvisioning,
-				infrav1.ConditionStatusError,
-				clusterv1.ConditionSeverityError,
-				"%s", err.Error())
-			conditions.MarkFalse(s.scope.YandexMachine,
-				infrav1.ConditionStatusRunning,
-				infrav1.ConditionStatusError,
-				clusterv1.ConditionSeverityError,
-				"%s", err.Error())
+			setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusNotfound), string(infrav1.ConditionStatusNotfound), "Compute instance was not found", metav1.ConditionTrue)
+			setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusProvisioning), infrav1.ConditionStatusError, err.Error(), metav1.ConditionFalse)
+			setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusRunning), infrav1.ConditionStatusError, err.Error(), metav1.ConditionFalse)
 			return nil
 		}
-		conditions.MarkUnknown(s.scope.YandexMachine,
-			infrav1.ConditionStatusProvisioning,
-			infrav1.ConditionStatusNotfound,
-			"%s", err.Error())
-		conditions.MarkUnknown(s.scope.YandexMachine,
-			infrav1.ConditionStatusRunning,
-			infrav1.ConditionStatusNotfound,
-			"%s", err.Error())
+		setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusProvisioning), string(infrav1.ConditionStatusNotfound), err.Error(), metav1.ConditionUnknown)
+		setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusRunning), string(infrav1.ConditionStatusNotfound), err.Error(), metav1.ConditionUnknown)
 		return fmt.Errorf("unable to find compute instance %v: %w", s.scope.GetInstanceID(), err)
 	}
 
 	instanceState := infrav1.InstanceStatus(vm.GetStatus().String())
+	setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusNotfound), string(infrav1.ConditionStatusRunning), "Compute instance exists", metav1.ConditionFalse)
 	if instanceState == infrav1.InstanceStatusRunning {
 		instanceAddress, err := s.getInstanceAddress(vm)
 		if err != nil {
-			conditions.MarkFalse(s.scope.YandexMachine,
-				infrav1.ConditionStatusRunning,
-				infrav1.ConditionStatusError,
-				clusterv1.ConditionSeverityError,
-				"%s", err.Error())
+			setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusRunning), infrav1.ConditionStatusError, err.Error(), metav1.ConditionFalse)
 			return err
 		}
 
@@ -90,7 +82,11 @@ func (s *Service) Reconcile(ctx context.Context) error {
 				return fmt.Errorf("failed to register controlplane compute instance in load balancer: %w", err)
 			}
 		}
-		conditions.MarkTrue(s.scope.YandexMachine, infrav1.ConditionStatusRunning)
+		setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusProvisioning), string(infrav1.ConditionStatusRunning), "Compute instance provisioning completed", metav1.ConditionFalse)
+		setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusRunning), string(infrav1.ConditionStatusRunning), "Compute instance is running", metav1.ConditionTrue)
+	} else {
+		setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusProvisioning), string(infrav1.ConditionStatusProvisioning), fmt.Sprintf("Compute instance state is %s", instanceState), metav1.ConditionTrue)
+		setMachineCondition(s.scope.YandexMachine, string(infrav1.ConditionStatusRunning), string(infrav1.ConditionStatusProvisioning), fmt.Sprintf("Compute instance state is %s", instanceState), metav1.ConditionFalse)
 	}
 
 	s.scope.SetInstanceStatus(infrav1.InstanceStatus(vm.GetStatus().String()))
diff --git a/internal/pkg/cloud/services/loadbalancers/alb.go b/internal/pkg/cloud/services/loadbalancers/alb.go
index d35a889..4c394cf 100644
--- a/internal/pkg/cloud/services/loadbalancers/alb.go
+++ b/internal/pkg/cloud/services/loadbalancers/alb.go
@@ -6,7 +6,6 @@ import (
 	"reflect"
 
 	alb "github.com/yandex-cloud/go-genproto/yandex/cloud/apploadbalancer/v1"
-	"sigs.k8s.io/cluster-api/util/conditions"
 	"sigs.k8s.io/controller-runtime/pkg/log"
 
 	infrav1 "github.com/yandex-cloud/cluster-api-provider-yandex/api/v1alpha1"
@@ -260,7 +259,6 @@ func (s *Service) reconcileALB(ctx context.Context, backendGroupID string) error
 			return err
 		}
 		logger.Info("application loadbalancer created", "instance id", id)
-		conditions.MarkTrue(s.scope.YandexCluster, infrav1.ConditionStatusReady)
 		return nil
 	}
 
diff --git a/main.go b/main.go
index 2e9ac48..1364d7f 100644
--- a/main.go
+++ b/main.go
@@ -35,7 +35,8 @@ import (
 	corev1 "k8s.io/api/core/v1"
 	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
 	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
-	clusterv1 "sigs.k8s.io/cluster-api/api/v1beta1"
+	clusterv1beta1 "sigs.k8s.io/cluster-api/api/core/v1beta1"
+	clusterv1 "sigs.k8s.io/cluster-api/api/core/v1beta2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	metricsserver "sigs.k8s.io/controller-runtime/pkg/metrics/server"
 
@@ -59,6 +60,7 @@ const defaultNamespace = "capy-system"
 func init() {
 	utilruntime.Must(clientgoscheme.AddToScheme(scheme))
 	utilruntime.Must(corev1.AddToScheme(scheme))
+	utilruntime.Must(clusterv1beta1.AddToScheme(scheme))
 	utilruntime.Must(clusterv1.AddToScheme(scheme))
 	utilruntime.Must(infrav1.AddToScheme(scheme))
 	//+kubebuilder:scaffold:scheme
