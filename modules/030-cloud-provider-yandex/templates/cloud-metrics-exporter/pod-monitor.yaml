{{- if and .Values.cloudProviderYandex.internal.providerDiscoveryData.natInstanceName .Values.cloudProviderYandex.cloudMetricsExporterEnabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: yandex-nat-instance-metrics
  namespace: d8-monitoring
  {{- include "helm_lib_module_labels" (list . (dict "prometheus" "main" "app" "yandex-cloud-metrics-exporter")) | nindent 2 }}
spec:
  jobLabel: yandex-nat-instance
  podMetricsEndpoints:
    - port: https-metrics
      path: /metrics/compute
      scheme: https
      tlsConfig:
        insecureSkipVerify: true
      honorLabels: true
      scrapeTimeout: "20s"
      metricRelabelings:
        - sourceLabels: [ resource_id ]
          regex: {{ .Values.cloudProviderYandex.internal.natInstanceName }}
          action: keep
        - targetLabel: "nat_instance"
          replacement: "true"
  selector:
    matchLabels:
      app: yandex-cloud-metrics-exporter
  namespaceSelector:
    matchNames:
      - d8-monitoring
{{- end }}
