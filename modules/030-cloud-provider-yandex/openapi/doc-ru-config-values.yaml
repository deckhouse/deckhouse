# This file is auto-generated by openapigen. Do not edit manually.
description: |
  Описывает конфигурацию модуля cloud-provider-yandex.

  Выполните следующую команду, чтобы изменить конфигурацию в работающем кластере:

  ```shell
  d8 k edit moduleconfig cloud-provider-yandex
  ```
properties:
  ccm:
    properties:
      disabled:
        type: boolean
      parameters:
        properties:
          additionalExternalNetworkIDs:
            description: |
              Дополнительные ID внешних сетей, которые будут распознаваться CCM как внешние сети.
            items:
              type: string
            type: array
        type: object
    type: object
  nodes:
    properties:
      disabled:
        type: boolean
      parameters:
        properties:
          dhcpOptions:
            description: |
              Список DHCP-опций, которые будут установлены на все подсети.

              [Возможные проблемы](https://deckhouse.ru/modules/cloud-provider-yandex/faq.html#проблемы-dhcpoptions-и-пути-их-решения) при использовании.
            properties:
              domainName:
                description: |
                  Search-домен.
                type: string
              domainNameServers:
                description: |
                  Список адресов рекурсивных DNS.
                items:
                  type: string
                type: array
            type: object
          existingNetworkID:
            description: |
              ID существующей VPC Network.
            type: string
          existingZoneToSubnetIDMap:
            additionalProperties:
              type: string
            description: |
              Одна или несколько ранее существовавших подсетей, сопоставленных с соответствующей зоной.

              **Внимание.** При использовании `cni-simple-bridge` DKP создаст таблицу маршрутизации, которую необходимо вручную привязать к указанным подсетям. К одной подсети можно привязать только одну таблицу маршрутизации, поэтому невозможно развернуть несколько кластеров с `cni-simple-bridge` в одних и тех же подсетях. Начиная с DKP 1.76 для новых кластеров в Yandex Cloud по умолчанию используется `cni-cilium` в режиме `VXLAN`. В этом режиме маршрутизация трафика подов не зависит от таблиц маршрутизации Yandex Cloud.
            type: object
          externalIPAddresses:
            additionalProperties:
              items:
                type: string
              type: array
            description: |
              Внешние IP-адреса для каждой группы узлов.

              Ключ — имя группы узлов (например, `system`, `master`, `worker`), значение — список внешних IP-адресов для узлов этой группы, перечисленных в порядке зон, в которых будут создаваться узлы.

              В списке можно указывать следующие значения:
              - IP-адрес из дополнительной внешней сети для соответствующей зоны (параметр `externalSubnetIDs`);
              - [зарезервированный публичный IP-адрес](faq.html#как-зарезервировать-публичный-ip-адрес), если список дополнительных внешних сетей не определён (параметр `externalSubnetIDs`);
              - `Auto` — для заказа публичного IP-адреса в соответствующей зоне.
            type: object
          externalSubnetIDs:
            additionalProperties:
              items:
                type: string
              type: array
            description: |
              ID дополнительных внешних сетей для каждой группы узлов.

              Ключ — имя группы узлов (например, `system`, `master`, `worker`), значение — список ID внешних подсетей для узлов этой группы, перечисленных в порядке зон, в которых будут создаваться узлы.
            type: object
          labels:
            additionalProperties:
              type: string
            description: |
              Лейблы, проставляемые на ресурсы, создаваемые в Yandex Cloud.

              Если поменять лейблы в рабочем кластере, после применения изменений необходимо пересоздать все машины.
            type: object
          layout:
            description: |
              Название схемы размещения.

              [Подробнее](https://deckhouse.ru/modules/cloud-provider-yandex/layouts.html) о возможных схемах размещения провайдера.
            type: string
          nodeNetworkCIDR:
            description: |
              Данная подсеть будет разделена на **четыре** равные части.

              Они будут использоваться в качестве основы для подсетей в четырёх зонах Yandex Cloud.
            type: string
          sshPublicKey:
            description: |
              Публичный ключ для доступа на узлы.
            type: string
          withNATInstance:
            description: |
              Настройки для схемы размещения [`WithNATInstance`](https://deckhouse.ru/modules/cloud-provider-yandex/layouts.html#withnatinstance).
            properties:
              externalSubnetID:
                description: |
                  Подключаемый к узлу дополнительный сетевой интерфейс, в который будет идти маршрут по умолчанию.
                type: string
              internalSubnetCIDR:
                description: |
                  CIDR автоматически создаваемой подсети для внутреннего интерфейса. Если указан вместе с `internalSubnetID`, `internalSubnetCIDR` имеет приоритет.
                type: string
              internalSubnetID:
                description: |
                  ID подсети для внутреннего интерфейса.
                type: string
              natInstanceExternalAddress:
                description: |
                  Внешний [зарезервированный IP-адрес](https://deckhouse.ru/modules/cloud-provider-yandex/faq.html#как-зарезервировать-публичный-ip-адрес) или адрес из `externalSubnetID` при указании опции.
                type: string
              natInstanceInternalAddress:
                description: |
                  Лучше не использовать эту опцию, а использовать автоматически назначаемые адреса.
                type: string
              natInstanceResources:
                description: |
                  Вычислительные ресурсы, выделяемые для NAT-инстанса. Если параметр не указан, будут использоваться значения по умолчанию.

                  > **Внимание.** При изменении этих параметров, `terraform-auto-converger` перезапустит машину NAT-инстанса автоматически, если включена настройка [autoConvergerEnabled](../terraform-manager/configuration.html#parameters-autoconvergerenabled). Это может привести к временному прерыванию трафика в кластере.
                properties:
                  cores:
                    description: |
                      Количество ядер у создаваемого NAT-инстанса.
                    type: integer
                  memory:
                    description: |
                      Количество оперативной памяти (в мегабайтах) у создаваемого NAT-инстанса.
                    type: integer
                  platform:
                    description: |
                      Тип платформы процессора у создаваемого NAT-инстанса.
                    type: string
                type: object
            type: object
          zones:
            description: |
              Глобальное ограничение набора зон, с которыми работает данный cloud-провайдер.
            items:
              type: string
            type: array
        type: object
    type: object
  provider:
    properties:
      parameters:
        description: |
          Содержит настройки для подключения к API Yandex Cloud.
        properties:
          cloudID:
            description: |
              Идентификатор облака.
            type: string
          folderID:
            description: |
              Идентификатор директории.
            type: string
        type: object
    type: object
  storage:
    properties:
      disabled:
        type: boolean
      parameters:
        properties:
          excludedStorageClasses:
            description: |
              Список классов хранения, исключаемых из использования в кластере.
            items:
              type: string
            type: array
        type: object
    type: object
type: object
