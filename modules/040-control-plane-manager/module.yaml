name: control-plane-manager
critical: true
weight: 40
subsystems:
  - kubernetes
namespace: kube-system
stage: General Availability
disable:
  confirmation: true
  message: "After the module is disabled, the control plane components will no longer be maintained, which may render the cluster inoperable."
  messages:
    en: "After the module is disabled, the control plane components will no longer be maintained, which may render the cluster inoperable."
    ru: "После отключения модуля компоненты control plane перестанут обслуживаться, что может привести к неработоспособности кластера."
descriptions:
  en: Manages the cluster's control plane components including certificates, configurations, and versions.
  ru: Управляет компонентами control plane кластера, включая сертификаты, конфигурации и версии.
accessibility:
  editions:
    _default:
      available: true
      enabledInBundles:
        - Default
    cse:
      available: true
      enabledInBundles:
        - Default
        - Managed
