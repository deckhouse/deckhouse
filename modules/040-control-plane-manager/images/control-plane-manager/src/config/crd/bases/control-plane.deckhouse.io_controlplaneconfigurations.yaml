---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: controlplaneconfigurations.control-plane.deckhouse.io
spec:
  group: control-plane.deckhouse.io
  names:
    kind: ControlPlaneConfiguration
    listKind: ControlPlaneConfigurationList
    plural: controlplaneconfigurations
    shortNames:
    - cpc
    singular: controlplaneconfiguration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ControlPlaneConfiguration is the Schema for the control plane
          configuration API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ControlPlaneConfigurationSpec defines the desired state of
              ControlPlaneConfiguration
            properties:
              components:
                description: Components contains per-component checksums
                properties:
                  etcd:
                    description: Etcd component checksum (SHA256 of etcd.yaml + referenced
                      files)
                    properties:
                      checksum:
                        description: Checksum is SHA256 hash of component manifest
                          and its referenced files
                        type: string
                    required:
                    - checksum
                    type: object
                  kube-apiserver:
                    description: KubeAPIServer component checksum (SHA256 of kube-apiserver.yaml
                      + oidc-ca.crt + webhook-config.yaml + ...)
                    properties:
                      checksum:
                        description: Checksum is SHA256 hash of component manifest
                          and its referenced files
                        type: string
                    required:
                    - checksum
                    type: object
                  kube-controller-manager:
                    description: KubeControllerManager component checksum (SHA256
                      of kube-controller-manager.yaml + referenced files)
                    properties:
                      checksum:
                        description: Checksum is SHA256 hash of component manifest
                          and its referenced files
                        type: string
                    required:
                    - checksum
                    type: object
                  kube-scheduler:
                    description: KubeScheduler component checksum (SHA256 of kube-scheduler.yaml
                      + scheduler-config.yaml)
                    properties:
                      checksum:
                        description: Checksum is SHA256 hash of component manifest
                          and its referenced files
                        type: string
                    required:
                    - checksum
                    type: object
                type: object
              pkiChecksum:
                description: PKIChecksum is SHA256 hash of PKI certificates from Secret
                  d8-pki
                type: string
            type: object
          status:
            description: ControlPlaneConfigurationStatus defines the observed state
              of ControlPlaneConfiguration
            properties:
              lastUpdateTime:
                description: LastUpdateTime is the last time the configuration was
                  updated
                format: date-time
                type: string
              message:
                description: Message provides additional details about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed ControlPlaneConfiguration
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of control plane configuration
                enum:
                - Pending
                - InProgress
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
