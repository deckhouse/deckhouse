---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: controlplanenodes.control-plane.deckhouse.io
spec:
  group: control-plane.deckhouse.io
  names:
    kind: ControlPlaneNode
    listKind: ControlPlaneNodeList
    plural: controlplanenodes
    shortNames:
    - cpn
    singular: controlplanenode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Etcd ready
      jsonPath: .status.conditions[?(@.type=='EtcdReady')].status
      name: ETCD
      type: string
    - description: API server ready
      jsonPath: .status.conditions[?(@.type=='APIServerReady')].status
      name: APISERVER
      type: string
    - description: Controller manager ready
      jsonPath: .status.conditions[?(@.type=='ControllerManagerReady')].status
      name: CONTROLLERMANAGER
      type: string
    - description: Scheduler ready
      jsonPath: .status.conditions[?(@.type=='SchedulerReady')].status
      name: SCHEDULER
      type: string
    - description: CA synced
      jsonPath: .status.conditions[?(@.type=='CASynced')].status
      name: CA
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              caChecksum:
                description: CAChecksum is the hash of d8-pki secret (CA certificates).
                type: string
              components:
                description: Checksums per component
                properties:
                  etcd:
                    description: ComponentSpec holds the desired state of a single
                      control plane component (used in CPN spec).
                    properties:
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                    type: object
                  kube-apiserver:
                    description: ComponentSpec holds the desired state of a single
                      control plane component (used in CPN spec).
                    properties:
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                    type: object
                  kube-controller-manager:
                    description: ComponentSpec holds the desired state of a single
                      control plane component (used in CPN spec).
                    properties:
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                    type: object
                  kube-scheduler:
                    description: ComponentSpec holds the desired state of a single
                      control plane component (used in CPN spec).
                    properties:
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                    type: object
                type: object
            type: object
          status:
            properties:
              caChecksum:
                type: string
              components:
                description: ComponentsStatus holds status checksums and observed
                  state for all control plane components.
                properties:
                  etcd:
                    description: ComponentStatus holds the observed state of a single
                      control plane component (used in CPN status).
                    properties:
                      certificatesExpirationDate:
                        additionalProperties:
                          format: date-time
                          type: string
                        description: |-
                          CertificatesExpirationDate maps cert file names to their NotAfter timestamps.
                          Populated via CertObserve command.
                        type: object
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                      lastObservedAt:
                        description: LastObservedAt is the timestamp of the last completed
                          CertObserve for this component.
                        format: date-time
                        type: string
                    type: object
                  kube-apiserver:
                    description: ComponentStatus holds the observed state of a single
                      control plane component (used in CPN status).
                    properties:
                      certificatesExpirationDate:
                        additionalProperties:
                          format: date-time
                          type: string
                        description: |-
                          CertificatesExpirationDate maps cert file names to their NotAfter timestamps.
                          Populated via CertObserve command.
                        type: object
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                      lastObservedAt:
                        description: LastObservedAt is the timestamp of the last completed
                          CertObserve for this component.
                        format: date-time
                        type: string
                    type: object
                  kube-controller-manager:
                    description: ComponentStatus holds the observed state of a single
                      control plane component (used in CPN status).
                    properties:
                      certificatesExpirationDate:
                        additionalProperties:
                          format: date-time
                          type: string
                        description: |-
                          CertificatesExpirationDate maps cert file names to their NotAfter timestamps.
                          Populated via CertObserve command.
                        type: object
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                      lastObservedAt:
                        description: LastObservedAt is the timestamp of the last completed
                          CertObserve for this component.
                        format: date-time
                        type: string
                    type: object
                  kube-scheduler:
                    description: ComponentStatus holds the observed state of a single
                      control plane component (used in CPN status).
                    properties:
                      certificatesExpirationDate:
                        additionalProperties:
                          format: date-time
                          type: string
                        description: |-
                          CertificatesExpirationDate maps cert file names to their NotAfter timestamps.
                          Populated via CertObserve command.
                        type: object
                      checksums:
                        description: Checksums holds config/pki/ca hashes for a single
                          control plane component.
                        properties:
                          ca:
                            description: |-
                              CA is the hash of CA certificates applied to this component.
                              In spec absent (CA is global). In status set when the component's pod restarts with the new CA.
                            type: string
                          config:
                            description: Config is the hash of the component static
                              pod template and extra-files.
                            type: string
                          pki:
                            description: PKI is the hash of PKI-related config keys
                              (certSANs, encryption-algorithm).
                            type: string
                        type: object
                      lastObservedAt:
                        description: LastObservedAt is the timestamp of the last completed
                          CertObserve for this component.
                        format: date-time
                        type: string
                    type: object
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
