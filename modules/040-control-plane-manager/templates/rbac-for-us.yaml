---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: d8-control-plane-manager
  namespace: kube-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "d8-control-plane-manager")) | nindent 2 }}
automountServiceAccountToken: false
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: d8:control-plane-manager
  namespace: kube-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "d8-control-plane-manager")) | nindent 2 }}
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "watch", "list"]
- apiGroups: ["coordination.k8s.io"]
  resources: ["leases"]
  verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "watch", "list"]
- apiGroups: [""]
  resources: ["configmaps"]
  verbs: ["get", "create", "update"]
- apiGroups: [""]
  resources: ["events"]
  verbs: ["create"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: d8:control-plane-manager
  namespace: kube-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "d8-control-plane-manager")) | nindent 2 }}
subjects:
- kind: ServiceAccount
  name: d8-control-plane-manager
  namespace: kube-system
roleRef:
  kind: Role
  name: d8:control-plane-manager
  apiGroup: rbac.authorization.k8s.io

---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: d8:control-plane-manager
  {{- include "helm_lib_module_labels" (list . (dict "app" "d8-control-plane-manager")) | nindent 2 }}
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list", "watch", "update"]
  - apiGroups: ["deckhouse.io"]
    resources: ["nodegroups"]
    verbs: ["get", "list", "watch"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: d8:control-plane-manager
  {{- include "helm_lib_module_labels" (list . (dict "app" "d8-control-plane-manager")) | nindent 2 }}
subjects:
  - kind: ServiceAccount
    name: d8-control-plane-manager
    namespace: kube-system
roleRef:
  kind: ClusterRole
  name: d8:control-plane-manager
  apiGroup: rbac.authorization.k8s.io
{{- $userAuthzEnabled := has "user-authz" ($.Values.global.enabledModules | default list) }}
{{- /*
  ClusterRoleBinding kubeadm:cluster-admins is gated by THREE independent flags to avoid the
  chicken-and-egg case on a fresh cluster: kubeadm pre-creates kubeadm:cluster-admins -> cluster-admin,
  Helm cannot mutate roleRef (it is immutable), and SSA fails permanently if the desired
  ClusterRole does not yet exist.

  1. $userAuthzEnabled            — user-authz module is turned on in values.
  2. $clusterBootstrapped         — set to true by the global hook cluster_is_bootstrapped after
                                    the first non-master node becomes Ready. Until then we MUST
                                    keep the kubeadm-default wildcard binding so the very first
                                    helm install of control-plane-manager can succeed.
  3. $userAuthzCRAvailable        — set to true by the OnBeforeHelm hook
                                    reconcile_kubeadm_cluster_admins_binding when it observes that
                                    ClusterRole user-authz:cluster-admin is actually present in
                                    the API. This catches the race where user-authz is enabled
                                    and the cluster is bootstrapped, but the user-authz module
                                    itself has not rendered yet (e.g., right after enabling it on
                                    an existing cluster). The flag lives under
                                    .internal.* and cannot be set via ModuleConfig.

  IMPORTANT: read the flags via `dig` (and not `.Values.<...> | default false`) because they are
  set asynchronously by hooks — an early helm render happens before the parent maps are populated,
  and a chained lookup through a missing parent would either fail or silently render the granular
  branch. The binding is switched ONLY when all three flags are explicitly true.

  NOTE on supplement: the supplement ClusterRole+ClusterRoleBinding render under a SINGLE gate
  ($userAuthzEnabled), without waiting for clusterBootstrapped/userAuthzCRAvailable. The supplement
  is a strict ADD-ON of permissions to the same Group/kubeadm:cluster-admins, so even before the
  main binding flips it does no harm (the kubeadm-default cluster-admin wildcard already grants
  more than the supplement does). Pre-rendering the supplement collapses the otherwise-unavoidable
  transient window where the main binding has already moved to user-authz:cluster-admin (which
  intentionally does NOT include nodes/proxy) but the supplement is not yet applied — without
  this split, kubectl exec/logs/port-forward via admin.conf would briefly return 403 right after
  the rebind.
*/}}
{{- $clusterBootstrapped := dig "clusterIsBootstrapped" false .Values.global }}
{{- $userAuthzCRAvailable := dig "internal" "userAuthzClusterAdminClusterRoleAvailable" false .Values.controlPlaneManager }}
{{- if $userAuthzEnabled }}
---
# Extra rules for identity kubernetes-admin (group kubeadm:cluster-admins) beyond user-authz:cluster-admin.
# No user-authz.deckhouse.io/access-level annotation: that pattern is for optional ClusterRoles merged into
# ClusterAuthorizationRule bindings; this role must apply only to kubeadm:cluster-admins via explicit bindings below.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:control-plane-manager:admin-kubeconfig-supplement
  {{- include "helm_lib_module_labels" (list .) | nindent 2 }}
rules:
- apiGroups: [""]
  resources: ["pods/portforward"]
  verbs: ["create", "get"]
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["create", "delete", "deletecollection", "patch", "update"]
- apiGroups: [""]
  resources: ["nodes/status"]
  verbs: ["get", "patch", "update"]
# Required for kube-apiserver-kubelet-client (group kubeadm:cluster-admins per kubeadm 1.29+ defaults):
# every kubelet API call from apiserver (exec, attach, logs, portforward) is authorized as nodes/proxy.
# Deliberately kept OUT of user-authz:cluster-admin so that the high-level ClusterAdmin role
# (granted to users via ClusterAuthorizationRule.accessLevel=ClusterAdmin) does NOT silently turn
# into a root-on-every-node permission via the kubelet API. nodes/proxy is admin.conf-specific
# and lives only in this supplement bound to Group/kubeadm:cluster-admins.
- apiGroups: [""]
  resources: ["nodes/proxy"]
  verbs: ["get", "create"]
- apiGroups: [""]
  resources: ["users", "groups", "serviceaccounts"]
  verbs: ["impersonate"]
# Standard k8s resources not granted by user-authz:cluster-admin.
- apiGroups: [""]
  resources: ["persistentvolumes"]
  verbs: ["create", "delete", "deletecollection", "patch", "update"]
- apiGroups: [""]
  resources: ["persistentvolumes/status"]
  verbs: ["get", "patch", "update"]
- apiGroups: [""]
  resources: ["replicationcontrollers"]
  verbs: ["create", "delete", "deletecollection", "patch", "update"]
- apiGroups: [""]
  resources: ["replicationcontrollers/scale"]
  verbs: ["get", "patch", "update"]
- apiGroups: [""]
  resources: ["componentstatuses"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["bindings", "pods/binding"]
  verbs: ["create"]
- apiGroups: [""]
  resources: ["events"]
  verbs: ["create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["events.k8s.io"]
  resources: ["events"]
  verbs: ["create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["apps"]
  resources: ["controllerrevisions"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["networking.k8s.io"]
  resources: ["ingressclasses"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["storage.k8s.io"]
  resources: ["volumeattachments", "csinodes", "csidrivers", "csistoragecapacities"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["node.k8s.io"]
  resources: ["runtimeclasses"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["flowcontrol.apiserver.k8s.io"]
  resources: ["flowschemas", "prioritylevelconfigurations"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["admissionregistration.k8s.io"]
  resources: ["validatingadmissionpolicies", "validatingadmissionpolicybindings"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["authorization.k8s.io"]
  resources: ["subjectaccessreviews", "localsubjectaccessreviews", "selfsubjectaccessreviews", "selfsubjectrulesreviews"]
  verbs: ["create"]
- apiGroups: ["authentication.k8s.io"]
  resources: ["tokenreviews", "selfsubjectreviews"]
  verbs: ["create"]
- apiGroups: ["deckhouse.io"]
  resources: ["*"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["deckhouse.io"]
  resources:
  - moduleconfigs
  - deckhousereleases
  - modulesources
  - moduleupdatepolicies
  - modulepulloverrides
  - packagerepositories
  - packagerepositoryoperations
  - applicationpackageversions
  - applicationpackages
  - applications
  - nodegroups
  - nodeusers
  - staticinstances
  - sshcredentials
  - authorizationrules
  - clusterauthorizationrules
  - users
  - useroperations
  - dexproviders
  - dexauthenticators
  - ingressnginxcontrollers
  - securitypolicies
  - securitypolicyexceptions
  - operationpolicies
  - customprometheusrules
  - clusterlogdestinations
  - clusterloggingconfigs
  - podloggingconfigs
  - projects
  verbs: ["create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["certificates.k8s.io"]
  resources: ["certificatesigningrequests"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["certificates.k8s.io"]
  resources: ["certificatesigningrequests/approval", "certificatesigningrequests/status"]
  verbs: ["get", "patch", "update"]
- nonResourceURLs: ["/api", "/api/*", "/apis", "/apis/*", "/healthz", "/healthz/*", "/version", "/openapi/*", "/logs", "/metrics"]
  verbs: ["get"]
- apiGroups: ["coordination.k8s.io"]
  resources: ["leases"]
  verbs: ["get", "list", "watch", "create", "delete", "patch", "update"]
- apiGroups: ["admissionregistration.k8s.io"]
  resources: ["mutatingwebhookconfigurations", "validatingwebhookconfigurations"]
  verbs: ["get", "list", "watch", "create", "delete", "patch", "update"]
- apiGroups: ["scheduling.k8s.io"]
  resources: ["priorityclasses"]
  verbs: ["get", "list", "watch", "create", "delete", "patch", "update"]
- apiGroups: ["apiregistration.k8s.io"]
  resources: ["apiservices"]
  verbs: ["get", "list", "watch", "create", "delete", "deletecollection", "patch", "update"]
- apiGroups: ["apiregistration.k8s.io"]
  resources: ["apiservices/status"]
  verbs: ["get", "patch", "update"]
- apiGroups: ["apps", "extensions"]
  resources: ["deployments/scale", "statefulsets/scale", "replicasets/scale"]
  verbs: ["get", "patch", "update"]
---
# Supplement ClusterRoleBinding renders under the SINGLE gate $userAuthzEnabled (deliberately
# without waiting for clusterBootstrapped/userAuthzCRAvailable). It is purely additive on the
# same Group/kubeadm:cluster-admins, so it cannot harm the cluster while the main binding is
# still on the kubeadm-default cluster-admin wildcard. Rendering it early eliminates the
# transient 403 window for kubectl exec/logs/port-forward right after the main rebind.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: d8:control-plane-manager:kubeadm-cluster-admins-supplement
  {{- include "helm_lib_module_labels" (list .) | nindent 2 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: d8:control-plane-manager:admin-kubeconfig-supplement
subjects:
- kind: Group
  name: kubeadm:cluster-admins
{{- end }}
{{/*
  ClusterRoleBinding kubeadm:cluster-admins: binds the admin.conf group to RBAC.
  Switched to user-authz:cluster-admin only when all three gates are satisfied; otherwise stays
  on the kubeadm-default cluster-admin wildcard so the very first helm install does not collide
  with the immutable roleRef rule. Hook reconcile_kubeadm_cluster_admins_binding (OnBeforeHelm,
  Order=5) actually recreates the binding when its desired roleRef changes — Helm SSA on its own
  cannot mutate roleRef.
*/}}
{{- if and $userAuthzEnabled (eq $clusterBootstrapped true) (eq $userAuthzCRAvailable true) }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubeadm:cluster-admins
  {{- include "helm_lib_module_labels" (list .) | nindent 2 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: user-authz:cluster-admin
subjects:
- kind: Group
  name: kubeadm:cluster-admins
{{- else }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubeadm:cluster-admins
  {{- include "helm_lib_module_labels" (list .) | nindent 2 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: Group
  name: kubeadm:cluster-admins
{{- end }}
