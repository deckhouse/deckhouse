#!/bin/bash

source /deckhouse/shell_lib.sh

function __config__() {
  cat <<EOF
    configVersion: v1
    onStartup: 10
EOF
}

function __on_startup() {
  if [[ -f /deckhouse/version ]] ; then
    values::set deckhouse.internal.currentVersion "\"$(cat /deckhouse/version)\""
  fi
}

hook::run "$@"
