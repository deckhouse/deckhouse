{{- $context := . }}
{{- $_ := set $context "import" `
- image: ssh-static
  add: /ssh/bin
  to: /bin
  before: setup
  includePaths:
    - ssh` }}
{{- $_ := set $context "build" (dict "file" "cmd/main.go") }}
{{- include "image-all" $context }}
