{{- if or (.Values.global.discovery.apiVersions | has "deckhouse.io/v1alpha1/SecurityPolicyException") (.Capabilities.APIVersions.Has "deckhouse.io/v1alpha1/SecurityPolicyException") }}
---
apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  name: node-controller
  namespace: d8-cloud-instance-manager
  {{- include "helm_lib_module_labels" (list . (dict "app" "node-controller")) | nindent 2 }}
spec:
  network:
    hostNetwork:
      allowedValue: true
      metadata:
        description: |
          Allow the node-controller to run on the host network during the bootstrap phase, when CNI is not yet available
    hostPorts:
      - port: 4206
        protocol: TCP
        metadata:
          description: Node controller health probe endpoint.
      - port: 4207
        protocol: TCP
        metadata:
          description: Node controller webhook listener.
      - port: 4208
        protocol: TCP
        metadata:
          description: Node controller kube-rbac-proxy metrics endpoint.
{{- end }}
