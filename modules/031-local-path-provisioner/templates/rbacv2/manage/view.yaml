apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: local-path-provisioner
    rbac.deckhouse.io/aggregate-to-infrastructure-as: viewer
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: d8-local-path-provisioner
  name: d8:system-capability:local-path-provisioner:view
  annotations:
    en.meta.deckhouse.io/title: "Module local-path-provisioner: view configuration"
    ru.meta.deckhouse.io/title: "Модуль local-path-provisioner: просмотр конфигурации"
    en.meta.deckhouse.io/description: "Read-only access to the local-path-provisioner module configuration."
    ru.meta.deckhouse.io/description: "Доступ только на чтение к конфигурации модуля local-path-provisioner."
rules:
- apiGroups:
  - deckhouse.io
  resources:
  - localpathprovisioners
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - deckhouse.io
  resourceNames:
  - local-path-provisioner
  resources:
  - moduleconfigs
  verbs:
  - get
  - list
  - watch
