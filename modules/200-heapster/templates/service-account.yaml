{{- if not (eq .Values.global.discovery.clusterType "ACS") }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: heapster
  namespace: kube-system
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
{{- end }}
