apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: heapster
  namespace: kube-system
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: heapster
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: heapster
