---
apiVersion: v1
kind: Secret
metadata:
  name: okmeter
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: okmeter
data:
  api-token: {{ .Values.okmeter.apiKey | b64enc | quote }}
