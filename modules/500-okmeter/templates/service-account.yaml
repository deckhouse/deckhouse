apiVersion: v1
kind: ServiceAccount
metadata:
  name: okmeter
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: okmeter
automountServiceAccountToken: true
