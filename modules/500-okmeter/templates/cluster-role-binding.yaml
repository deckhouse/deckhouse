apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  name: kube-okmeter
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: okmeter
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: okmeter
subjects:
- kind: ServiceAccount
  name: okmeter
  namespace: kube-okmeter
