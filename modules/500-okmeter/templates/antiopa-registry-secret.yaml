apiVersion: v1
kind: Secret
metadata:
  name: okmeter-antiopa-registry
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
type: kubernetes.io/dockercfg
data:
  .dockercfg: {{ $.Values.global.modulesImages.registryDockercfg }}
