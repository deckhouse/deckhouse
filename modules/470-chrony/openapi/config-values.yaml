type: object
properties:
  ntpServers:
    type: array
    items:
      type: string
    x-examples:
    - ["pool.ntp.org"]
    - ["pool.ntp.org", "ntp.ubuntu.com", "time.google.com"]
    description: |
      NTP servers to sync.
    default: ["pool.ntp.org"]
