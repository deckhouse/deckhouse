---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: user-authz:scale
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
rules:
- apiGroups:
  - apps
  - extensions
  resources:
  - deployments/scale
  - statefulsets/scale
  verbs:
  - get
  - patch
  - update
