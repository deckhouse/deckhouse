apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:manage:all:viewer
  labels:
    heritage: deckhouse
    rbac.deckhouse.io/kind: manage
    rbac.deckhouse.io/aggregate-to-scope: others
    rbac.deckhouse.io/aggregate-to-role: manager
    rbac.deckhouse.io/aggregate-to-all-role: viewer
aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.deckhouse.io/kind: manage
        rbac.deckhouse.io/aggregate-to-scope: others
        rbac.deckhouse.io/aggregate-to-role: viewer
rules: []