apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:manage:all:manager
  labels:
    heritage: deckhouse
    module: user-authz
    rbac.deckhouse.io/use-role: user
    rbac.deckhouse.io/kind: manage
    rbac.deckhouse.io/level: all
    user-authz.deckhouse.io/can-assign-basic-max: ClusterAdmin
    user-authz.deckhouse.io/can-assign-scope: system
    user-authz.deckhouse.io/can-assign-max-level: admin
aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.deckhouse.io/kind: manage
        rbac.deckhouse.io/aggregate-to-all-as: manager
rules: []