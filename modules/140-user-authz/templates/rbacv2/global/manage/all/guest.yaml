apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:manage:all:guest
  labels:
    heritage: deckhouse
    module: user-authz
    rbac.deckhouse.io/kind: manage
aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.deckhouse.io/aggregate-to-all-as: guest
rules: []