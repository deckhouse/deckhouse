apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:subsystem:security:manager
  annotations:
    en.meta.deckhouse.io/title: "Security: Manager"
    ru.meta.deckhouse.io/title: "Управление подсистемы безопасности"
    en.meta.deckhouse.io/description: "Management of the Security subsystem configuration."
    ru.meta.deckhouse.io/description: "Управление конфигурацией подсистемы безопасности."
  labels:
    heritage: deckhouse
    module: user-authz
    rbac.deckhouse.io/use-role: admin
    rbac.deckhouse.io/kind: role
    rbac.deckhouse.io/scope: subsystem
    rbac.deckhouse.io/subsystem: security
    rbac.deckhouse.io/aggregate-to-security-as: superadmin
    rbac.deckhouse.io/aggregate-to-system-as: manager
    # can-assign range of the identity-assign webhook: the security manager assigns basic access
    # levels up to ClusterAdmin and security subsystem roles up to the manager level, but no
    # superadmin role, cluster-admin or SuperAdmin.
    user-authz.deckhouse.io/can-assign-basic-max: ClusterAdmin
    user-authz.deckhouse.io/can-assign-scope: subsystem
    user-authz.deckhouse.io/can-assign-subsystem: security
    user-authz.deckhouse.io/can-assign-max-level: admin
aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.deckhouse.io/aggregate-to-security-as: manager
