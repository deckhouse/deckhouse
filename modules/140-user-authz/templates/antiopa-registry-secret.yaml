{{- if or (.Values.userAuthz.enableMultiTenancy) (not .Values.userAuthz.disableWebhookConfigurator) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Chart.Name }}-antiopa-registry
  namespace: kube-system
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
type: kubernetes.io/dockercfg
data:
  .dockercfg: {{ $.Values.global.modulesImages.registryDockercfg }}
{{- end }}
