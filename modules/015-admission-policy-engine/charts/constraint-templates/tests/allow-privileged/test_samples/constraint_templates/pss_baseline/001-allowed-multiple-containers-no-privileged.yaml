apiVersion: v1
kind: Pod
metadata:
  name: allowed-multiple-containers-no-privileged
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
    - name: redis
      image: redis
      securityContext:
        privileged: false

