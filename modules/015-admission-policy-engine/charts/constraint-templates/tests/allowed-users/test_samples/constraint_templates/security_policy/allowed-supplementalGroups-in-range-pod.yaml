apiVersion: v1
kind: Pod
metadata:
  name: allowed-supplementalGroups-in-range-pod
  namespace: testns
spec:
  securityContext:
    runAsUser: 5
    runAsGroup: 50
    fsGroup: 50
    supplementalGroups:
    - 50
    - 75
  containers:
    - name: nginx
      image: nginx

