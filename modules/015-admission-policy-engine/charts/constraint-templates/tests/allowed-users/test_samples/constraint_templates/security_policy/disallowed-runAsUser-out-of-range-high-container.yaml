apiVersion: v1
kind: Pod
metadata:
  name: disallowed-runAsUser-out-of-range-high-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        runAsUser: 11

