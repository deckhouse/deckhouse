apiVersion: apps/v1
kind: Deployment
metadata:
  name: allowed-deployment-runAsUser-in-range-container
  namespace: testns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      securityContext:
        runAsUser: 5
        runAsGroup: 50
        fsGroup: 50
        supplementalGroups:
        - 50
      containers:
        - name: nginx
          image: nginx

