apiVersion: v1
kind: Pod
metadata:
  name: allowed-runAsUser-on-edge-min-container
  namespace: testns
spec:
  securityContext:
    runAsUser: 1
    runAsGroup: 50
    fsGroup: 50
    supplementalGroups:
    - 50
  containers:
    - name: nginx
      image: nginx

