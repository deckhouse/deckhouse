apiVersion: v1
kind: Pod
metadata:
  name: allowed-all-fields-in-range-with-pod-level
  namespace: testns
spec:
  securityContext:
    runAsUser: 5
    runAsGroup: 50
    fsGroup: 50
    supplementalGroups:
    - 50
  containers:
    - name: nginx
      image: nginx

