apiVersion: v1
kind: Pod
metadata:
  name: allowed-runAsUser-non-zero-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        runAsUser: 1000

