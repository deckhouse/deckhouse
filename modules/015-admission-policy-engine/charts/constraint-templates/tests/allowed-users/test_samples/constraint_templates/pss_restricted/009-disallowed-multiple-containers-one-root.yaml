apiVersion: v1
kind: Pod
metadata:
  name: disallowed-multiple-containers-one-root
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        runAsUser: 1000
    - name: redis
      image: redis
      securityContext:
        runAsUser: 0

