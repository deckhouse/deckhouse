apiVersion: v1
kind: Pod
metadata:
  name: allowed-by-exception-user-mismatch-but-constraint-allows
  namespace: testns
  labels:
    security.deckhouse.io/securityPolicyException: allow-root-user
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        runAsUser: 1000

