apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-custom-user-range
spec:
  securityContext:
    runAsUser:
      allowedValues:
      - 1000
      - 2000

