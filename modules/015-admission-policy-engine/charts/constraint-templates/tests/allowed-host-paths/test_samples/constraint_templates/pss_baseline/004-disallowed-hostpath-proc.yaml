apiVersion: v1
kind: Pod
metadata:
  name: disallowed-hostpath-proc
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: proc-volume
          mountPath: /host-proc
  volumes:
    - name: proc-volume
      hostPath:
        path: /proc

