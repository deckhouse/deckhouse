apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-proc-etc
spec:
  volumes:
    hostPath:
      allowedValues:
      - path: /proc/etc
        readOnly: false

