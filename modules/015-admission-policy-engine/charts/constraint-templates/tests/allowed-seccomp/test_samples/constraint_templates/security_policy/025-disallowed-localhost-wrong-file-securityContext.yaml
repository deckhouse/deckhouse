apiVersion: v1
kind: Pod
metadata:
  name: disallowed-localhost-wrong-file-securityContext
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        seccompProfile:
          type: Localhost
          localhostProfile: wrong-file.json

