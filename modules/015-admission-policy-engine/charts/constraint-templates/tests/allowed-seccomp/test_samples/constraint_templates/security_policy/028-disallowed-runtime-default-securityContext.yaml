apiVersion: v1
kind: Pod
metadata:
  name: disallowed-runtime-default-securityContext
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        seccompProfile:
          type: RuntimeDefault

