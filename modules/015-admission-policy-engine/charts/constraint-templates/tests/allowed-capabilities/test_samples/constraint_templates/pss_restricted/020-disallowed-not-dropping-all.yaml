apiVersion: v1
kind: Pod
metadata:
  name: disallowed-not-dropping-all
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        capabilities:
          drop:
            - SYS_ADMIN
            - NET_ADMIN

