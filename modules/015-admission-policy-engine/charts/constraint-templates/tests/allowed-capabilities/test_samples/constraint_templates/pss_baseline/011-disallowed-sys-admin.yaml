apiVersion: v1
kind: Pod
metadata:
  name: disallowed-sys-admin
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        capabilities:
          add:
            - SYS_ADMIN

