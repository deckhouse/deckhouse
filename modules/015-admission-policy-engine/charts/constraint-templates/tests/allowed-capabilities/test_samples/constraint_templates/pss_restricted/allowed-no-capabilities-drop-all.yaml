apiVersion: v1
kind: Pod
metadata:
  name: allowed-no-capabilities-drop-all
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        capabilities:
          drop:
            - ALL

