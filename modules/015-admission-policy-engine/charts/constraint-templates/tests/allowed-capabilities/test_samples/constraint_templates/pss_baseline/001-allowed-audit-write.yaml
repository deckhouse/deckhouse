apiVersion: v1
kind: Pod
metadata:
  name: allowed-audit-write
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        capabilities:
          add:
            - AUDIT_WRITE

