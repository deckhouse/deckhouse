apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  name: allow-net-admin-drop-all
spec:
  securityContext:
    capabilities:
      allowedValues:
        add:
        - NET_ADMIN
        drop:
        - ALL

