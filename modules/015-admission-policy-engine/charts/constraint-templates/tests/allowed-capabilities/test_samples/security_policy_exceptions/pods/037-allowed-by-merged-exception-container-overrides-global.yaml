apiVersion: v1
kind: Pod
metadata:
  name: allowed-by-merged-exception-container-overrides-global
  namespace: testns
  labels:
    security.deckhouse.io/security-policy-exception: allow-net-admin-container
    security.deckhouse.io/security-policy-exception/nginx: allow-net-admin-container
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        capabilities:
          add:
            - NET_ADMIN
    - name: redis
      image: redis
      securityContext:
        capabilities:
          add:
            - NET_ADMIN