apiVersion: v1
kind: Pod
metadata:
  name: disallowed-by-exception-drop-mismatch
  namespace: testns
  labels:
    security.deckhouse.io/securityPolicyException: allow-net-admin-drop-all
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        capabilities:
          add:
            - NET_ADMIN
          drop:
            - SYS_ADMIN

