apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  name: allow-multiple-capabilities
spec:
  securityContext:
    capabilities:
      allowedValues:
        add:
        - NET_ADMIN
        - SYS_ADMIN
        - AUDIT_WRITE

