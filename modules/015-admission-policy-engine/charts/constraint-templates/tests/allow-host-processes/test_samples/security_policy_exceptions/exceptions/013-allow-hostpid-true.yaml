apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-hostpid-true
spec:
  network:
    hostPID:
      allowedValue: true
    hostIPC:
      allowedValue: false

