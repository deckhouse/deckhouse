apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: disallowed-daemonset-net-fake-sysctl
  namespace: testns
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      securityContext:
        sysctls:
        - name: net.fake.sysctl
          value: "1"
      containers:
        - name: nginx
          image: nginx

