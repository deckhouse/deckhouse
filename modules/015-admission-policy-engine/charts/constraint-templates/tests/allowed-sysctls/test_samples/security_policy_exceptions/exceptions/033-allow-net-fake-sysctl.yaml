apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns

  name: allow-net-fake-sysctl
spec:
  securityContext:
    sysctls:
      allowedValues:
      - net.fake.sysctl
