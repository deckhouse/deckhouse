apiVersion: deckhouse.io/v1alpha1
kind: ConstraintTestFields
metadata:
  name: priority-class
spec:
  objectKind: Pod
  objectKinds:
    - Pod
    - Deployment
  operations:
    - CREATE
    - UPDATE
  objectFields:
    - path: spec.priorityClassName
      level: pod
      defaultBehavior: Undefined
      description: Pod priorityClassName must be in allowed priorityClassNames list. When absent, the rule should NOT fire (old behaviour used undefined reference, not "" default).
      requiredScenarios: [positive, negative, absent]
  applicableTracks:
    functional: true
    spePod: false
    speContainer: false
