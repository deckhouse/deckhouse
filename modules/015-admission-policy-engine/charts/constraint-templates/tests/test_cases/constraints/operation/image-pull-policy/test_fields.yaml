apiVersion: deckhouse.io/v1alpha1
kind: ConstraintTestFields
metadata:
  name: image-pull-policy
spec:
  objectKind: Pod
  objectKinds:
    - Pod
    - Deployment
  operations:
    - CREATE
    - UPDATE
  objectFields:
    - path: spec.containers[].imagePullPolicy
      level: container
      defaultBehavior: undefined
      containerScope: containers
      description: Container imagePullPolicy must match required policy value. When absent, the rule should NOT fire (old behaviour used undefined reference, not "" default).
      requiredScenarios: [positive, negative, absent]
  applicableTracks:
    functional: true
    spePod: false
    speContainer: false
