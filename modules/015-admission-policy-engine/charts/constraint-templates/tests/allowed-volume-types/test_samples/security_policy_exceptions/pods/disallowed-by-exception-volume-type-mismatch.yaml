apiVersion: v1
kind: Pod
metadata:
  name: disallowed-by-exception-volume-type-mismatch
  namespace: testns
  labels:
    security.deckhouse.io/securityPolicyException: allow-hostPath-volume
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: nfs
          mountPath: /nfs
  volumes:
    - name: nfs
      nfs:
        server: nfs-server.example.com
        path: /exports

