apiVersion: v1
kind: Pod
metadata:
  name: disallowed-hostPath-volume
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: hostpath
          mountPath: /hostpath
  volumes:
    - name: hostpath
      hostPath:
        path: /var/lib

