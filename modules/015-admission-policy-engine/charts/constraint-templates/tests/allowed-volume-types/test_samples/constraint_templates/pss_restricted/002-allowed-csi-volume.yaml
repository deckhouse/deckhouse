apiVersion: v1
kind: Pod
metadata:
  name: allowed-csi-volume
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: csi-volume
          mountPath: /csi
  volumes:
    - name: csi-volume
      csi:
        driver: csi-driver.example.com

