apiVersion: v1
kind: Pod
metadata:
  name: allowed-projected-volume
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: projected
          mountPath: /etc/projected
  volumes:
    - name: projected
      projected:
        sources:
          - secret:
              name: my-secret

