apiVersion: v1
kind: Pod
metadata:
  name: allowed-secret-volume
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: secret
          mountPath: /etc/secret
  volumes:
    - name: secret
      secret:
        secretName: my-secret

