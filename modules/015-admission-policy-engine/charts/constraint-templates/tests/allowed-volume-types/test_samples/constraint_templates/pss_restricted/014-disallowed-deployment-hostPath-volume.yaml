apiVersion: apps/v1
kind: Deployment
metadata:
  name: disallowed-deployment-hostPath-volume
  namespace: testns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx
          volumeMounts:
            - name: hostpath
              mountPath: /hostpath
      volumes:
        - name: hostpath
          hostPath:
            path: /var/lib

