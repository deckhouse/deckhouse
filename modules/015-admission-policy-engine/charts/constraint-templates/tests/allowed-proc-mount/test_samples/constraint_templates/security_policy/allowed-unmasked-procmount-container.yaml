apiVersion: v1
kind: Pod
metadata:
  name: allowed-unmasked-procmount-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        procMount: Unmasked

