apiVersion: v1
kind: Pod
metadata:
  name: disallowed-multiple-containers-one-unmasked
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        procMount: Default
    - name: redis
      image: redis
      securityContext:
        procMount: Unmasked

