apiVersion: v1
kind: Pod
metadata:
  name: disallowed-unmasked-procmount-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        procMount: Unmasked

