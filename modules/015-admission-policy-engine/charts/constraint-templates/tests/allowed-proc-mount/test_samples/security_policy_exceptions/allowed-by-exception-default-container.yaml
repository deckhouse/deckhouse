apiVersion: v1
kind: Pod
metadata:
  name: allowed-by-exception-default-container
  namespace: testns
  labels:
    security.deckhouse.io/securityPolicyException: allow-default
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        procMount: Default

