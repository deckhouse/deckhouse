apiVersion: v1
kind: Pod
metadata:
  name: allowed-by-exception-unmasked-container
  namespace: testns
  labels:
    security.deckhouse.io/securityPolicyException: allow-unmasked
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        procMount: Unmasked

