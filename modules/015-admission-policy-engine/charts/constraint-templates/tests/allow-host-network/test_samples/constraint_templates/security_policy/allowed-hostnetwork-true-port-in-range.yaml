apiVersion: v1
kind: Pod
metadata:
  name: allowed-hostnetwork-true-port-in-range
  namespace: testns
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 9000
          hostPort: 25000

