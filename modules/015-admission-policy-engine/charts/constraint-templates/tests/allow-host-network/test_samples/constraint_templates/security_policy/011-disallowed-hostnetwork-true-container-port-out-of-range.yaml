apiVersion: v1
kind: Pod
metadata:
  name: disallowed-hostnetwork-true-container-port-out-of-range
  namespace: testns
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 10000

