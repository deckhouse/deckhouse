apiVersion: v1
kind: Pod
metadata:
  name: disallowed-hostnetwork-true-port-out-of-range-high
  namespace: testns
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 35000
          hostPort: 35000

