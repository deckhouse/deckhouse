apiVersion: v1
kind: Pod
metadata:
  name: disallowed-hostnetwork-true-port-out-of-range-low
  namespace: testns
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 10000
          hostPort: 10000

