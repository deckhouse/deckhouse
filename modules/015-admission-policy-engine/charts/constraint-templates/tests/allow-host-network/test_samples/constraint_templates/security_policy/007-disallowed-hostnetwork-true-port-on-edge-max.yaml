apiVersion: v1
kind: Pod
metadata:
  name: disallowed-hostnetwork-true-port-on-edge-max
  namespace: testns
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 30001
          hostPort: 30001

