apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-hostport-only-no-hostnetwork
spec:
  network:
    hostPorts:
      - port: 5000
        protocol: TCP
