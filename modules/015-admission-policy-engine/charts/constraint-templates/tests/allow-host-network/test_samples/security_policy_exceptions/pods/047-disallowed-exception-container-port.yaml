apiVersion: v1
kind: Pod
metadata:
  name: 046-disallowed-exception-container-port
  namespace: testns
  labels:
    security.deckhouse.io/security-policy-exception: allow-hostport-despite-empty-ranges  
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 5001
          protocol: TCP
