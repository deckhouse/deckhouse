apiVersion: v1
kind: Pod
metadata:
  name: disallowed-no-exception-label
  namespace: testns
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 5000
          hostPort: 5000
          protocol: TCP

