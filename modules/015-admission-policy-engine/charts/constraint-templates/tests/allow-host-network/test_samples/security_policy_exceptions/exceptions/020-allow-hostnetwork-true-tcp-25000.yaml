apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-hostnetwork-true-tcp-25000
spec:
  network:
    hostNetwork:
      allowedValue: true
    hostPorts:
    - port: 25000
      protocol: TCP
