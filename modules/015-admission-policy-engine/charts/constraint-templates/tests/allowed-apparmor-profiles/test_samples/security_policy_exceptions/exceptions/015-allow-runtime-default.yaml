apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-runtime-default
spec:
  securityContext:
    appArmorProfile:
      allowedValues:
      - runtime/default

