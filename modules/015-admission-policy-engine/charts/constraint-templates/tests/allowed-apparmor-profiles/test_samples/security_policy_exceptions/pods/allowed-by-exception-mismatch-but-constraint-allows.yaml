apiVersion: v1
kind: Pod
metadata:
  name: allowed-by-exception-mismatch-but-constraint-allows
  namespace: testns
  labels:
    security.deckhouse.io/securityPolicyException: allow-unconfined
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        appArmorProfile:
          type: RuntimeDefault

