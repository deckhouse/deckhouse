apiVersion: deckhouse.io/v1alpha1
kind: SecurityPolicyException
metadata:
  namespace: testns
  name: allow-spc-t
spec:
  securityContext:
    seLinuxOptions:
      allowedValues:
      - type: spc_t

