apiVersion: v1
kind: Pod
metadata:
  name: allowed-exact-match-securityContext-pod
  namespace: testns
spec:
  securityContext:
    seLinuxOptions:
      type: sometype
      level: somelevel
      role: somerole
      user: someuser
  containers:
    - name: nginx
      image: nginx

