apiVersion: v1
kind: Pod
metadata:
  name: disallowed-wrong-role-securityContext-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        seLinuxOptions:
          type: sometype
          level: somelevel
          role: wrongrole
          user: someuser

