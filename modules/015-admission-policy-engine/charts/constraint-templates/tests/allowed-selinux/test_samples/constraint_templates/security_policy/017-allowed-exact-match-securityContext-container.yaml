apiVersion: v1
kind: Pod
metadata:
  name: allowed-exact-match-securityContext-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        seLinuxOptions:
          type: sometype
          level: somelevel
          role: somerole
          user: someuser

