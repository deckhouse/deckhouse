apiVersion: v1
kind: Pod
metadata:
  name: disallowed-wrong-level-securityContext-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        seLinuxOptions:
          type: sometype
          level: wronglevel
          role: somerole
          user: someuser

