apiVersion: v1
kind: Pod
metadata:
  name: allowed-empty-type-securityContext-pod
  namespace: testns
spec:
  securityContext:
    seLinuxOptions:
      type: ""
  containers:
    - name: nginx
      image: nginx

