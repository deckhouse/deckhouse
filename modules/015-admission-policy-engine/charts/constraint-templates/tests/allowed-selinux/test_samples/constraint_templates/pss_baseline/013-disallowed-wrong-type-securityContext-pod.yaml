apiVersion: v1
kind: Pod
metadata:
  name: disallowed-wrong-type-securityContext-pod
  namespace: testns
spec:
  securityContext:
    seLinuxOptions:
      type: spc_t
  containers:
    - name: nginx
      image: nginx

