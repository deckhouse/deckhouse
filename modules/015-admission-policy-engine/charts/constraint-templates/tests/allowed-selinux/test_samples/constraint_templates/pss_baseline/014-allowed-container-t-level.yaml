apiVersion: v1
kind: Pod
metadata:
  name: 014-allowed-container-t-level
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        seLinuxOptions:
          type: container_t
          level: s0:c0,c1

