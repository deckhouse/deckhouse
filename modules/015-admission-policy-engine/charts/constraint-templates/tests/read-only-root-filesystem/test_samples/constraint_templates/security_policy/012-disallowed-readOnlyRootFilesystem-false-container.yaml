apiVersion: v1
kind: Pod
metadata:
  name: disallowed-readOnlyRootFilesystem-false-container
  namespace: testns
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        readOnlyRootFilesystem: false

