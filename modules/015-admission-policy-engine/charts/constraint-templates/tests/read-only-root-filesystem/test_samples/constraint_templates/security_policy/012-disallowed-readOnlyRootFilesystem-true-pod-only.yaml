apiVersion: v1
kind: Pod
metadata:
  name: disallowed-readOnlyRootFilesystem-true-pod-only
  namespace: testns
spec:
  securityContext:
    readOnlyRootFilesystem: true
  containers:
    - name: nginx
      image: nginx

