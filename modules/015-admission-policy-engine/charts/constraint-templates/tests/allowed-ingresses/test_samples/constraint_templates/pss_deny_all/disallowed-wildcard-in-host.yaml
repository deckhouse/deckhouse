apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: disallowed-wildcard-in-host
  namespace: testns
spec:
  rules:
  - host: "*.example.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test
            port:
              number: 80

