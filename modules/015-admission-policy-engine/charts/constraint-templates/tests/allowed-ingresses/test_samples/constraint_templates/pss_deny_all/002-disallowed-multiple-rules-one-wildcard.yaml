apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: disallowed-multiple-rules-one-wildcard
  namespace: testns
spec:
  rules:
  - host: example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test
            port:
              number: 80
  - host: "*.example.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test
            port:
              number: 80

