apiVersion: config.gatekeeper.sh/v1alpha1
kind: Config
metadata:
  name: config
  namespace: d8-{{.Chart.Name}}
spec:
  match:
    - excludedNamespaces: ["d8-*", "kube-system"]
      processes: ["*"]
