apiVersion: v1
kind: Pod
metadata:
  name: opa-disallowed
  namespace: testns
  labels:
    app: opa-disallowed
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - mountPath: /cache
      name: cache-volume
      readOnly: true
  volumes:
  - name: cache-volume
    hostPath:
      path: /foo
