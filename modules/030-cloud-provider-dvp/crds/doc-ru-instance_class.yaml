# This file is auto-generated by openapigen. Do not edit manually.
spec:
  group: deckhouse.io
  names:
    categories:
    - cloudinstanceclasses
    kind: DVPInstanceClass
    listKind: DVPInstanceClassList
    plural: dvpinstanceclasses
    singular: dvpinstanceclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              additionalDisks:
                description: |
                  Параметры дополнительных дисков виртуальной машины.

                  Каждый элемент массива описывает отдельный дополнительный диск.
                  Для каждого диска необходимо задать параметры `size` и `storageClass`.
                items:
                  properties:
                    size:
                      description: |
                        Размер дополнительного диска.
                      type: string
                    storageClass:
                      description: |
                        Имя существующего StorageClass, который будет использоваться для создания дополнительного диска.
                      type: string
                  type: object
                type: array
              etcdDisk:
                description: |
                  Параметры диска для etcd.
                properties:
                  size:
                    description: |
                      Размер дополнительного диска.
                    type: string
                  storageClass:
                    description: |
                      Имя существующего StorageClass, который будет использоваться для создания дополнительного диска.
                    type: string
                type: object
              rootDisk:
                description: |
                  Указывает настройки для корневого диска виртуальной машины.
                properties:
                  image:
                    description: |
                      Параметры образа, который будет использоваться для создания корневого диска виртуальной машины.
                    properties:
                      kind:
                        description: |
                          Тип источника изображения.
                        type: string
                      name:
                        description: |
                          Имя образа, который будет использоваться для создания корневого диска.
                          > Для установки требуются образы ОС Linux с предустановленным cloud-init.
                        type: string
                    type: object
                  size:
                    description: |
                      Размер корневого диска.
                    type: string
                  storageClass:
                    description: |
                      Имя существующего StorageClass будет использоваться для создания корневого диска виртуальной машины.

                      Если значение не указано, то будет использоваться StorageClass, согласно настройке [глобального параметра storageClass](/products/kubernetes-platform/documentation/v1/reference/api/global.html#parameters-modules-storageclass).
                    type: string
                type: object
              virtualMachine:
                description: |
                  Настройки виртуальной машины для созданного узла.

                  > Для виртуальных машин узлов используется политика запуска `runPolicy: AlwaysOnUnlessStoppedManually`.
                  > Это позволяет вручную останавливать ВМ (например, для обслуживания) без принудительного автозапуска.
                properties:
                  additionalAnnotations:
                    additionalProperties:
                      type: string
                    description: |
                      Дополнительные аннотации для ресурса виртуальной машины.
                    type: object
                  additionalLabels:
                    additionalProperties:
                      type: string
                    description: |
                      Дополнительные метки для ресурса виртуальной машины.
                    type: object
                  bootloader:
                    description: |
                      Определяет загрузчик для виртуальной машины.

                      * `BIOS` — используется BIOS;
                      * `EFI` — используется Unified Extensible Firmware (EFI/UEFI);
                      * `EFIWithSecureBoot` — используется UEFI/EFI c поддержкой Secure Boot.
                    type: string
                  cpu:
                    description: |
                      Настройки процессора для виртуальной машины.
                    properties:
                      coreFraction:
                        description: |
                          Процент гарантированной доли CPU, которая будет выделена виртуальной машине.
                        type: string
                      cores:
                        description: |
                          Количество ядер процессора для виртуальной машины.
                        type: integer
                    type: object
                  liveMigrationPolicy:
                    description: |
                      Политика живой миграции виртуальной машины.

                      * `Manual` — миграция управляется вручную;
                      * `Never` — миграция отключена;
                      * `AlwaysSafe` — всегда использовать безопасную миграцию (может не сработать при высокой скорости изменений памяти ВМ);
                      * `PreferSafe` — предпочитать безопасную миграцию, переключаться на forced при необходимости;
                      * `AlwaysForced` — всегда использовать forced-миграцию с замедлением ВМ;
                      * `PreferForced` — предпочитать forced-миграцию (рекомендуется для master-узлов из-за высокой активности памяти).
                    type: string
                  memory:
                    description: |
                      Определяет параметры памяти для виртуальной машины.
                    properties:
                      size:
                        description: |
                          Количество ресурсов памяти, разрешенных для виртуальной машины.
                        type: string
                    type: object
                  nodeSelector:
                    description: |
                      Позволяет назначить виртуальную машину на указанные узлы DVP.
                      [Аналогично](https://kubernetes.io/docs/tasks/configure-pod-container/assign-pods-nodes/) параметру `spec.nodeSelector` в Kubernetes Pods.
                    properties:
                      nodeSelectorTerms:
                        items:
                          properties:
                            matchExpressions:
                              items:
                                properties:
                                  key:
                                    type: string
                                  operator:
                                    type: string
                                  values:
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                            matchFields:
                              items:
                                properties:
                                  key:
                                    type: string
                                  operator:
                                    type: string
                                  values:
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                          type: object
                        type: array
                    type: object
                  priorityClassName:
                    description: |
                      [Аналогично](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/) параметру `spec.priorityClassName` в Kubernetes Pods.
                    type: string
                  runPolicy:
                    description: |
                      Политика запуска виртуальной машины.

                      * `AlwaysOn` — виртуальная машина всегда должна быть запущена;
                      * `AlwaysOff` — виртуальная машина всегда должна быть остановлена;
                      * `Manual` — состояние виртуальной машины управляется вручную;
                      * `AlwaysOnUnlessStoppedManually` — виртуальную машину можно остановить вручную (например, для обслуживания), но она автоматически запустится после перезагрузки хоста.
                    type: string
                  tolerations:
                    description: |
                      Позволяет задать tolerations для виртуальных машин на узле DVP.
                      [Аналогично](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/) параметру `spec.tolerations` в Kubernetes Pods.
                    items:
                      properties:
                        effect:
                          type: string
                        key:
                          type: string
                        operator:
                          type: string
                        tolerationSeconds:
                          format: int64
                          nullable: true
                          type: integer
                        value:
                          type: string
                      type: object
                    type: array
                  virtualMachineClassName:
                    description: |
                      Имя VirtualMachineClass.

                      Ресурс VirtualMachineClass предназначен для централизованной конфигурации предпочтительных параметров виртуальных машин. Он позволяет задавать инструкции CPU, политики конфигурации ресурсов CPU и памяти для виртуальных машин, а также устанавливать соотношения этих ресурсов.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
