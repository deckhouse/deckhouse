# This file is auto-generated by openapigen. Do not edit manually.
description: |
  Описывает конфигурацию облачного кластера в Deckhouse Virtualization Platform (DVP).

  Используется облачным провайдером, если управляющий слой (control plane) кластера размещён в облаке.

  Выполните следующую команду, чтобы изменить конфигурацию в работающем кластере:

  ```shell
  d8 k edit moduleconfig cloud-provider-dvp
  ```

  > Чтобы изменения вступили в силу, после изменения параметров узлов выполните команду `dhctl converge`.
properties:
  ccm:
    description: |
      Настройки подсистемы Cloud Controller Manager (CCM).

      CCM обеспечивает интеграцию кластера с облачным провайдером — например, управляет балансировщиками нагрузки.
      CCM можно включать и отключать независимо от других подсистем. Например, оставьте CCM включённым, если в кластере нужно управлять только балансировщиками нагрузки.
    properties:
      disabled:
        description: |
          Отключает Cloud Controller Manager.

          Установите в `true`, если CCM не требуется. Оставьте включённым (`false`), если нужно управление облачными балансировщиками нагрузки.
        type: boolean
    type: object
  nodes:
    description: |
      Настройки подсистемы управления узлами.

      Управляет узлами в кластере.
    properties:
      disabled:
        description: |
          Отключает подсистему управления узлами.
        type: boolean
      parameters:
        description: |
          Параметры подсистемы управления узлами.
        properties:
          ipAddresses:
            additionalProperties:
              items:
                type: string
              type: array
            description: |
              Статические IP-адреса, назначаемые сетевым интерфейсам виртуальных машин. Количество адресов должно совпадать с количеством создаваемых реплик — каждый IP-адрес будет назначен отдельной реплике.

              > Эти адреса должны принадлежать диапазону адресов, заданному в конфигурации модуля виртуализации в параметре `virtualMachineCIDRs`.
            type: object
          layout:
            description: |
              Название схемы размещения.

              [Подробнее о возможных схемах размещения провайдера](/modules/cloud-provider-dvp/layouts.html).
            type: string
          region:
            description: |
              Название региона.

              Чтобы использовать эту настройку, на узлах DVP должен быть установлен лейбл `topology.kubernetes.io/region`.
              [Подробнее о топологических лейблах](https://kubernetes.io/docs/reference/labels-annotations-taints/#topologykubernetesioregion)

              > Как установить требуемый лейбл для узла DVP, можно прочитать в [документации по NodeGroup](https://deckhouse.ru/documentation/v1/modules/040-node-manager/cr.html#nodegroup-v1-spec-nodetemplate-labels).
            type: string
          sshPublicKey:
            description: |
              Публичный ключ для доступа на узлы.
            type: string
          zones:
            description: |
              Набор зон, в которых могут быть созданы узлы.

              Чтобы использовать эту настройку, на узлах DVP должна быть установлен лейбл `topology.kubernetes.io/zone`.
              [Подробнее о топологических лейблах.](https://kubernetes.io/docs/reference/labels-annotations-taints/#topologykubernetesioregion)

              > Как установить требуемый лейбл для узла DVP, можно прочитать в [документации по NodeGroup](https://deckhouse.ru/documentation/v1/modules/040-node-manager/cr.html#nodegroup-v1-spec-nodetemplate-labels).
            items:
              type: string
            type: array
        type: object
    type: object
  provider:
    description: |
      Настройки подключения к родительской платформе Deckhouse Virtualization Platform (DVP).
    properties:
      parameters:
        description: |
          Содержит настройки для подключения к API Deckhouse Kubernetes Platform.
        properties:
          namespace:
            description: |
              Неймспейс, в котором будут созданы ресурсы кластера DKP.

              > Если не указано явно, будет использоваться неймспейс по умолчанию для kubeconfig.
            type: string
          networkPolicy:
            description: |
              Правила управления сетевым трафиком к нагрузкам и от нагрузок, запущенных в рамках ресурса Project.

              * `Isolated` — применяется ограничивающая allowlist-политика: разрешён только сетевой трафик, необходимый для работы платформенных компонентов; весь остальной трафик запрещён.
              * `None` — кластер не заказывает сетевые политики. Ограничения, заданные на уровне Project, продолжают действовать.
            type: string
        type: object
    type: object
  storage:
    description: |
      Настройки подсистемы хранения данных.

      Управляет возможностью заказа дисков в кластере.
    properties:
      disabled:
        description: |
          Отключает подсистему хранения данных.

          При значении `true` заказ дисков в кластере недоступен.
        type: boolean
      parameters:
        description: |
          Параметры подсистемы хранения данных.
        properties:
          excludedStorageClasses:
            description: |
              Список имён StorageClass (или регулярных выражений для имён), которые не нужно создавать в кластере.
            items:
              type: string
            type: array
        type: object
    type: object
type: object
