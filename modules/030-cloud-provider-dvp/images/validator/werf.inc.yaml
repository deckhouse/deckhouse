---
image: {{ $.ModuleName }}/{{ $.ImageName }}
from: common/distroless
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
  add: /dhctl-provider-dvp
  to: /dhctl-provider-dvp
  before: setup
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
final: false
fromImage: common/src-artifact
git:
- add: /{{ $.ModulePath }}modules/030-{{ $.ModuleName }}/images/{{ $.ImageName }}/src
  to: /src/validator/src
  excludePaths:
  - "**/*.md"
  - "**/*.yaml"
  stageDependencies:
    install:
    - go.mod
    - go.sum
    - "**/*.go"
- add: /{{ $.ModulePath }}modules/030-{{ $.ModuleName }}/pkg
  to: /src/validator/pkg
  excludePaths:
  - "**/*.md"
  - "**/*.yaml"
  stageDependencies:
    install:
    - go.mod
    - go.sum
    - "**/*.go"
- add: {{ .gitPrefix }}/go_lib/cloud-provider
  to: /src/go_lib/cloud-provider
  excludePaths:
  - "**/*.md"
  - "**/*.yaml"
  stageDependencies:
    install:
    - go.mod
    - go.sum
    - "**/*.go"
- add: {{ .gitPrefix }}/go_lib/dhctl-provider-protocol
  to: /src/go_lib/dhctl-provider-protocol
  excludePaths:
  - "**/*.md"
  stageDependencies:
    install:
    - go.mod
    - go.sum
    - "**/*.go"
shell:
  install:
  - cd /src/validator/pkg
  - sed -i -e 's# ../../../go_lib/cloud-provider# /src/go_lib/cloud-provider#g' go.mod
  - sed -i -e 's# ../../../go_lib/dhctl-provider-protocol# /src/go_lib/dhctl-provider-protocol#g' go.mod
  - cd /src/validator/src
  - sed -i -e 's# ../../../pkg# /src/validator/pkg#g' go.mod
  - sed -i -e 's# ../../../../../go_lib/cloud-provider# /src/go_lib/cloud-provider#g' go.mod
  - sed -i -e 's# ../../../../../go_lib/dhctl-provider-protocol# /src/go_lib/dhctl-provider-protocol#g' go.mod
  - cd /src/go_lib/cloud-provider
  - sed -i -e 's# ../dhctl-provider-protocol# /src/go_lib/dhctl-provider-protocol#g' go.mod
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
final: false
from: {{ index $.Images "builder/golang" }}
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src
  to: /src
  before: install
mount:
{{ include "mount points for golang builds" . }}
shell:
  install:
  - export GOPROXY={{ $.GOPROXY }}
  - cd /src/validator/src
  - go mod download
  - CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o /dhctl-provider-dvp .
  - chown 64535:64535 /dhctl-provider-dvp
  - chmod 0755 /dhctl-provider-dvp
