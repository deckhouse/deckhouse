x-extend:
  schema: config-values.yaml
type: object
properties:
  internal:
    type: object
    default: {}
    properties:
      activated:
        type: boolean
        default: false
        description: Внутренний флаг для деплой DaemonSet только при наличии настроенной log-pipeline.
