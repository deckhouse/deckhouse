x-extend:
  schema: config-values.yaml
type: object
properties:
  internal:
    type: object
    default: {}
    properties:
      activated:
        type: boolean
        default: false
        description: Internal switch for DaemonSet deploy only if correct log-pipeline created.
