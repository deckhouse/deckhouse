---
apiVersion: deckhouse.io/v1alpha1
kind: ConversionWebhook
metadata:
  name: clusterlogdestinations.deckhouse.io
  {{- include "helm_lib_module_labels" (list .) | nindent 2 }}
conversions:
  - from: v1alpha1
    to: v1alpha2
    handler:
      python: |
        def v1alpha1_to_v1alpha2(self, o: dict) -> typing.Tuple[str | None, dict]:
            import copy

            if o.get("apiVersion") != "deckhouse.io/v1alpha1":
                return None, o
            out = copy.deepcopy(o)
            out["apiVersion"] = "deckhouse.io/v1alpha2"
            spec = out["spec"]
            bo, bc = "{", "}"

            for t in spec.get("transformations") or []:
                if t.get("action") == "DropLabels":
                    dl = t["dropLabels"]
                    dl["labels"] = [{"label": p} for p in dl["labels"]]
                if t.get("action") == "ParseMessage":
                    pm = t["parseMessage"]
                    if pm.get("sourceFormat") == "String":
                        st = dict(pm["string"])
                        tf = st.pop("targetField")
                        st.clear()
                        st["regex"] = r"^(?P<%s>.*)$" % tf
                        st["setLabels"] = {tf: (bo * 2 + " %s " % tf + bc * 2)}
                        pm["string"] = st
            return None, out

  - from: v1alpha2
    to: v1alpha1
    handler:
      python: |
        def v1alpha2_to_v1alpha1(self, o: dict) -> typing.Tuple[str | None, dict]:
            import copy

            if o.get("apiVersion") != "deckhouse.io/v1alpha2":
                return None, o
            out = copy.deepcopy(o)
            out["apiVersion"] = "deckhouse.io/v1alpha1"
            spec = out["spec"]

            for t in spec.get("transformations") or []:
                if t.get("action") == "DropLabels":
                    dl = t["dropLabels"]
                    dl["labels"] = [e["label"] for e in dl["labels"]]
                if t.get("action") == "ParseMessage":
                    pm = t["parseMessage"]
                    pm.pop("targetLabel", None)
                    if pm.get("sourceFormat") == "String":
                        sl = (pm.get("string") or {}).get("setLabels") or {}
                        if sl:
                            pm["string"] = {"targetField": next(iter(sl))}
                        else:
                            pm["string"] = {"targetField": "msg"}
            return None, out
