x-extend:
  schema: config-values.yaml
type: object
properties:
  internal:
    type: object
    default: {}
    properties:
      serviceCert:
        type: object
        x-required-for-helm:
        - crt
        - key
        - ca
        properties:
          crt:
            type: string
          key:
            type: string
          ca:
            type: string
      customCertificateData:
        type: object
        properties:
          tls.crt:
            type: string
            x-examples:
              - plainstring
          tls.key:
            type: string
            x-examples:
              - plainstring
          ca.crt:
            type: string
            x-examples:
              - plainstring
      oidcCA:
        type: string
        description: |
          OIDC provider CA.
