diff --git a/pkg/framework/plugins/defaultevictor/defaultevictor.go b/pkg/framework/plugins/defaultevictor/defaultevictor.go
index 909473f2f..f505886ea 100644
--- a/pkg/framework/plugins/defaultevictor/defaultevictor.go
+++ b/pkg/framework/plugins/defaultevictor/defaultevictor.go
@@ -18,6 +18,7 @@ import (
 	"errors"
 	"fmt"
 	"slices"
+	"strings"

 	v1 "k8s.io/api/core/v1"
 	"k8s.io/apimachinery/pkg/runtime"
@@ -87,6 +88,13 @@ func New(ctx context.Context, args runtime.Object, handle frameworktypes.Handle)
 }

 func (d *DefaultEvictor) addAllConstraints(logger klog.Logger, handle frameworktypes.Handle) error {
+	// don't use pods from d8-* and kube-system namespaces
+	d.constraints = append(d.constraints, func(pod *v1.Pod) error {
+		if strings.HasPrefix(pod.Namespace, "d8-") || pod.Namespace == "kube-system" {
+			return fmt.Errorf("pod in the deckhouse namespace(%s)", pod.Namespace)
+		}
+		return nil
+	})
 	args := d.args
 	// Determine effective protected policies based on the provided arguments.
 	effectivePodProtections := getEffectivePodProtections(args)
