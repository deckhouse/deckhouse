apiVersion: deckhouse.io/v1alpha2
kind: Descheduler
metadata:
  name: e2e-sts-pdb-allows-one
spec:
  podLabelSelector:
    matchLabels:
      e2e-test: sts-pdb-allows-one
  strategies:
    removeDuplicates:
      enabled: true
