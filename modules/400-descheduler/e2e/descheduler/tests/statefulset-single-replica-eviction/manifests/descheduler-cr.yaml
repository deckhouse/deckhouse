apiVersion: deckhouse.io/v1alpha2
kind: Descheduler
metadata:
  name: e2e-sts-single-replica
spec:
  podLabelSelector:
    matchLabels:
      e2e-test: sts-single-replica
  strategies:
    removePodsViolatingInterPodAntiAffinity:
      enabled: true
