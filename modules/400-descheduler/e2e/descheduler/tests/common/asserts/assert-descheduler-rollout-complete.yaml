apiVersion: apps/v1
kind: Deployment
metadata:
  name: descheduler
  namespace: d8-descheduler
status:
  replicas: 1
  updatedReplicas: 1
  readyReplicas: 1
(status.observedGeneration == metadata.generation): true
(length(x_k8s_list($client, 'v1', 'Pod', 'd8-descheduler').items[?metadata.labels.app == 'descheduler' && metadata.deletionTimestamp == `null`]) == `1`): true
(spec.template.metadata.annotations."checksum/config" == (x_k8s_list($client, 'v1', 'Pod', 'd8-descheduler').items[?metadata.labels.app == 'descheduler' && metadata.deletionTimestamp == `null`] | [0].metadata.annotations."checksum/config")): true
