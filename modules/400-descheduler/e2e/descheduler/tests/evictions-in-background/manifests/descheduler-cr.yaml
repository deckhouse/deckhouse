apiVersion: deckhouse.io/v1alpha2
kind: Descheduler
metadata:
  name: e2e-evictions-in-background
spec:
  podLabelSelector:
    matchLabels:
      e2e-test: evictions-in-background
  strategies:
    removeDuplicates:
      enabled: true
