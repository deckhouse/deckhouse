apiVersion: deckhouse.io/v1alpha2
kind: Descheduler
metadata:
  name: e2e-sts-remove-duplicates
spec:
  podLabelSelector:
    matchLabels:
      e2e-test: sts-remove-duplicates
  strategies:
    removeDuplicates:
      enabled: true
