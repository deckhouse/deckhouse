apiVersion: deckhouse.io/v1alpha2
kind: Descheduler
metadata:
  name: e2e-minreplicas-probe
spec:
  podLabelSelector:
    matchLabels:
      e2e-test: minreplicas-probe
  strategies:
    removeDuplicates:
      enabled: true
