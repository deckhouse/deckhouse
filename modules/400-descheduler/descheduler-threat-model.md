# Модель угроз и поверхности атаки модуля `400-descheduler`

---

## 1. Определение целей и критичных функций модуля

| Параметр | Описание |
| -------- | -------- |
| **Наименование модуля** | `400-descheduler`, Deckhouse-модуль `descheduler` (namespace `d8-descheduler`, `stage: General Availability`, `Chart.yaml: version 0.0.1`). |
| **Назначение** | Периодический анализ состояния кластера и вытеснение (eviction) подов согласно активным стратегиям, чтобы kube-scheduler заново распределил рабочие нагрузки оптимальнее. Модуль основан на upstream-проекте `kubernetes-sigs/descheduler` версии `0.35.1` (`oss.yaml`) с локальным патчем Deckhouse. |
| **Режим эксплуатации** | Кластерный/облачный. Один экземпляр `Deployment` (`replicas: 1`) внутри кластера Kubernetes. Запуск цикла вытеснения по интервалу: `Frequent`=5м, `Moderate`=15м (по умолчанию), `Rare`=30м (`openapi/config-values.yaml`, аргумент `--descheduling-interval`). Модуль не обслуживает входящий пользовательский сетевой трафик. |
| **Среда исполнения** | DKP/Kubernetes, namespace `d8-descheduler`. Контейнерный образ `descheduler` на базе `base/distroless`, статически слинкованный бинарь (`CGO_ENABLED=0`, `GOOS=linux`), запуск от пользователя `deckhouse` (не root). Боковой контейнер `kube-rbac-proxy`. Источники данных и исполнения действий — Kubernetes API и (опционально) Metrics API (`metrics.k8s.io`). |
| **Основные функции** | Чтение состояния кластера (Pods, Nodes, Namespaces, PriorityClasses, PDB, PVC, при наличии — метрики); применение политики `DeschedulerPolicy`, сформированной из ресурсов `Descheduler` (CRD); вытеснение подов через subresource `pods/eviction`; генерация событий (`events.k8s.io`); экспорт Prometheus-метрик через `kube-rbac-proxy`; автоопределение `metrics.k8s.io` и переключение источника утилизации; конвертация CR `v1alpha1`↔`v1alpha2`. |
| **Критичные функции** | (1) Принятие решения о вытеснении и само вытеснение/удаление подов в масштабах всего кластера; (2) корректность и неизменность политики `DeschedulerPolicy`; (3) защита привилегированной учётной записи (ServiceAccount `descheduler` с правом cluster-wide `pods` `delete` и `pods/eviction` `create`); (4) соблюдение защитных ограничений (`DefaultEvictor`: PDB, `system-critical` приоритет, DaemonSet, локальное хранилище; патч Deckhouse: namespaces `d8-*` и `kube-system`); (5) целостность цепочки поставки образа. |
| **Критичные последствия** | Массовое вытеснение/удаление рабочих нагрузок → отказ в обслуживании приложений и нарушение работоспособности кластера; вытеснение в обход PDB при использовании verb `pods/delete`; искажение политики вытеснения; раскрытие топологии кластера (имена подов/namespace/узлов) через метрики и подробные логи; компрометация привилегированного SA-токена; внедрение скомпрометированного кода через цепочку поставки (выполнение в контексте SA с правом массового вытеснения). |
| **Объекты защиты** | Ресурсы `Descheduler` (`deckhouse.io/v1alpha1`, `v1alpha2`, cluster-scoped) и `ModuleConfig/descheduler`; `ConfigMap d8-descheduler/descheduler-policy` (сгенерированная политика); токен ServiceAccount `descheduler` и его `ClusterRole`; `Secret d8-descheduler/deckhouse-registry` (dockerconfigjson); данные состояния кластера, читаемые модулем; Prometheus-метрики и журналы; контейнерный образ и цепочка его сборки. |
| **Категории субъектов** | См. подраздел [1.1. Категории субъектов](#11-категории-субъектов) ниже. |
| **Реализованные меры защиты, выявленные по исходным данным** | См. подраздел [1.2. Реализованные меры защиты](#12-реализованные-меры-защиты-выявленные-по-исходным-данным) ниже. |
| **Неопределённости** | **Требует уточнения:** полный SBOM релизного образа (в исходных данных есть `go.mod`/`go.sum`, `oss.yaml`, VEX, но не машиночитаемый SBOM); расхождение версий зависимостей между VEX и `go.mod` (см. раздел 2); фактическое использование verb `pods` `delete` бинарём (RBAC выдаёт `delete` сверх `eviction`); наличие `NetworkPolicy` для `d8-descheduler`; конфигурация аудита Kubernetes API; точные значения `seccompProfile`/capabilities/`readOnlyRootFilesystem`, формируемые helm-хелпером; назначение зависимости `kubevirt.io/*` в `go.mod`. |

### 1.1. Категории субъектов

- **(0) Внешний неаутентифицированный субъект сети кластера** — достижим только endpoint `:10258/healthz` (исключён из авторизации), влияние минимально.
- **(0′) Внешний поставщик цепочки поставки** (`SOURCE_REPO`, `GOPROXY`, upstream-репозиторий, registry) — **высокий потенциал**.
- **(1) Пользователь Kubernetes с правами в собственном namespace** — **низкий внутренний потенциал** (влияет на метки/поведение только своих подов).
- **(2) Субъект с доступом к метрикам/логам** (`d8-monitoring`, Prometheus) — **средний внутренний потенциал**.
- **(3) Администратор кластера / infrastructure-manager** — право создавать/изменять `Descheduler` и `ModuleConfig` — **высокий внутренний потенциал**.
- **(4) Привилегированный внутренний нарушитель** с доступом к control-plane / namespace `d8-descheduler` / SA-токену / возможности регистрировать `APIService` — **высокий внутренний потенциал**.

Доверенные/ограниченно доверенные компоненты: Kubernetes API, addon-operator (hooks), `kube-rbac-proxy`, metrics-server.

### 1.2. Реализованные меры защиты, выявленные по исходным данным

- Бинарь не root (`runAsUser` `deckhouse`), контекст безопасности по профилю PSS Restricted (`helm_lib_module_container_security_context_pss_restricted_flexible`).
- Образ `distroless`, статический бинарь без CGO.
- `--bind-address 127.0.0.1` (secure-serving только на loopback).
- Метрики только через `kube-rbac-proxy` с авторизацией `SubjectAccessReview` на subresource `deployments/prometheus-metrics`, `/healthz` исключён.
- ServiceAccount с `automountServiceAccountToken: false` (включается только на уровне пода).
- Локальный патч Deckhouse запрещает вытеснение подов из `d8-*` и `kube-system`.
- В политике жёстко зафиксированы `evictSystemCriticalPods: false`, `nodeFit: true` (в v1alpha2 нет параметра для включения вытеснения system-critical подов).
- Порог приоритета по умолчанию — `system-cluster-critical`.
- `DefaultEvictor` соблюдает PDB.
- `PodDisruptionBudget` и `VerticalPodAutoscaler` для самого модуля.
- Запись на CRD `Descheduler` разрешена только уровню ClusterAdmin/infrastructure-manager (`user-authz`, `rbacv2/manage`).
- Наличие VEX (`known_vulnerabilities.vex`) для 5 CVE.

---

## 2. Архитектурная модель модуля

### 2.1. Компоненты модуля и границы доверия

| Компонент | Тип | Назначение | Уровень доверия | Граница доверия |
| --------- | --- | ---------- | --------------- | --------------- |
| **Helm/Deckhouse-шаблоны модуля** (`templates/*`) | Внутренний компонент | Формирование Namespace, ServiceAccount, RBAC, Deployment, ConfigMap политики, PodMonitor, PDB, VPA, ConversionWebhook, registry-secret | Доверенный субъект при контроле релизного артефакта | Да — между релизным артефактом и Kubernetes API |
| **Go-хуки модуля** (`hooks/*`: `discover_metrics_api`, `get_crds`, `migrate/remove_old_descheduler_cm`) | Внутренний компонент (исполняется в addon-operator/Deckhouse, вне `d8-descheduler`) | Наблюдение за `APIService` (`metrics.k8s.io`) и CR `Descheduler`; формирование `descheduler.internal.*`; разовая миграция (удаление legacy CM `d8-system/descheduler-config-migration`) | Доверенный субъект | Да — между Kubernetes API и значениями Helm |
| **ConversionWebhook `deschedulers.deckhouse.io`** (`templates/conversion-webhook.yaml`, Python) | Внутренний компонент (исполняется в контуре Deckhouse) | Конвертация CR `v1alpha1`↔`v1alpha2`, разбор строковых label-селекторов | Ограниченно доверенный субъект (обрабатывает содержимое CR) | Да — между Kubernetes API admission/conversion и обработчиком |
| **`descheduler` container** (upstream `0.35.1` + патч) | Внутренний компонент | Чтение состояния кластера, применение политики, вытеснение/удаление подов, secure-serving (healthz/metrics) на `127.0.0.1:10258` | Ограниченно доверенный субъект (обрабатывает данные состояния кластера, влияемые многими субъектами) | Да — между Pod и Kubernetes API |
| **`kube-rbac-proxy` sidecar** | Внутренний компонент | Терминация доступа к метрикам на `<podIP>:10258`, авторизация scrape через `SubjectAccessReview`; проксирование на loopback-upstream | Доверенный субъект | Да — между Prometheus и secure-serving descheduler |
| **`ConfigMap descheduler-policy`** | Внутренний объект данных | Хранение сгенерированной `policy.yaml`, монтируется в `/policy` | Доверенный объект защиты (целостность критична) | Нет внутри namespace; Да при доступе на запись |
| **ServiceAccount `descheduler` + ClusterRole `d8:descheduler:descheduler`** | Внутренний объект/идентичность | Привилегированный сервисный доступ к API (в т.ч. cluster-wide `pods delete`, `pods/eviction create`) | Доверенная привилегированная идентичность | Да — концентрация привилегий, ключевая граница |
| **CRD `deschedulers.deckhouse.io`** (cluster-scoped; `v1alpha1` deprecated/served, `v1alpha2` served/stored) | Внутренний интерфейс конфигурации | Декларативное описание стратегий вытеснения | Ограниченно доверенный вход (запись — только ClusterAdmin) | Да — между субъектом-администратором и модулем |
| **`Secret deckhouse-registry`** | Внутренний объект данных | `dockerconfigjson` для pull образов | Доверенный объект защиты | Да при доступе к секрету |
| **Kubernetes API server** | Внешняя система | Хранилище CR/ConfigMap/состояния; точка применения eviction; admission/RBAC/audit | Ограниченно доверенная внешняя система | Да |
| **metrics-server / `metrics.k8s.io`** | Внешняя система | Источник фактической утилизации для `LowNodeUtilization`/`HighNodeUtilization` | Ограниченно доверенная внешняя система | Да |
| **Prometheus / `d8-monitoring`** | Внешняя система мониторинга | Сбор метрик через `kube-rbac-proxy` (PodMonitor, `prometheus-token`) | Ограниченно доверенная система | Да |
| **Обрабатываемые рабочие нагрузки кластера** (Pods/контроллеры в пользовательских namespace) | Внешние данные/объекты воздействия | Цели анализа и вытеснения; их метки/namespace/число рестартов влияют на отбор | Недоверенные/ограниченно доверенные в зависимости от RBAC namespace | Да |
| **`SOURCE_REPO` / `GOPROXY` / upstream `kubernetes-sigs/descheduler` / Container Registry** | Внешние/сборочные системы | Источник исходного кода, Go-модулей, базового образа и публикации образа | Ограниченно доверенная система поставки | Да |

### 2.2. Основные интерфейсы и потоки данных

| Источник | Получатель | Протокол/формат | Назначение | Доверенность данных |
| -------- | ---------- | --------------- | ---------- | ------------------- |
| Администратор кластера | Kubernetes API | CR `Descheduler` (YAML/JSON), `ModuleConfig` | Конфигурирование стратегий и интервала | Ограниченно доверенные (запись — только ClusterAdmin), целостность критична |
| Kubernetes API → хук `get_crds` | Helm values `descheduler.internal.deschedulers` | Kubernetes watch/list | Формирование политики | Ограниченно доверенные |
| Kubernetes API → хук `discover_metrics_api` | `descheduler.internal.isMetricsServerEnabled` | Kubernetes watch `APIService` | Включение `metricsProviders` | Ограниченно доверенные (зависят от регистрации `APIService`) |
| Helm → `ConfigMap descheduler-policy` → `/policy/policy.yaml` | `descheduler` container | ConfigMap volume mount | Передача политики бинарю | Доверенные (сформированы модулем) |
| `descheduler` | Kubernetes API | client-go (HTTPS), watch/list/get | Чтение Pods/Nodes/Namespaces/PriorityClasses/PDB/PVC | Ограниченно доверенные (содержимое влияемо многими субъектами) |
| `descheduler` | Kubernetes API | `POST pods/eviction`, `DELETE pods`, `create events` | Вытеснение/удаление подов, события | Действие модуля; последствия критичны |
| `descheduler` | metrics-server (`metrics.k8s.io`) | client-go (HTTPS), get/list | Получение фактической утилизации | Ограниченно доверенные данные |
| Prometheus | `kube-rbac-proxy` (`<podIP>:10258`) | HTTPS scrape + `prometheus-token`, `SubjectAccessReview`, `insecureSkipVerify: true` | Сбор метрик | Ограниченно доверенные |
| `kube-rbac-proxy` | `descheduler` secure-serving (`127.0.0.1:10258`) | HTTPS loopback (`upstreamInsecureSkipVerify: true`) | Проксирование метрик/healthz | Доверенные (loopback внутри пода) |
| kubelet | `<podIP>:10258/healthz` | HTTPS GET (исключён из авторизации) | Liveness-проба | Доверенное служебное обращение |
| `descheduler` | stdout (log-shipper) | JSON-логи, уровень `--v 6` | Диагностика, фиксация решений | Производные данные (могут содержать топологию) |
| Build/update flow (`werf.inc.yaml`) | Registry / Kubernetes | git clone `SOURCE_REPO`, `go build`, OCI-образ | Сборка и поставка модуля | Ограниченно доверенные данные поставки |

### 2.3. Используемые сторонние компоненты и зависимости (с фиксацией версий)

| Компонент | Версия / источник | Назначение | Замечания безопасности |
| --------- | ----------------- | ---------- | ---------------------- |
| `kubernetes-sigs/descheduler` | `0.35.1` (`oss.yaml`; `werf.inc.yaml`: `get_oss_version_by_id "descheduler"`, clone `SOURCE_REPO/kubernetes-sigs/descheduler.git`, checkout `v0.35.1`) | Основной бинарь | Применяется локальный патч `001-filter-pods-in-deckhouse-namespaces.patch`; `go mod vendor` + `go build` с `GOPROXY`. |
| Go toolchain | `go 1.25.0` / `toolchain go1.25.1`, `godebug default=go1.25` (`go.mod`) | Сборка | Современная версия; сборка `CGO_ENABLED=0`. |
| `k8s.io/{api,apimachinery,apiserver,client-go,component-base,component-helpers,metrics,kms}` | `v0.35.1` (`go.mod`) | Доступ к Kubernetes API, eviction, metrics | Соответствует целевой версии кластера. |
| `kube-rbac-proxy` | Из общего образа Deckhouse (`helm_lib_module_common_image ... kubeRbacProxy`) | Авторизация доступа к метрикам | Точная версия в исходных данных модуля не зафиксирована — **требует уточнения**. |
| `go.opentelemetry.io/otel`, `otel/sdk`, exporters | `v1.41.0` (`go.mod`) | Трассировка (опционально) | VEX ссылается на `otel/sdk@v1.36.0` (см. ниже) — расхождение версий. |
| `google.golang.org/grpc` | `v1.79.1` (`go.mod`) | Транспорт к API/OTLP | VEX: `CVE-2026-33186` — `not_affected` (descheduler — только gRPC-клиент). |
| `golang.org/x/crypto` | `v0.48.0` (`go.mod`) | Криптопримитивы | VEX ссылается на `x/crypto@v0.38.0` (`CVE-2025-47914`, `CVE-2025-58181`) — расхождение версий. |
| `golang.org/x/net` | `v0.50.0` (`go.mod`) | Сетевой стек | — |
| `prometheus/client_golang`, `prometheus/common` | `v1.23.2`, `v0.67.5` (`go.mod`) | Метрики | — |
| `spf13/cobra`, `spf13/pflag` | `v1.10.2`, `v1.0.10` (`go.mod`) | CLI/флаги | — |
| `kubevirt.io/api`, `kubevirt.io/client-go` | `v1.3.0` (`go.mod`, прямые зависимости; в `go.sum` присутствуют) | Зависимости upstream-сборки | Назначение в бинаре `cmd/descheduler` и попадание в путь исполнения — **требует уточнения** (влияет на точность SBOM/VEX). |
| `base/distroless`, `builder/golang-alpine` | Общие образы Deckhouse (`werf.inc.yaml`) | Базовый/сборочный образ | Минимальная поверхность runtime; точные версии — вне исходных данных модуля. |
| VEX (`known_vulnerabilities.vex`) | OpenVEX `v0.2.0`, version 7 | Обработка известных CVE | 5 statements, все `not_affected` (`vulnerable_code_not_in_execute_path`): `CVE-2026-24051`, `CVE-2025-47914`, `CVE-2025-58181`, `CVE-2026-33186`, `CVE-2026-39883`. **Расхождение:** продукты в VEX указаны для версий, отличных от `go.mod` (`otel/sdk` 1.36.0 vs 1.41.0; `x/crypto` 0.38.0 vs 0.48.0) — VEX может быть неактуален для текущего `go.mod`. |

**Сводка архитектуры (текстовая DFD):**
Администратор → (CR `Descheduler`/`ModuleConfig`) → Kubernetes API → хук `get_crds`/`discover_metrics_api` → Helm values → `ConfigMap descheduler-policy` → (`/policy`) → контейнер `descheduler` → (client-go) → Kubernetes API → **eviction/delete подов** в пользовательских namespace. Параллельно: Prometheus → `kube-rbac-proxy` → loopback secure-serving. Цепочка поставки: `SOURCE_REPO`+патч+`GOPROXY` → `werf` build → registry → Deployment.

---

## 3. Анализ поверхности атаки модуля

> Поверхность атаки не учитывает аппаратные уязвимости и root-пользователя ОС. Доверенными считаются только данные, созданные самим модулем, и hardcoded-данные. **Особенность модуля:** он не обслуживает недоверенный входящий сетевой трафик (нет `Service`/`Ingress`; secure-serving — на loopback; метрики — за `kube-rbac-proxy`). Доминирующая поверхность атаки — привилегированная RBAC-идентичность, конфигурационные входы и цепочка поставки, а не классический разбор сетевого ввода.

| Элемент | Компонент | Версия | Функция безопасности | Тип интерфейса | Уровень доступа | Характер взаимодействия | Недоверенные данные |
| ------- | --------- | ------ | -------------------- | -------------- | --------------- | ----------------------- | ------------------- |
| **CRD `Descheduler` (запись)** | Kubernetes API + хук `get_crds` | CRD `v1alpha1`/`v1alpha2`; descheduler `0.35.1` | Ограничение записи через `user-authz`/`rbacv2` (ClusterAdmin/manager) | Программный API (декларативный) | Привилегированный (ClusterAdmin) | Внутренний через Kubernetes API | Стратегии, пороги, селекторы (`podLabelSelector`, `namespaceLabelSelector`, `nodeLabelSelector`), `priorityClassThreshold`, `evictLocalStoragePods` |
| **`ModuleConfig/descheduler`** | Deckhouse | config-values v2 | RBAC модуля | Конфигурационный | Привилегированный | Внутренний | `deschedulingInterval` (enum) |
| **`ConfigMap d8-descheduler/descheduler-policy`** | Helm/descheduler | — | Реконсиляция из CR (перезапись addon-operator) | Файловое/конфигурационное (mount `/policy`) | Привилегированный (запись в namespace `d8-descheduler`) | Внутренний | Содержимое `policy.yaml` при наличии прав записи |
| **`<podIP>:10258` metrics (`https-metrics`)** | `kube-rbac-proxy` | образ Deckhouse (**требует уточнения**) | Авторизация `SubjectAccessReview` на `deployments/prometheus-metrics`; TLS | Программный HTTPS | Ограниченный (RBAC), достижим в сети кластера | Внутренний | Scrape-запросы; метрики содержат `node` и др. метки |
| **`<podIP>:10258/healthz`** | `kube-rbac-proxy` → descheduler | descheduler `0.35.1` | Исключён из авторизации (`excludePaths: /healthz`) | Программный HTTPS | Публичный в пределах сети кластера | Внешний (сеть кластера) / служебный | Запросы проверки живости; раскрытие факта существования/состояния |
| **`127.0.0.1:10258` secure-serving** | `descheduler` | descheduler `0.35.1`; `k8s.io/apiserver 0.35.1` | `--bind-address 127.0.0.1` (только loopback) | Программный HTTPS | Локальный (внутри пода) | Внутренний | Доступно только из пространства имён сети пода |
| **ServiceAccount `descheduler` / ClusterRole `d8:descheduler:descheduler`** | RBAC | — | Разграничение прав сервисного доступа | Kubernetes API (идентичность) | Привилегированный сервисный | Внутренний | `pods` get/watch/list/**delete**; `pods/eviction` create; nodes/namespaces/pvc/priorityclasses/pdb (get/watch/list); events; leases; `metrics.k8s.io` nodes/pods |
| **Данные состояния кластера (Pods/Nodes/Namespaces/PDB/PriorityClass)** | Kubernetes API | — | `DefaultEvictor`-ограничения; патч `d8-*`/`kube-system` | Программный API (вход решений) | Чтение cluster-wide | Внутренний/внешний (контент влияем пользователями namespace) | Метки подов/namespace, число рестартов, owner refs, affinity, topology constraints |
| **Данные `metrics.k8s.io`** | metrics-server | API `metrics.k8s.io` | — | Программный API | Чтение | Внешний (зависит от регистрации `APIService`) | Значения CPU/Memory утилизации (влияют на отбор для `LowNodeUtilization`/`HighNodeUtilization`) |
| **`APIService` `*.metrics.k8s.io` (watch)** | хук `discover_metrics_api` | — | Фильтр по `spec.group` | Программный API | Чтение (cluster-wide) | Внешний | Наличие группы `metrics.k8s.io` (флаг включает `metricsProviders` и перезапуск пода) |
| **ConversionWebhook (Python)** | Deckhouse conversion | — | Изоляция в контуре Deckhouse | Программный (admission/conversion) | Привилегированный (вход — только ClusterAdmin) | Внутренний | Строковые label-селекторы и поля CR при конвертации |
| **`Secret deckhouse-registry`** | Helm | — | RBAC namespace | Объект-секрет | Привилегированный | Внутренний | `dockerconfigjson` (доступ к registry) |
| **Журналы `--v 6` (JSON, stdout)** | `descheduler` | — | — | Диагностический поток | Ограниченный (доступ к логам) | Внутренний | Имена подов/namespace/узлов, решения о вытеснении |
| **Build/update flow** | `werf.inc.yaml`, патчи | descheduler `0.35.1`, Go `1.25.1` | Закрепление версии (`oss.yaml`), секреты сборки | Supply chain | Привилегированный сборочный | Внешний/сборочный | `SOURCE_REPO`, `GOPROXY`, исходный код upstream, патч, Go-модули, базовый образ |
| **Локальный патч `001-...patch`** | Сборка образа | — | Hardening (запрет `d8-*`/`kube-system`) | Supply chain / логика | Привилегированный сборочный | Внутренний | Изменение логики `DefaultEvictor.addAllConstraints` |

### 3.1. Требует уточнения (поверхность атаки)

| Наблюдение | Значение для дальнейшего анализа |
| ---------- | ------------------------------- |
| ClusterRole выдаёт `pods` `delete` дополнительно к `pods/eviction` `create`. | Прямое `DELETE` пода игнорирует PDB и graceful-логику eviction. Требует уточнения, использует ли бинарь `0.35.1` `delete` или только `eviction`; влияет на оценку DoS/целостности. |
| В `templates/configmap.yaml` значение `nodeSelector: {{ $d.nodeLabelSelector }}` подставляется в YAML без кавычек. | Безопасность опирается на CRD-валидацию и pruning (поле `nodeSelector`-строка отсутствует в схеме `v1alpha2`, форматирование через `metav1.FormatLabelSelector`). Требует уточнения устойчивость к краевым значениям селекторов. |
| Нет `Service`/`Ingress`/`NetworkPolicy` в шаблонах модуля. | Достижимость `<podIP>:10258/healthz` и `/metrics` определяется сетевой политикой платформы (вне модуля) — требует уточнения. |
| Расхождение версий VEX ↔ `go.mod`. | Часть `not_affected`-обоснований может относиться к устаревшим версиям зависимостей; нужен пересмотр VEX/SBOM для текущего `go.mod`. |
| Зависимости `kubevirt.io/*` присутствуют в `go.mod`/`go.sum`. | Требует уточнения попадание в путь исполнения `cmd/descheduler`; влияет на корректность SBOM/анализ CVE. |
| Точные `seccompProfile`/capabilities/`readOnlyRootFilesystem` формируются helm-хелпером PSS Restricted. | Требует уточнения фактический манифест для подтверждения hardening. |

---

## 4. Идентификация угроз (STRIDE × БДУ ФСТЭК)

> Для каждого компонента/элемента поверхности атаки определены классы угроз STRIDE и сопоставлены угрозы из БДУ (`УБИ.1`–`УБИ.11`). Потенциал нарушителя — согласно `abbr.md`.

| Компонент | Элемент поверхности атаки | STRIDE | БДУ | Название угрозы | Источник угрозы | Потенциал | К/Ц/Д |
| --------- | ------------------------- | ------ | --- | --------------- | --------------- | --------- | ----- |
| **ServiceAccount/ClusterRole descheduler** | Cluster-wide `pods/eviction` create | Denial of Service | УБИ.6 | Угроза отказа в обслуживании | Внутренний нарушитель (4) | Высокий | Д |
| **ServiceAccount/ClusterRole descheduler** | Cluster-wide `pods` delete | Tampering / Impact | УБИ.5 | Угроза удаления информационных ресурсов | Внутренний нарушитель (4) | Высокий | Ц, Д |
| **ServiceAccount/ClusterRole descheduler** | Концентрация привилегий, SA-токен | Elevation of Privilege | УБИ.2 | Угроза несанкционированного доступа | Внутренний нарушитель (4) | Высокий | К, Ц, Д |
| **descheduler container** | Логика вытеснения (нарушение баланса/чёрн подов) | Denial of Service | УБИ.8 | Угроза нарушения функционирования (работоспособности) | Внутренний нарушитель (3/4) | Средний/Высокий | Д |
| **CRD `Descheduler` (запись)** | Стратегии, пороги, селекторы | Tampering | УБИ.3 | Угроза несанкционированной модификации (искажения) | Администратор (3) | Высокий | Ц |
| **CRD `Descheduler` (запись)** | Агрессивные пороги/широкие селекторы | Denial of Service | УБИ.6 | Угроза отказа в обслуживании | Администратор (3) | Высокий | Д |
| **ConfigMap `descheduler-policy`** | Запись в namespace `d8-descheduler` | Tampering | УБИ.3 | Угроза несанкционированной модификации (искажения) | Внутренний нарушитель (4) | Высокий | Ц, Д |
| **metrics.k8s.io / APIService** | Подмена/регистрация `APIService`, ложные метрики | Spoofing | УБИ.4 | Угроза несанкционированной подмены | Внутренний нарушитель (4) | Высокий | Ц |
| **metrics.k8s.io / APIService** | Влияние на решения утилизации | Tampering | УБИ.3 | Угроза несанкционированной модификации (искажения) | Внутренний нарушитель (4) | Средний/Высокий | Ц, Д |
| **metrics endpoint (kube-rbac-proxy)** | Scrape метрик с метками топологии | Information Disclosure | УБИ.11 | Угроза несанкционированного массового сбора информации | Внутренний нарушитель (2) | Средний | К |
| **Журналы `--v 6`** | Имена подов/namespace/узлов | Information Disclosure | УБИ.1 | Угроза утечки информации | Внутренний нарушитель (2) | Низкий/Средний | К |
| **`Secret deckhouse-registry` / SA-токен** | Чтение секрета/токена | Information Disclosure | УБИ.1 | Угроза утечки информации | Внутренний нарушитель (4) | Средний/Высокий | К |
| **Данные состояния кластера** | Метки/рестарты собственных подов | Tampering (косвенно) | УБИ.3 | Угроза несанкционированной модификации (искажения) | Пользователь namespace (1) | Низкий | Ц, Д |
| **ConversionWebhook (Python)** | Разбор строковых селекторов CR | Denial of Service / Tampering | УБИ.8 | Угроза нарушения функционирования (работоспособности) | Администратор (3) | Низкий/Средний | Ц, Д |
| **Действия модуля (eviction/events)** | Отсутствие/недостаточность аудита источника изменений CR | Repudiation | УБИ.3 | Угроза несанкционированной модификации (искажения) | Внутренний нарушитель (3/4) | Средний | Ц |
| **Build/update flow** | `SOURCE_REPO`/`GOPROXY`/upstream/registry | Tampering | УБИ.9 | Угроза получения информационных ресурсов из недоверенного или скомпрометированного источника | Внешний поставщик / внутренний нарушитель сборки (0′/4) | Высокий | К, Ц, Д |
| **Локальный патч / зависимости** | Подмена патча/Go-модуля/базового образа | Spoofing / Tampering | УБИ.4 | Угроза несанкционированной подмены | Внешний поставщик / внутренний (0′/4) | Высокий | Ц |
| **descheduler идентичность** | Использование ресурсов кластера не по назначению (цикл вытеснения как инструмент давления) | Misuse | УБИ.7 | Угроза ненадлежащего (нецелевого) использования | Внутренний нарушитель (3/4) | Средний | Д |
| **Модуль в целом** | Распространение противоправной информации | — | УБИ.10 | Угроза распространения противоправной информации | — | — | — (рассмотрена, неприменима: модуль не генерирует/не проксирует контент вовне) |

### 4.1. Покрытие STRIDE

| STRIDE | Покрытые элементы | Вывод |
| ------ | ----------------- | ----- |
| **Spoofing** | Подмена/регистрация `APIService metrics.k8s.io`; подмена образа/патча/зависимости в цепочке поставки | Риск связан с подменой источника данных утилизации и компонентов сборки. |
| **Tampering** | CR `Descheduler`, `ConfigMap descheduler-policy`, данные метрик, локальный патч/зависимости, косвенно — метки собственных подов | Риск изменения политики вытеснения и логики/артефактов сборки. |
| **Repudiation** | Отсутствие/неполнота аудита авторства изменений CR и чтения секретов; события/логи | Требует уточнения полнота аудита Kubernetes API и доступа к `Descheduler`/Secret. |
| **Information Disclosure** | Метрики (метки топологии), подробные логи `--v 6`, SA-токен, registry-secret | Риск раскрытия внутренней структуры кластера и привилегированных секретов; доступ ограничен RBAC/`kube-rbac-proxy`. |
| **Denial of Service** | Массовое вытеснение/удаление подов, агрессивные стратегии, перезапуск пода при смене `metricsProviders`, обработка CR в conversion | Риск нарушения доступности приложений и баланса планирования. |
| **Elevation of Privilege** | Концентрация привилегий ClusterRole (cluster-wide pods delete/eviction), компрометация SA-токена/цепочки поставки | Риск превращения доступа к поду/токену/сборке в воздействие на любые рабочие нагрузки кластера. |

---

## 5. Моделирование сценариев атак

### Сценарий AS-01. Массовое вытеснение/удаление подов при компрометации идентичности descheduler

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-01 |
| **Связанная угроза** | УБИ.6, УБИ.8, УБИ.5, УБИ.2 |
| **Элемент поверхности атаки** | ServiceAccount `descheduler` / ClusterRole `d8:descheduler:descheduler` (`pods` delete, `pods/eviction` create cluster-wide) |
| **Источник угрозы** | Внутренний нарушитель (4): компрометация пода `d8-descheduler`, кража SA-токена, ошибочное связывание ClusterRole |
| **Начальный уровень доступа** | Доступ к SA-токену descheduler или возможность исполнять код в его контексте |
| **Вектор атаки** | Прямые вызовы `POST .../pods/eviction` и/или `DELETE pods` по всем namespace |
| **Используемая уязвимость** | Концентрация привилегий: одна сервисная идентичность имеет cluster-wide право вытеснения/удаления подов; verb `delete` шире, чем eviction (обход PDB) |
| **Краткая последовательность действий** | 1. Нарушитель получает токен/исполнение в контексте SA. 2. Перечисляет поды cluster-wide. 3. Вызывает eviction/delete для рабочих нагрузок. 4. Приложения теряют доступность; при `delete` — в обход PDB. |
| **Последствия** | Кластерный отказ в обслуживании; смягчение: патч защищает `d8-*`/`kube-system`, `DefaultEvictor` соблюдает PDB при eviction, но не при прямом `delete`. |

### Сценарий AS-02. Деструктивное конфигурирование стратегий через CR `Descheduler`

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-02 |
| **Связанная угроза** | УБИ.3, УБИ.6, УБИ.7 |
| **Элемент поверхности атаки** | CRD `Descheduler` (cluster-scoped), стратегии/пороги/селекторы |
| **Источник угрозы** | Администратор (3) или компрометация учётной записи с правом записи `deschedulers` |
| **Начальный уровень доступа** | Право `create/update deschedulers` (`user-authz: ClusterAdmin` или `rbacv2 manage edit`) |
| **Вектор атаки** | Создание CR с агрессивными `thresholds`/`targetThresholds`, широкими `namespaceLabelSelector`/`podLabelSelector`, `evictLocalStoragePods: true`, частым `deschedulingInterval: Frequent` |
| **Используемая уязвимость** | Стратегии по построению вытесняют поды; при широких селекторах и низких порогах возможен постоянный «чёрн» подов; `evictSystemCriticalPods` зафиксирован `false` (ограничивает ущерб) |
| **Краткая последовательность действий** | 1. Нарушитель с правами администратора создаёт/меняет CR. 2. Хук формирует политику, под перезапускается. 3. Каждые 5 минут вытесняются целевые рабочие нагрузки. 4. Постоянные перезапуски снижают доступность. |
| **Последствия** | Нарушение доступности/работоспособности целевых приложений; нецелевое использование механизма. Смягчение: запись ограничена высоким потенциалом (ClusterAdmin). |

### Сценарий AS-03. Подмена/искажение политики через `ConfigMap descheduler-policy`

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-03 |
| **Связанная угроза** | УБИ.3, УБИ.6 |
| **Элемент поверхности атаки** | `ConfigMap d8-descheduler/descheduler-policy`, монтируемый в `/policy` |
| **Источник угрозы** | Внутренний нарушитель (4) с правом записи ConfigMap в `d8-descheduler` |
| **Начальный уровень доступа** | RBAC на запись ConfigMap в namespace `d8-descheduler` |
| **Вектор атаки** | Изменение `policy.yaml` (включение/ослабление эвиктора, изменение селекторов/порогов) |
| **Используемая уязвимость** | Бинарь читает политику из ConfigMap; при наличии прав записи возможна подмена до реконсиляции addon-operator |
| **Краткая последовательность действий** | 1. Нарушитель получает права записи в `d8-descheduler`. 2. Меняет `policy.yaml`. 3. До перегенерации хуком политика действует. 4. Возможна аномальная политика вытеснения. |
| **Последствия** | Кратковременное искажение поведения вытеснения. Смягчение: политика декларативно реконсилируется из CR; запись в системный namespace требует высоких привилегий. |

### Сценарий AS-04. Влияние на решения вытеснения через подмену данных `metrics.k8s.io`

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-04 |
| **Связанная угроза** | УБИ.4, УБИ.3, УБИ.8 |
| **Элемент поверхности атаки** | Источник `metrics.k8s.io`; watch `APIService` хуком `discover_metrics_api` |
| **Источник угрозы** | Внутренний нарушитель (4) с правом регистрировать `APIService` или скомпрометированный metrics-server |
| **Начальный уровень доступа** | Возможность зарегистрировать/подменить `APIService` группы `metrics.k8s.io` или влиять на ответы metrics-server |
| **Вектор атаки** | Регистрация ложного `APIService` (включает `metricsProviders` и перезапуск пода) и/или выдача искажённых значений утилизации |
| **Используемая уязвимость** | Хук определяет провайдер по факту наличия группы `metrics.k8s.io` (`spec.group`), без проверки доверенности реализации; стратегии `LowNodeUtilization`/`HighNodeUtilization` доверяют данным утилизации |
| **Краткая последовательность действий** | 1. Нарушитель регистрирует/компрометирует источник `metrics.k8s.io`. 2. Модуль включает `KubernetesMetrics` и перезапускается. 3. Искажённые метрики смещают классификацию узлов. 4. Вытеснение/уплотнение выполняется по ложным данным. |
| **Последствия** | Некорректные решения о вытеснении (нарушение целостности логики, возможный DoS/неоптимальное размещение). Требует высоких привилегий регистрации `APIService`. |

### Сценарий AS-05. Раскрытие топологии кластера через метрики и подробные логи

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-05 |
| **Связанная угроза** | УБИ.1, УБИ.11 |
| **Элемент поверхности атаки** | Prometheus-метрики (через `kube-rbac-proxy`), JSON-логи уровня `--v 6` |
| **Источник угрозы** | Внутренний нарушитель (2) с доступом к Prometheus/метрикам или к логам пода |
| **Начальный уровень доступа** | Доступ к `d8-monitoring`/Prometheus или к системе сбора логов |
| **Вектор атаки** | Сбор метрик и чтение логов с именами подов/namespace/узлов и решениями о вытеснении |
| **Используемая уязвимость** | Подробное логирование (`--v 6`) и метрики раскрывают внутреннюю структуру; доступ ограничен RBAC/`kube-rbac-proxy`, часть меток отбрасывается relabeling |
| **Краткая последовательность действий** | 1. Нарушитель получает доступ к метрикам/логам. 2. Извлекает карту namespace/узлов/нагрузок. 3. Использует для подготовки последующих атак. |
| **Последствия** | Раскрытие топологии и активности кластера; повышение эффективности дальнейших атак. |

### Сценарий AS-06. Компрометация цепочки поставки образа descheduler

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-06 |
| **Связанная угроза** | УБИ.9, УБИ.4, УБИ.3, УБИ.2 |
| **Элемент поверхности атаки** | `SOURCE_REPO`, upstream `kubernetes-sigs/descheduler`, локальный патч, `GOPROXY`/Go-модули, базовый образ, registry |
| **Источник угрозы** | Внешний поставщик / скомпрометированный источник (0′), внутренний нарушитель сборочной среды (4) |
| **Начальный уровень доступа** | Доступ к источнику исходного кода/зависимостей/патчу/registry или сборочным секретам |
| **Вектор атаки** | Подмена исходного кода/тега, Go-модуля, базового образа или внедрение в патч |
| **Используемая уязвимость** | Большая цепочка зависимостей (`go.mod`/`go.sum`); полный SBOM релизного образа в исходных данных отсутствует; VEX рассинхронизирован с `go.mod`; версия задаётся через `get_oss_version_by_id` |
| **Краткая последовательность действий** | 1. Нарушитель внедряет изменение в зависимость/патч/образ. 2. `werf` собирает и публикует образ. 3. Deckhouse разворачивает Deployment. 4. Код исполняется в контексте SA с правом cluster-wide вытеснения. |
| **Последствия** | Полная компрометация функции вытеснения, доступ к токену/секретам, кластерный DoS. |

### Сценарий AS-07. Прямое удаление подов в обход PDB (verb `pods/delete`)

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-07 |
| **Связанная угроза** | УБИ.5, УБИ.6 |
| **Элемент поверхности атаки** | ClusterRole verb `pods` `delete` (сверх `pods/eviction`) |
| **Источник угрозы** | Внутренний нарушитель (4) с идентичностью descheduler |
| **Начальный уровень доступа** | Контекст SA descheduler |
| **Вектор атаки** | `DELETE` подов напрямую, минуя eviction API и проверки PDB |
| **Используемая уязвимость** | RBAC предоставляет `delete`; прямое удаление не уважает PDB и graceful-гарантии |
| **Краткая последовательность действий** | 1. Нарушитель в контексте SA. 2. Удаляет критичные поды без учёта PDB. 3. Нарушается доступность даже защищённых PDB нагрузок. |
| **Последствия** | Удаление рабочих нагрузок в обход бюджета прерываний. **Требует уточнения**, использует ли штатный бинарь `delete`; при отсутствии использования угроза остаётся как избыточность прав. |

### Сценарий AS-08. Влияние low-priv пользователя на отбор собственных подов

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-08 |
| **Связанная угроза** | УБИ.3 |
| **Элемент поверхности атаки** | Метки/число рестартов/affinity собственных подов как вход для селекторов/стратегий |
| **Источник угрозы** | Пользователь namespace (1), низкий внутренний потенциал |
| **Начальный уровень доступа** | Право управлять собственными рабочими нагрузками в namespace |
| **Вектор атаки** | Настройка меток/поведения подов, чтобы попасть под (или избежать) критерии вытеснения |
| **Используемая уязвимость** | Решения частично зависят от влияемых пользователем атрибутов подов; воздействие ограничено собственными подами и защитами `DefaultEvictor` |
| **Краткая последовательность действий** | 1. Пользователь задаёт метки/провоцирует рестарты своих подов. 2. Влияет на их попадание в выборку. |
| **Последствия** | Ограниченное влияние на вытеснение собственных нагрузок; cross-namespace воздействие отсутствует. |

### Сценарий AS-09. Нарушение работы/ошибки ConversionWebhook при обработке CR

| Параметр | Значение |
| -------- | -------- |
| **ID сценария** | AS-09 |
| **Связанная угроза** | УБИ.8, УБИ.3 |
| **Элемент поверхности атаки** | Python-обработчик конвертации `v1alpha1`↔`v1alpha2`, разбор строковых label-селекторов |
| **Источник угрозы** | Администратор (3) (только ClusterAdmin может создавать CR) |
| **Начальный уровень доступа** | Право создавать CR `Descheduler` |
| **Вектор атаки** | Подача `v1alpha1` CR с краевыми/некорректными строковыми селекторами или структурой |
| **Используемая уязвимость** | Ручной разбор строк селекторов; ошибки конвертации могут приводить к некорректной/пустой политике (отфильтровывается `HasValidStrategies`) |
| **Краткая последовательность действий** | 1. Администратор подаёт краевой CR. 2. Конвертация даёт неожиданный результат/ошибку. 3. Возможен пропуск/искажение политики. |
| **Последствия** | Локальное нарушение работоспособности конвертации/политики. Низкий приоритет; вход — только высокопривилегированный субъект. |

---

## 6. Оценка актуальности и формирование модели угроз

### 6.1. Оценка актуальности

| ID угрозы (БДУ) | ID сценария | Наличие уязвимости | Реализуемость | Потенциальный ущерб | Итоговая категория | Решение | Обоснование |
| --------------- | ----------- | ------------------ | ------------- | ------------------- | ------------------ | ------- | ----------- |
| УБИ.6, УБИ.8, УБИ.5, УБИ.2 | AS-01 | Да | Средняя | Критический | Критический | **Актуальна** | Идентичность имеет cluster-wide право вытеснения/удаления подов; компрометация токена/пода ведёт к кластерному DoS. Смягчено патчем `d8-*`/`kube-system` и PDB (для eviction). |
| УБИ.3, УБИ.6, УБИ.7 | AS-02 | Да | Средняя | Высокий | Высокий | **Актуальна** | Вытеснение — штатная функция; агрессивная конфигурация ведёт к «чёрну» подов. Смягчено ограничением записи до ClusterAdmin и hardcoded `evictSystemCriticalPods: false`. |
| УБИ.3, УБИ.6 | AS-03 | Да | Низкая/Средняя | Средний/Высокий | Средний | **Условно актуальна** | Требует прав записи в `d8-descheduler`; политика реконсилируется из CR (быстрое восстановление). |
| УБИ.4, УБИ.3, УБИ.8 | AS-04 | Да | Низкая/Средняя | Средний/Высокий | Средний | **Условно актуальна** | Требует прав регистрации `APIService`/компрометации metrics-server; включение провайдера по факту наличия группы без проверки доверенности. |
| УБИ.1, УБИ.11 | AS-05 | Да | Средняя | Средний | Средний | **Условно актуальна** | Метрики/логи реально раскрывают топологию; доступ ограничен RBAC/`kube-rbac-proxy`; `--v 6` повышает детальность логов. |
| УБИ.9, УБИ.4, УБИ.3, УБИ.2 | AS-06 | Да | Средняя | Критический | Критический | **Актуальна** | Большая цепочка зависимостей; полный SBOM отсутствует, VEX рассинхронизирован с `go.mod`; исполнение в привилегированном контексте. |
| УБИ.5, УБИ.6 | AS-07 | Требует уточнения | Низкая/Средняя | Высокий | Высокий | **Условно актуальна** | RBAC даёт `pods delete` (обход PDB). Требует уточнения фактическое использование verb; как минимум — избыточность прав. |
| УБИ.3 | AS-08 | Да | Низкая | Низкий | Низкий | **Условно актуальна** | Воздействие ограничено собственными подами пользователя; защиты `DefaultEvictor` снижают эффект. |
| УБИ.8, УБИ.3 | AS-09 | Да | Низкая | Низкий | Низкий | **Условно актуальна** | Вход — только ClusterAdmin; ошибки конвертации отфильтровываются `HasValidStrategies`. |
| УБИ.1 | AS-01/AS-06 (секреты) | Да | Низкая/Средняя | Высокий | Высокий | **Актуальна** | SA-токен и `deckhouse-registry` ценны; их раскрытие усиливает AS-01/AS-06; требует доступа к поду/секрету. |
| УБИ.7 | AS-02 | Да | Низкая | Средний | Средний | **Условно актуальна** | Нецелевое использование механизма вытеснения как инструмента давления на нагрузки; требует прав администратора. |
| УБИ.10 | — | Нет | — | — | — | **Неактуальна** | Модуль не генерирует, не рассылает и не проксирует контент во внешние системы. |

### 6.2. Итоговая модель актуальных угроз

| ID | Угроза | Актуальность | Основные компоненты | Приоритет нейтрализации |
| -- | ------ | ------------ | ------------------- | ----------------------- |
| TM-01 | Массовое вытеснение/удаление подов (кластерный DoS) при компрометации идентичности descheduler | Актуальна | SA/ClusterRole `descheduler`, Kubernetes API | Критический |
| TM-02 | Деструктивное/некорректное конфигурирование стратегий через CR `Descheduler`/`ModuleConfig` | Актуальна | CRD `Descheduler`, хук `get_crds`, ConfigMap политики | Высокий |
| TM-03 | Подмена/искажение политики через `ConfigMap descheduler-policy` | Условно актуальна | ConfigMap `descheduler-policy`, namespace `d8-descheduler` | Средний |
| TM-04 | Влияние на решения вытеснения через подмену `metrics.k8s.io`/`APIService` | Условно актуальна | хук `discover_metrics_api`, metrics-server, стратегии утилизации | Средний |
| TM-05 | Раскрытие топологии кластера через метрики и подробные логи (`--v 6`) | Условно актуальна | `kube-rbac-proxy`, Prometheus, журналы descheduler | Средний |
| TM-06 | Компрометация цепочки поставки образа descheduler | Актуальна | `werf.inc.yaml`, `SOURCE_REPO`/`GOPROXY`, патч, зависимости, registry | Критический |
| TM-07 | Прямое удаление подов в обход PDB (verb `pods/delete`) | Условно актуальна | ClusterRole `d8:descheduler:descheduler` | Высокий |
| TM-08 | Влияние low-priv пользователя на отбор собственных подов | Условно актуальна | Данные состояния (метки/рестарты подов), `DefaultEvictor` | Низкий |
| TM-09 | Нарушение/ошибки ConversionWebhook при обработке CR | Условно актуальна | ConversionWebhook (Python), CRD `v1alpha1/v1alpha2` | Низкий |
| TM-10 | Раскрытие/компрометация секретов (SA-токен, `deckhouse-registry`) | Актуальна | SA `descheduler`, `Secret deckhouse-registry` | Высокий |
| TM-11 | Нецелевое использование механизма вытеснения (давление на нагрузки) | Условно актуальна | descheduler, CRD `Descheduler` | Средний |

### 6.3. Меры по нейтрализации

| Угроза | Приоритет | Рекомендуемые меры |
| ------ | --------- | ------------------ |
| TM-01 | Критический | Минимизировать число субъектов, способных получить SA-токен; ограничить exec/доступ к подам `d8-descheduler` через `NetworkPolicy`/RBAC; контролировать связывания ClusterRole; мониторить аномальный рост вытеснений/удалений; сохранить и проверять защиты (патч `d8-*`/`kube-system`, PDB, `evictSystemCriticalPods: false`). |
| TM-02 | Высокий | Подтвердить, что запись `deschedulers`/`ModuleConfig` ограничена ClusterAdmin/infrastructure-manager; ревью допустимых стратегий/порогов/селекторов; мониторинг создания/изменения CR; ограничить `deschedulingInterval: Frequent` без обоснования. |
| TM-03 | Средний | Ограничить запись ConfigMap в `d8-descheduler` до системных компонентов; полагаться на реконсиляцию из CR; алертинг на ручные изменения `descheduler-policy`. |
| TM-04 | Средний | Контролировать право регистрации `APIService`; доверять только штатному metrics-server; рассмотреть верификацию источника `metrics.k8s.io`; учитывать, что данные утилизации — лишь дополнительный источник к requests/limits. |
| TM-05 | Средний | Ограничить RBAC на метрики и доступ к логам; пересмотреть уровень логирования (`--v 6`) для прод-контуров; продолжить отбрасывание высоко-кардинальных меток (relabeling); задать сроки хранения логов. |
| TM-06 | Критический | Сформировать и поддерживать полный SBOM релизного образа; синхронизировать VEX с фактическими версиями `go.mod`; проверять подписи/хэши зависимостей и базового образа; защищать `SOURCE_REPO`/`GOPROXY`/registry/секреты сборки; проводить SCA/SAST патча и зависимостей. |
| TM-07 | Высокий | Уточнить необходимость verb `pods` `delete`; при отсутствии использования — удалить из ClusterRole, оставив только `pods/eviction`; тестировать соблюдение PDB. |
| TM-08 | Низкий | Документировать ограниченность воздействия; полагаться на защиты `DefaultEvictor` и namespace-границы RBAC. |
| TM-09 | Низкий | Покрыть конвертацию тестами на краевые селекторы; контролировать, что некорректные CR не приводят к опасной политике (`HasValidStrategies`). |
| TM-10 | Высокий | Включить аудит чтения Secret/SA-токена; ротация при подозрении на компрометацию; ограничить монтирование токена (`automountServiceAccountToken` на уровне SA уже `false`). |
| TM-11 | Средний | Организационный контроль изменения политик вытеснения; мониторинг частоты/объёма eviction; алертинг на аномалии. |

### 6.4. Компоненты, подлежащие тестированию

| Компонент | Виды тестирования | Связанные угрозы | Цель тестирования |
| --------- | ----------------- | ---------------- | ----------------- |
| ClusterRole/RBAC `d8:descheduler:descheduler` | Ревью RBAC, регрессионные тесты прав | TM-01, TM-07, TM-10 | Принцип наименьших привилегий; необходимость verb `pods delete`; корректность binding |
| `descheduler` бинарь (`0.35.1` + патч) | Регрессионные/модульные тесты, ревью патча | TM-01, TM-07 | Соблюдение PDB/`DefaultEvictor`, корректность фильтра `d8-*`/`kube-system` |
| Шаблон политики (`templates/configmap.yaml`) | Тесты шаблонов (`template_tests`), фаззинг значений CR | TM-02, TM-03 | Безопасная генерация `policy.yaml` (в т.ч. безкавычный `nodeSelector`), отсутствие YAML-инъекций |
| Хук `discover_metrics_api` | Модульные тесты | TM-04 | Корректность определения провайдера и поведения при подмене `APIService` |
| Хук `get_crds` / типы `v1alpha2` | Модульные тесты | TM-02 | Корректная трансляция CR в значения, `HasValidStrategies` |
| ConversionWebhook (Python) | Модульные/краевые тесты | TM-09 | Устойчивость разбора селекторов, отсутствие опасной/пустой политики |
| `kube-rbac-proxy` + PodMonitor | Регрессионные тесты доступа | TM-05 | Авторизация scrape (`SubjectAccessReview`), исключение `/healthz`, релейблинг |
| Build/update flow (`werf.inc.yaml`, патч, зависимости) | SCA/SAST, supply-chain проверки | TM-06 | Целостность зависимостей/патча/образа, SBOM/VEX-консистентность |
| Манифесты безопасности пода | Конфигурационное ревью | TM-01, TM-10 | Подтверждение PSS Restricted, non-root, loopback-bind, минимальных прав |

### 6.5. Требует уточнения

| Вопрос | Влияние на модель |
| ------ | ----------------- |
| Использует ли бинарь `0.35.1` verb `pods` `delete` (помимо eviction)? | Определяет перевод TM-07 в актуальную/неактуальную и масштаб обхода PDB. |
| Наличие и состав `NetworkPolicy` для `d8-descheduler`. | Влияет на достижимость `<podIP>:10258` и egress; на реализуемость TM-05 и части TM-01. |
| Полный SBOM релизного образа и синхронизация VEX с `go.mod`. | Необходимо для точной оценки TM-06 и известных CVE (расхождение `otel/sdk`, `x/crypto`). |
| Назначение и путь исполнения зависимостей `kubevirt.io/*`. | Корректность SBOM и анализа CVE для образа. |
| Конфигурация аудита Kubernetes API (изменения CR, чтение Secret/SA). | Оценка Repudiation (TM-02/TM-10) и остаточного риска. |
| Точные `seccompProfile`/capabilities/`readOnlyRootFilesystem` из helm-хелпера. | Подтверждение hardening (TM-01). |
| Версия образа `kube-rbac-proxy`. | Оценка известных уязвимостей компонента безопасности (TM-05). |
| Доверенность источника `metrics.k8s.io` и защита от регистрации ложного `APIService`. | Реализуемость TM-04. |

### 6.6. План проверки безопасности

| Направление | Проверки |
| ----------- | -------- |
| Configuration / RBAC review | Аудит ClusterRole (необходимость `pods delete`), binding'ов, `user-authz`/`rbacv2` прав записи CR; проверка PSS-контекста, loopback-bind, `automountServiceAccountToken`; наличие `NetworkPolicy`. |
| Code review | Ревью патча `001-...patch` (фильтр namespace), шаблона `configmap.yaml` (генерация политики), хуков `get_crds`/`discover_metrics_api`, Python-конвертера. |
| Регрессионные/модульные тесты | Соблюдение PDB/`DefaultEvictor`, защита `d8-*`/`kube-system`, корректная политика для всех стратегий, поведение при включении/отключении metrics-server. |
| Fuzzing | Краевые значения CR (селекторы, пороги, `priorityClassThreshold`), вход конвертера `v1alpha1`→`v1alpha2`. |
| DAST (ограниченно) | Проверка авторизации метрик через `kube-rbac-proxy`, недоступность secure-serving вне loopback, поведение `/healthz`. |
| Supply-chain review | SBOM/VEX-консистентность с `go.mod`/`go.sum`, закрепление версии (`oss.yaml`/`get_oss_version_by_id`), защита `SOURCE_REPO`/`GOPROXY`/registry, сканирование образа. |

---

## ПРИЛОЖЕНИЕ 2. Дополнения к модели угроз

### Контроль полноты модели угроз

- **Покрытие компонентов:** в разделах 2–4 учтены все компоненты модуля — Helm-шаблоны, Go-хуки, ConversionWebhook, контейнер `descheduler`, `kube-rbac-proxy`, ConfigMap политики, RBAC/SA, CRD, registry-secret, цепочка поставки. Внешние системы (Kubernetes API, metrics-server, Prometheus, registry) отнесены к границам доверия.
- **Покрытие STRIDE:** все шесть категорий STRIDE покрыты (раздел 4.1); для каждого класса определены элементы и вывод.
- **Покрытие БДУ:** задействованы `УБИ.1`–`УБИ.9`, `УБИ.11`; `УБИ.10` рассмотрена и обоснованно признана неприменимой.
- **Трассируемость «угроза → сценарий → решение»:** обеспечена связками БДУ → `AS-01…AS-09` → `TM-01…TM-11` (разделы 4 → 5 → 6.1 → 6.2).

### Порядок актуализации при изменениях

| Тип изменения | Какие разделы пересматриваются |
| ------------- | ------------------------------ |
| **Minor** (правка стратегий/порогов в CR, изменение `deschedulingInterval`, обновление зависимостей без смены архитектуры) | Раздел 2.3 (версии зависимостей, VEX/SBOM), раздел 3 (если меняются параметры поверхности), раздел 6.1 (переоценка актуальности затронутых угроз). |
| **Major** (изменение RBAC/прав SA, добавление `Service`/сетевых интерфейсов, смена upstream-версии descheduler, изменение патча/логики вытеснения, изменение схемы CRD) | Полный пересмотр разделов 1–6: цели/критичные функции, архитектура и границы доверия, поверхность атаки, STRIDE×БДУ, сценарии, актуальность и меры. |
| **Изменение цепочки поставки** (`SOURCE_REPO`/`GOPROXY`/базовый образ/процесс сборки) | Раздел 2.3, AS-06/TM-06, раздел 6.6 (supply-chain review). |

### Числовое ранжирование / детальная оценка рисков

При необходимости детального ранжирования актуальных угроз (`TM-01…TM-11`) рекомендуется применить методику **DREAD** (Damage, Reproducibility, Exploitability, Affected users, Discoverability) — в настоящем документе использована качественная категоризация (Критический/Высокий/Средний/Низкий) согласно разделу 6 Методики.
