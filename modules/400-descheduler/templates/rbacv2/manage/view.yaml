apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: descheduler
    rbac.deckhouse.io/aggregate-to-infrastructure-as: viewer
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/capability: "system-capability.descheduler.view"
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: d8-descheduler
  name: d8:system-capability:descheduler:view
  annotations:
    en.meta.deckhouse.io/title: "Module descheduler: view configuration"
    ru.meta.deckhouse.io/title: "Модуль descheduler: просмотр конфигурации"
    en.meta.deckhouse.io/description: "Read-only access to the descheduler module configuration."
    ru.meta.deckhouse.io/description: "Доступ только на чтение к конфигурации модуля descheduler."
rules:
- apiGroups:
  - deckhouse.io
  resources:
  - deschedulers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - deckhouse.io
  resourceNames:
  - descheduler
  resources:
  - moduleconfigs
  verbs:
  - get
  - list
  - watch
