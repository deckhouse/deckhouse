{{- $binaries := "/usr/sbin/xtables-nft-multi /usr/sbin/ip6tables-nft /usr/sbin/ip6tables-nft-restore /usr/sbin/ip6tables-nft-save /usr/sbin/iptables-nft /usr/sbin/iptables-nft-restore /usr/sbin/iptables-nft-save /usr/sbin/xtables-legacy-multi /usr/sbin/iptables /usr/sbin/iptables-restore /usr/sbin/iptables-save /usr/sbin/iptables-legacy /usr/sbin/iptables-legacy-restore /usr/sbin/iptables-legacy-save /usr/sbin/ip6tables /usr/sbin/ip6tables-restore /usr/sbin/ip6tables-save /usr/sbin/ip6tables-legacy /usr/sbin/ip6tables-legacy-restore /usr/sbin/ip6tables-legacy-save /usr/usr/sbin/conntrack /usr/lib64/libnetfilter_conntrack.so*" }}

---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-binaries-artifact
fromArtifact: relocate-artifact
shell:
  beforeInstall:
  {{- include "alt packages proxy" . | nindent 2 }}
  - apt-get install -y iptables iptables-nft
  install:
  - /binary_replace.sh -i "{{ $binaries }}" -o /relocate
  - |
    for cmd in iptables iptables-save iptables-restore ip6tables ip6tables-save ip6tables-restore ip6tables-nft ip6tables-nft-restore ip6tables-nft-save iptables-nft iptables-nft-restore iptables-nft-save; do
      ln -f -s /iptables-wrapper "/relocate/usr/sbin/${cmd}"
    done
    # broken symlinks are not imported from the artifact
    touch /iptables-wrapper

{{- range $key, $value := .CandiVersionMap.k8s }}
{{- $version := toString $key }}
{{- $patch := $value.patch | toString }}
{{- $v := semver $version }}
{{- $image_version := printf "%s.%s" $version $patch | replace "." "-" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $version | replace "." "-" }}
fromImage: common/distroless
import:
  - artifact: {{ $.ModuleName }}/{{ $.ImageName }}-binaries-artifact
    add: /relocate
    to: /
    before: setup
  - artifact: {{ $.ModuleName }}/{{ $.ImageName }}-binaries-artifact
    add: /
    to: /
    includePaths:
    - usr/lib64/iptables
    - usr/lib64/libm.*
    before: setup
  - artifact: common/kubernetes-artifact-{{ $image_version }}
    add: /src/_output/bin/kube-proxy
    to: /usr/local/bin/kube-proxy
    before: setup
  - image: common/iptables-wrapper
    add: /iptables-wrapper
    to: /iptables-wrapper
    before: setup
docker:
  ENTRYPOINT: ["/usr/local/bin/kube-proxy"]
{{- end }}
