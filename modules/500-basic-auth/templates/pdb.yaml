apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ .Chart.Name }}
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: {{ .Chart.Name }}
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
