{{- if .Values.dashboard.gitlabBaseUrl }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: {{ .Values.global.ingressClass | quote }}
  name: dashboard-oauth2-proxy
  namespace: kube-system
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: oauth2-proxy
spec:
  rules:
  - host: dashboard.{{ .Values.global.clusterHostname }}
    http:
      paths:
      - backend:
          serviceName: dashboard-oauth2-proxy
          servicePort: 4180
        path: /oauth2
  {{- if and (.Values.global.ingressACME) (.Values.global.enabledModules | has "cert-manager") }}
  tls:
  - hosts:
      - dashboard.{{ .Values.global.clusterHostname }}
    secretName: ingress-tls
  {{- end }}
{{- end }}
