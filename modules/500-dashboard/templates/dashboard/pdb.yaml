apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: dashboard
  namespace: kube-system
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: dashboard
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: dashboard
