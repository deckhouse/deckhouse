kind: Service
apiVersion: v1
metadata:
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: dashboard
  name: dashboard
  namespace: kube-system
spec:
  ports:
  - port: 80
    targetPort: 9090
  selector:
    module: {{ $.Chart.Name }}
    app: dashboard
