kind: Service
apiVersion: v1
metadata:
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: dashboard
  name: dashboard
  namespace: kube-system
spec:
  ports:
  - name: https
    port: 443
  selector:
    module: {{ $.Chart.Name }}
    app: dashboard
