---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    heritage: deckhouse
    module: multitenancy-manager
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: clustergrantableresources.multitenancy.deckhouse.io
spec:
  group: multitenancy.deckhouse.io
  names:
    kind: ClusterGrantableResource
    listKind: ClusterGrantableResourceList
    plural: clustergrantableresources
    shortNames:
    - cgr
    singular: clustergrantableresource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.grantedResource.kind
      name: Granted
      type: string
    - jsonPath: .spec.enforcement
      name: Enforcement
      type: string
    - jsonPath: .spec.defaultAvailability
      name: Default
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterGrantableResource registers a cluster-scoped resource
          as grant-controllable.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterGrantableResource.
            properties:
              coerceToDefault:
                description: |-
                  CoerceToDefault, when true, makes the /defaults webhook rewrite a referencing value that is not
                  available to the project to the project default, instead of letting /is-granted reject it. Enable
                  it only for fields a built-in admission controller pre-populates with a cluster default that the
                  project may not allow — e.g. PVC spec.storageClassName (DefaultStorageClass). For fields with no
                  such defaulter, leave it false so an explicit out-of-list value is rejected, not silently rewritten.
                type: boolean
              defaultAvailability:
                default: All
                description: 'DefaultAvailability is the baseline when no grant allows
                  an object: All or None.'
                enum:
                - All
                - None
                type: string
              defaultFrom:
                description: DefaultFrom marks the cluster-wide default object by
                  annotation (fallback default).
                properties:
                  annotationKey:
                    description: AnnotationKey is the annotation on a granted object
                      that marks it as the cluster-wide default.
                    type: string
                type: object
              enforcement:
                default: Managed
                description: Enforcement is Managed (our webhooks) or External (the
                  module's own webhook enforces).
                enum:
                - Managed
                - External
                type: string
              excluded:
                description: Excluded lists objects never available to tenants, regardless
                  of any grant (hard deny).
                properties:
                  matchExpressions:
                    description: MatchExpressions is a list of label selector requirements
                      matched against the granted object labels.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: MatchLabels is a map of {key,value} pairs matched
                      against the granted object labels.
                    type: object
                  names:
                    description: Names is an explicit list of object names.
                    items:
                      type: string
                    type: array
                type: object
              grantedResource:
                description: GrantedResource is the cluster-scoped resource being
                  governed (absent ⇒ value-backed).
                properties:
                  apiVersion:
                    description: APIVersion is the group/version of the granted resource
                      (e.g. storage.k8s.io/v1).
                    type: string
                  kind:
                    description: Kind is the kind of the granted resource (e.g. StorageClass).
                    type: string
                type: object
              usageReferences:
                description: UsageReferences declares where the granted name is referenced
                  and what is measurable.
                items:
                  description: UsageReference declares where a granted name is referenced
                    and what is measurable there.
                  properties:
                    countable:
                      description: |-
                        Countable, if true, lets the count of these usage objects be limited; the measure key is the
                        resource plural.
                      type: boolean
                    fieldPath:
                      description: |-
                        FieldPath is the default JSONPath to the granted object's name (a string), for all matched
                        group/versions. May target an annotation, e.g. $.metadata.annotations['ipam.cilium.io/ip-pool'].
                      type: string
                    match:
                      description: Match guards this reference; it applies only when
                        the predicate holds on the object.
                      properties:
                        equals:
                          description: Equals matches when the value equals this string.
                          type: string
                        fieldPath:
                          description: FieldPath is the JSONPath to the value tested
                            by the predicate.
                          type: string
                        in:
                          description: In matches when the value is one of these strings.
                          items:
                            type: string
                          type: array
                      required:
                      - fieldPath
                      type: object
                    paths:
                      description: Paths overrides FieldPath per group/version when
                        the field moved between versions.
                      items:
                        description: |-
                          PathOverride overrides FieldPath for specific groups/versions, because a field may sit at
                          different paths across versions.
                        properties:
                          apiGroups:
                            description: APIGroups restricts this override to these
                              groups (empty = any matched group).
                            items:
                              type: string
                            type: array
                          apiVersions:
                            description: APIVersions restricts this override to these
                              versions (empty = any matched version).
                            items:
                              type: string
                            type: array
                          fieldPath:
                            description: FieldPath is the JSONPath to the granted
                              name for the matched group/versions.
                            type: string
                        required:
                        - fieldPath
                        type: object
                      type: array
                    quantities:
                      description: Quantities are summable quantity fields; each Name
                        is a measure key.
                      items:
                        description: QuantityMeasure is a summable quantity field;
                          Name is the measure (quota) key.
                        properties:
                          fieldPath:
                            description: FieldPath is the JSONPath to the resource.Quantity
                              value to sum.
                            type: string
                          name:
                            description: Name is the measure key used in GrantQuota
                              (e.g. requests.storage).
                            type: string
                        required:
                        - fieldPath
                        - name
                        type: object
                      type: array
                    rule:
                      description: Rule matches which usage objects this reference
                        applies to (groups/versions/resources).
                      properties:
                        apiGroups:
                          description: APIGroups are the API groups of the usage object
                            (e.g. networking.k8s.io, extensions); "*" = any.
                          items:
                            type: string
                          type: array
                        apiVersions:
                          description: APIVersions are the versions to match (e.g.
                            v1, v1beta1); "*" = any.
                          items:
                            type: string
                          type: array
                        resources:
                          description: Resources are the plural names of the usage
                            object (e.g. ingresses).
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - apiVersions
                      - resources
                      type: object
                  required:
                  - fieldPath
                  - rule
                  type: object
                type: array
            type: object
          status:
            description: status defines the observed state of ClusterGrantableResource.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
