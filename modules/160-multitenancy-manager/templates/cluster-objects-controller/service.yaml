---
apiVersion: v1
kind: Service
metadata:
  name: cluster-objects-controller
  namespace: d8-{{ .Chart.Name }}
  {{- include "helm_lib_module_labels" (list . (dict "app" "cluster-objects-controller")) | nindent 2 }}
spec:
  selector:
    app: cluster-objects-controller
  ports:
    - name: webhook
      port: 9443
      targetPort: webhook
      protocol: TCP
