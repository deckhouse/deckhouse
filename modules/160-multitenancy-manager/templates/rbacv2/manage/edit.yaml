apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: multitenancy-manager
    rbac.deckhouse.io/aggregate-to-deckhouse-as: manager
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/capability: "system-capability.multitenancy-manager.edit"
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: d8-multitenancy-manager
  name: d8:system-capability:multitenancy-manager:edit
  annotations:
    en.meta.deckhouse.io/title: "Module multitenancy-manager: edit configuration"
    ru.meta.deckhouse.io/title: "Модуль multitenancy-manager: управление конфигурацией"
    en.meta.deckhouse.io/description: "Manage the multitenancy-manager module configuration."
    ru.meta.deckhouse.io/description: "Управление конфигурацией модуля multitenancy-manager."
rules:
- apiGroups:
  - deckhouse.io
  resources:
  - projects
  - projecttemplates
  verbs:
  - create
  - update
  - patch
  - delete
  - deletecollection
- apiGroups:
  - deckhouse.io
  resourceNames:
  - multitenancy-manager
  resources:
  - moduleconfigs
  verbs:
  - create
  - update
  - patch
  - delete
# Cluster operators author the per-project availability policies. The registrations and reference paths
# (grantableclusterresource{definitions,references}) are shipped by the module and are not creatable —
# read-only via the view role. The catalog (availableclusterresources) is controller-rendered too.
- apiGroups:
  - multitenancy.deckhouse.io
  resources:
  - clusterresourcegrantpolicies
  verbs:
  - create
  - update
  - patch
  - delete
  - deletecollection
