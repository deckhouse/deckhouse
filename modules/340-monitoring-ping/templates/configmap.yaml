---
apiVersion: v1
kind: ConfigMap
metadata:
  name: monitoring-ping-config
  namespace: d8-system
{{ include "helm_lib_module_labels" (list . (dict "app" "monitoring-ping")) | indent 2 }}
data:
  targets.json: >
    {{ .Values.monitoringPing.targets | toJson }}
