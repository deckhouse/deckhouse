---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: d8-system:monitoring-ping
{{ include "helm_lib_module_labels" (list . (dict "app" "monitoring-ping")) | indent 2 }}
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["list"]
