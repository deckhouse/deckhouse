type: object
properties:
  logLevel:
    description: logLevel
  routingTableIDMin:
    description: Минимальное значение, которое можно выбрать в качестве iproute table ID для ресурсов `RoutingTable` без явного указания `spec.ipRoutingTableID`.
  RoutingTableIDMax:
    description: Максимальное значение, которое можно выбрать в качестве iproute table ID для ресурсов `RoutingTable` без явного указания `spec.ipRoutingTableID`.
