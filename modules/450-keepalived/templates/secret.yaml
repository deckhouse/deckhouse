{{- range $instance_name, $instance := .Values.keepalived.instances }}
---
apiVersion: v1
kind: Secret
metadata:
  name: keepalived-instance-secret-{{ $instance_name }}
  namespace: d8-{{ $.Chart.Name }}
{{ include "helm_lib_module_labels" (list $ (dict "app" "keepalived") (dict "keepalived-instance" (printf "%s" $instance_name))) | indent 2 }}
type: Opaque
data:
  authPass: {{ $instance.authPass | b64enc }}
{{- end }}
