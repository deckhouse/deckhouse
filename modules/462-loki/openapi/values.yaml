x-extend:
  schema: config-values.yaml
type: object
properties:
  internal:
    type: object
    default: {}
    properties:
      effectiveStorageClass:
        default: false
        oneOf:
          - type: string
          - type: boolean
            enum: [false]
      grafanaToken:
        type: string
        default: ""
      logShipperToken:
        type: string
        default: ""
      kubeRbacProxyTLS:
        type: object
        default: {}
        properties:
          cert:
            type: string
          key:
            type: string
          ca: 
            type: string
      pvcSize:
        type: integer
        format: int64
        default: 0
      stsStorageSize:
        type: integer
        format: int64
        default: 0
      cleanupThreshold:
        type: integer
        format: int64
        default: 0
