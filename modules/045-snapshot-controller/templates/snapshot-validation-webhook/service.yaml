apiVersion: v1
kind: Service
metadata:
  name: d8-snapshot-validation-webhook
  namespace: kube-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "d8-snapshot-validation-webhook")) | nindent 2 }}
spec:
  selector:
    app: d8-snapshot-validation-webhook
  ports:
    - protocol: TCP
      port: 4443
      targetPort: 4443
