apiVersion: v1
kind: ConfigMap
metadata:
  name: ping-exporter-config
  namespace: kube-prometheus
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
data:
  targets.json: >
    {{ .Values.pingExporter.targets | toJson }}
