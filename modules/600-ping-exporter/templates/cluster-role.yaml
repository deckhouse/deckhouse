kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: kube-prometheus:ping-exporter
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["list"]
