{{- $commit := "a3796b0920dfa062456f318bff03781fe94969b8" }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}
final: false
fromImage: common/src-artifact
shell:
  install:
  - git clone --depth 1 {{ $.SOURCE_REPO }}/python-modules/wheels /wheels
  - cd /wheels
  - git reset --hard {{ $commit }}
  - rm -rf /wheels/.git
