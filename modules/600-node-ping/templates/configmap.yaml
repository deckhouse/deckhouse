apiVersion: v1
kind: ConfigMap
metadata:
  name: node-ping-config
  namespace: kube-prometheus
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
data:
  nodes.json: >
    {{ .Values.nodePing.nodes | toJson }}
