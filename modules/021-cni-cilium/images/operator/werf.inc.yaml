# https://github.com/cilium/cilium/releases/tag/v1.13.2
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
from: quay.io/cilium/operator:v1.13.2@sha256:2c518afd4a1a5123755c1335e3068883283c9572f4355727d789a4846c46c2ae
---
image: {{ $.ModuleName }}/{{ $.ImageName }}
from: {{ $.Images.BASE_SCRATCH }}
import:
- artifact: {{ $.ModuleName }}/builder-cert-artifact
  add: /etc/ssl/certs/ca-certificates.crt
  to: /etc/ssl/certs/ca-certificates.crt
  before: install
- artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
  add: /usr/bin/cilium-operator
  to: /usr/bin/cilium-operator
  before: install
docker:
  USER: nobody
  ENTRYPOINT: ["/usr/bin/cilium-operator"]
