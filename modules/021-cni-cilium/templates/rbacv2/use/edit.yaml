apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  {{- include "helm_lib_module_labels" (list . (dict "rbac.deckhouse.io/aggregate-to-namespace-as" "manager" "rbac.deckhouse.io/kind" "capability" "rbac.deckhouse.io/scope" "namespace")) | nindent 2 }}
  name: d8:namespace-capability:cni-cilium:edit
  annotations:
    en.meta.deckhouse.io/title: "Module cni-cilium: edit"
    ru.meta.deckhouse.io/title: "Модуль cni-cilium: редактирование"
    en.meta.deckhouse.io/description: "Manage cni-cilium resources in a namespace."
    ru.meta.deckhouse.io/description: "Управление ресурсами модуля cni-cilium в пространстве имён."
rules:
- apiGroups:
  - networking.k8s.io
  resources:
  - networkpolicies
  verbs:
  - create
  - update
  - patch
  - delete
  - deletecollection
