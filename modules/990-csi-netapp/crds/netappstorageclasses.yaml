---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: netappstorageclasses.storage.deckhouse.io
  labels:
    heritage: deckhouse
    module: csi-netapp
    backup.deckhouse.io/cluster-config: "true"
spec:
  group: storage.deckhouse.io
  names:
    kind: NetappStorageClass
    listKind: NetappStorageClassList
    plural: netappstorageclasses
    shortNames:
      - nasc
    singular: netappstorageclass
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                connectionName:
                  description: 'Required: Reference to NetappStorageConnection by name'
                  type: string
                reclaimPolicy:
                  description: 'Optional: Reclaim policy for volumes (Delete or Retain)'
                  enum:
                    - Delete
                    - Retain
                  type: string
                fsType:
                  default: ext4
                  description: 'Optional: Filesystem type (ext3, ext4, xfs)'
                  enum:
                    - ext3
                    - ext4
                    - xfs
                  type: string
                provisioningType:
                  default: thick
                  description: 'Optional: Volume provisioning type (thin or thick)'
                  enum:
                    - thin
                    - thick
                  type: string
              required:
                - connectionName
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
