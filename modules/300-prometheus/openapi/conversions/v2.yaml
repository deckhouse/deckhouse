version: 2
conversions:
  - del(.auth.password) | if .auth == {} then del(.auth) end
description:
  ru: "Удалите поле `.auth.password`.  затем `auth`, если он пуст."
  en: "Remove `.auth.password` field, then `.auth` if empty."
