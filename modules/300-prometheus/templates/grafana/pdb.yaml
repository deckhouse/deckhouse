apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: grafana
  namespace: kube-prometheus
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: grafana
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: grafana
