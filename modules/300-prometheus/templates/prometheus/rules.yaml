apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-main-rules
  namespace: kube-prometheus
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: prometheus
    prometheus: main
    component: rules
data:
{{ (.Files.Glob "prometheus-rules/*.yml").AsConfig | indent 2 }}
