apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: trickster
  namespace: kube-prometheus
  labels:
    heritage: antiopa
    module: {{ $.Chart.Name }}
    app: trickster
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: trickster
