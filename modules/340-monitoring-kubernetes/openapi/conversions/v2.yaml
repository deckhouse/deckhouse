version: 2
conversions:
  - if .ebpfExporterEnabled == true then .oomKillsExporterEnabled=true end
  - del(.ebpfExporterEnabled)
description:
  ru: "Если для поля `.ebpfExporterEnabled` установлено значение `true`, добавьте поле `.oomKillsExporterEnabled=true`. После этого удалите поле `.ebpfExporterEnabled` из объекта."
  en: "If the `.ebpfExporterEnabled` field is set to `true`, add `.oomKillsExporterEnabled=true`. Then, delete the `.ebpfExporterEnabled` field from the object."
