#!/bin/bash

source /antiopa/shell_lib.sh

function __config__() {
  jo -p beforeHelm=10
}

function __main__() {
  if ! values::has userAuthn.publishAPI ; then
    values::set userAuthn.publishAPI "false"
  fi
}

hook::run "$@"
