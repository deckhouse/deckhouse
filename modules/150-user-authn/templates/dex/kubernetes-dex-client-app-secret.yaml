---
apiVersion: v1
kind: Secret
metadata:
  name: kubernetes-dex-client-app-secret
  namespace: kube-{{ .Chart.Name }}
  labels:
    heritage: antiopa
    module: {{ .Chart.Name }}
    app: dex
data:
  secret: {{ .Values.userAuthn.kubernetesDexClientAppSecret | b64enc }}
