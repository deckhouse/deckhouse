diff --git a/storage/storage.go b/storage/storage.go
--- a/storage/storage.go	(revision ea98ff5e903bc331cd53e591f723cf5cdcfe876e)
+++ b/storage/storage.go	(date 1729533350794)
@@ -354,6 +354,9 @@

 	// Randomly generated user ID. This is NOT the primary ID of the Password object.
 	UserID string `json:"userID"`
+
+	// Groups assigned to the user
+	Groups []string `json:"groups"`
 }

 // Connector is an object that contains the metadata about connectors used to login to Dex.
diff --git a/storage/kubernetes/types.go b/storage/kubernetes/types.go
--- a/storage/kubernetes/types.go	(revision ea98ff5e903bc331cd53e591f723cf5cdcfe876e)
+++ b/storage/kubernetes/types.go	(date 1729580170885)
@@ -431,9 +431,21 @@
 	// This field is IMMUTABLE. Do not change.
 	Email string `json:"email,omitempty"`

-	Hash     []byte `json:"hash,omitempty"`
-	Username string `json:"username,omitempty"`
-	UserID   string `json:"userID,omitempty"`
+	Hash     []byte  `json:"hash,omitempty"`
+	Username string  `json:"username,omitempty"`
+	UserID   string  `json:"userID,omitempty"`
+	Groups   []Group `json:"groups,omitempty"`
+}
+
+type Group string
+
+func (g *Group) UnmarshalJSON(b []byte) error {
+	*g = Group(strings.Trim(string(b), `"`))
+	return nil
+}
+
+func (g *Group) String() string {
+	return string(*g)
 }

 // PasswordList is a list of Passwords.
@@ -445,6 +457,10 @@

 func (cli *client) fromStoragePassword(p storage.Password) Password {
 	email := strings.ToLower(p.Email)
+	var groups []Group
+	for _, g := range p.Groups {
+		groups = append(groups, Group(g))
+	}
 	return Password{
 		TypeMeta: k8sapi.TypeMeta{
 			Kind:       kindPassword,
@@ -458,15 +474,21 @@
 		Hash:     p.Hash,
 		Username: p.Username,
 		UserID:   p.UserID,
+		Groups:   groups,
 	}
 }

 func toStoragePassword(p Password) storage.Password {
+	var groups []string
+	for _, g := range p.Groups {
+		groups = append(groups, g.String())
+	}
 	return storage.Password{
 		Email:    p.Email,
 		Hash:     p.Hash,
 		Username: p.Username,
 		UserID:   p.UserID,
+		Groups:   groups,
 	}
 }

diff --git a/server/server.go b/server/server.go
--- a/server/server.go	(revision ea98ff5e903bc331cd53e591f723cf5cdcfe876e)
+++ b/server/server.go	(date 1729533350783)
@@ -532,6 +532,7 @@
 		Username:      p.Username,
 		Email:         p.Email,
 		EmailVerified: true,
+		Groups:        p.Groups,
 	}, true, nil
 }

@@ -556,6 +557,7 @@
 	// No other fields are expected to be refreshable as email is effectively used
 	// as an ID and this implementation doesn't deal with groups.
 	identity.Username = p.Username
+	identity.Groups = p.Groups

 	return identity, nil
 }
diff --git a/cmd/dex/config.go b/cmd/dex/config.go
--- a/cmd/dex/config.go	(revision ea98ff5e903bc331cd53e591f723cf5cdcfe876e)
+++ b/cmd/dex/config.go	(date 1729533350779)
@@ -95,11 +95,12 @@

 func (p *password) UnmarshalJSON(b []byte) error {
 	var data struct {
-		Email       string `json:"email"`
-		Username    string `json:"username"`
-		UserID      string `json:"userID"`
-		Hash        string `json:"hash"`
-		HashFromEnv string `json:"hashFromEnv"`
+		Email       string   `json:"email"`
+		Username    string   `json:"username"`
+		UserID      string   `json:"userID"`
+		Hash        string   `json:"hash"`
+		HashFromEnv string   `json:"hashFromEnv"`
+		Groups      []string `json:"groups"`
 	}
 	if err := json.Unmarshal(b, &data); err != nil {
 		return err
@@ -108,6 +109,7 @@
 		Email:    data.Email,
 		Username: data.Username,
 		UserID:   data.UserID,
+		Groups:   data.Groups,
 	})
 	if len(data.Hash) == 0 && len(data.HashFromEnv) > 0 {
 		data.Hash = os.Getenv(data.HashFromEnv)
diff --git a/storage/sql/crud.go b/storage/sql/crud.go
--- a/storage/sql/crud.go	(revision ea98ff5e903bc331cd53e591f723cf5cdcfe876e)
+++ b/storage/sql/crud.go	(date 1729533350792)
@@ -598,13 +598,13 @@
 	p.Email = strings.ToLower(p.Email)
 	_, err := c.Exec(`
 		insert into password (
-			email, hash, username, user_id
+			email, hash, username, user_id, groups
 		)
 		values (
-			$1, $2, $3, $4
+			$1, $2, $3, $4, $5
 		);
 	`,
-		p.Email, p.Hash, p.Username, p.UserID,
+		p.Email, p.Hash, p.Username, p.UserID, encoder(p.Groups),
 	)
 	if err != nil {
 		if c.alreadyExistsCheck(err) {
@@ -629,10 +629,10 @@
 		_, err = tx.Exec(`
 			update password
 			set
-				hash = $1, username = $2, user_id = $3
-			where email = $4;
+				hash = $1, username = $2, user_id = $3, groups = $4
+			where email = $5;
 		`,
-			np.Hash, np.Username, np.UserID, p.Email,
+			np.Hash, np.Username, np.UserID, encoder(p.Groups), p.Email,
 		)
 		if err != nil {
 			return fmt.Errorf("update password: %v", err)
@@ -648,7 +648,7 @@
 func getPassword(q querier, email string) (p storage.Password, err error) {
 	return scanPassword(q.QueryRow(`
 		select
-			email, hash, username, user_id
+			email, hash, username, user_id, groups
 		from password where email = $1;
 	`, strings.ToLower(email)))
 }
@@ -656,7 +656,7 @@
 func (c *conn) ListPasswords() ([]storage.Password, error) {
 	rows, err := c.Query(`
 		select
-			email, hash, username, user_id
+			email, hash, username, user_id, groups
 		from password;
 	`)
 	if err != nil {
@@ -680,7 +680,7 @@

 func scanPassword(s scanner) (p storage.Password, err error) {
 	err = s.Scan(
-		&p.Email, &p.Hash, &p.Username, &p.UserID,
+		&p.Email, &p.Hash, &p.Username, &p.UserID, decoder(&p.Groups),
 	)
 	if err != nil {
 		if err == sql.ErrNoRows {
