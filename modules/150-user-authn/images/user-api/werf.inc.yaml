---
image: {{ .ModuleName }}/{{ .ImageName }}
fromImage: common/distroless
import:
  - image: {{ $.ModuleName }}/{{ $.ImageName }}-user-api-artifact
    add: /src/user-api
    to: /user-api
    before: setup
imageSpec:
  config:
    entrypoint: [ "/user-api" ]
---
image: {{ .ModuleName }}/{{ .ImageName }}-user-api-src-artifact
fromImage: common/src-artifact
final: false
git:
- add: /{{ $.ModulePath }}modules/{{ $.ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/src
  to: /src
  includePaths:
  - '**/*.go'
  - '**/go.mod'
  - '**/go.sum'
  stageDependencies:
    install:
    - '**/*.go'
    - '**/go.mod'
    - '**/go.sum'
---
image: {{ .ModuleName }}/{{ .ImageName }}-user-api-artifact
fromImage: {{ eq $.SVACE_ENABLED "false" | ternary "builder/golang-alpine" "builder/golang-alt-svace" }}
final: false
import:
- image: {{ $.ModuleName }}/{{ $.ImageName }}-user-api-src-artifact
  add: /src
  to: /src
  before: install
mount:
{{ include "mount points for golang builds" . }}
secrets:
- id: GOPROXY
  value: {{ .GOPROXY }}
shell:
  install:
    - export GOPROXY=$(cat /run/secrets/GOPROXY)
    - cd /src
    - export CGO_ENABLED=0 GOOS=linux
    - |
      {{- include "image-build.build" (set $ "BuildCommand" `go build -ldflags '-s -w' -o user-api ./cmd/main.go`) | indent 6 }}
    - chown 64535:64535 user-api
    - chmod 0700 user-api
