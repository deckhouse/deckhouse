apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: kube-dns
    rbac.deckhouse.io/aggregate-to-kubernetes-as: manager
    rbac.deckhouse.io/aggregate-to-networking-as: manager
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/capability: "system-capability.kube-dns.edit"
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: kube-system
  name: d8:system-capability:kube-dns:edit
  annotations:
    en.meta.deckhouse.io/title: "Module kube-dns: edit configuration"
    ru.meta.deckhouse.io/title: "Модуль kube-dns: управление конфигурацией"
    en.meta.deckhouse.io/description: "Manage the kube-dns module configuration."
    ru.meta.deckhouse.io/description: "Управление конфигурацией модуля kube-dns."
rules:
- apiGroups:
  - deckhouse.io
  resourceNames:
  - kube-dns
  resources:
  - moduleconfigs
  verbs:
  - create
  - update
  - patch
  - delete
