linters-settings:
  module:
    oss:
      disable: true
  templates:
    exclude-rules:
      service-port:
        - name: d8-kube-dns-sts-pods-hosts-appender-webhook
          port: https
  openapi:
    exclude-rules:
      enum:
        - properties.internal.properties.specificNodeType
