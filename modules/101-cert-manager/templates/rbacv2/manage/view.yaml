# Generated by dmt (rbac/sync) from rbac.yaml, contract 1. Edit rbac.yaml and run "dmt lint --linter rbac --fix", or remove this line to maintain the file by hand.
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:system-capability:cert-manager:view
  {{- include "helm_lib_module_labels" (list . (dict "rbac.deckhouse.io/aggregate-to-security-as" "viewer" "rbac.deckhouse.io/capability" "system-capability.cert-manager.view" "rbac.deckhouse.io/kind" "capability" "rbac.deckhouse.io/namespace" "d8-cert-manager" "rbac.deckhouse.io/scope" "system")) | nindent 2 }}
  annotations:
    en.meta.deckhouse.io/description: "Read-only access to the cert-manager module configuration."
    en.meta.deckhouse.io/title: "Module cert-manager: view configuration"
    ru.meta.deckhouse.io/description: "Доступ только на чтение к конфигурации модуля cert-manager."
    ru.meta.deckhouse.io/title: "Модуль cert-manager: просмотр конфигурации"
rules:
- apiGroups:
  - cert-manager.io
  resources:
  - clusterissuers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - deckhouse.io
  resources:
  - moduleconfigs
  resourceNames:
  - cert-manager
  verbs:
  - get
  - list
  - watch
