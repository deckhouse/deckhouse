linters-settings:
  module:
    oss:
      disable: true
  templates:
    exclude-rules:
      ingress:
        - kind: Ingress
          name: deckhouse-tools
  container:
    exclude-rules:
      read-only-root-filesystem:
        - kind: Deployment
          name: deckhouse-tools
          container: web