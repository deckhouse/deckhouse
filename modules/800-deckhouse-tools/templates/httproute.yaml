{{- $moduleGateway := dict }}
{{- include "helm_lib_module_gateway" (list . $moduleGateway) }}
{{- $satisfyAny := dig "auth" "satisfyAny" false .Values.deckhouseTools }}
{{- if and ($moduleGateway) (.Values.global.modules.publicDomainTemplate) (.Values.global.clusterIsBootstrapped) (include "helm_lib_kind_exists" (list . "HTTPRoute")) (include "helm_lib_kind_exists" (list . "ListenerSet")) }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  {{- include "helm_lib_module_labels" (list . (dict "app" "deckhouse-tools")) | nindent 2 }}
  name: deckhouse-tools
  namespace: d8-system
spec:
  listeners:
{{- if (include "helm_lib_module_https_route_tls_enabled" .) }}
  - allowedRoutes:
      namespaces:
        from: Same
    hostname: {{ include "helm_lib_module_public_domain" (list . "tools") }}
    name: tools
    port: 443
    protocol: HTTPS
    tls:
      certificateRefs:
      - group: ""
        kind: Secret
        name: {{ include "helm_lib_module_https_secret_name" (list . "tools-httproute-tls") }}
        namespace: d8-system
      mode: Terminate
  - allowedRoutes:
      namespaces:
        from: Same
    hostname: {{ include "helm_lib_module_public_domain" (list . "tools") }}
    name: tools-redirect
    port: 80
    protocol: HTTP
{{- else }}
  - allowedRoutes:
      namespaces:
        from: Same
    hostname: {{ include "helm_lib_module_public_domain" (list . "tools") }}
    name: tools
    port: 80
    protocol: HTTP
{{- end }}
  parentRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: {{ $moduleGateway.name }}
    namespace: {{ $moduleGateway.namespace }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: deckhouse-tools
  namespace: d8-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "deckhouse-tools")) | nindent 2 }}
  annotations:
    web.deckhouse.io/export-name: "tools"
    web.deckhouse.io/export-icon: "/public/img/tools.ico"
  {{- if and (ne (include "helm_lib_module_https_mode" .) "Disabled") .Values.deckhouseTools.auth.externalAuthentication }}
    alb.network.deckhouse.io/auth-signin: {{ replace "$host" (include "helm_lib_module_public_domain" (list . "tools")) .Values.deckhouseTools.auth.externalAuthentication.authSignInURL | quote }}
    alb.network.deckhouse.io/auth-url: {{ .Values.deckhouseTools.auth.externalAuthentication.authURL | quote }}
    {{- if $satisfyAny }}
    alb.network.deckhouse.io/satisfy: "any"
    {{- end }}
  {{- else }}
    alb.network.deckhouse.io/basic-auth-secret: d8-system/tools-basic-auth
  {{- end }}
  {{- if or (eq (include "helm_lib_module_https_mode" .) "CertManager") (eq (include "helm_lib_module_https_mode" .) "CustomCertificate") }}
    alb.network.deckhouse.io/response-headers-to-add: '{"Strict-Transport-Security":"max-age=31536000; includeSubDomains"}'
  {{- end }}
spec:
  hostnames:
  - {{ include "helm_lib_module_public_domain" (list . "tools") }}
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: ListenerSet
    name: deckhouse-tools
    namespace: d8-system
    sectionName: tools
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: deckhouse-tools
      port: 80
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /
{{- if (include "helm_lib_module_https_route_tls_enabled" .) }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: deckhouse-tools-redirect
  namespace: d8-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "deckhouse-tools")) | nindent 2 }}
spec:
  hostnames:
  - {{ include "helm_lib_module_public_domain" (list . "tools") }}
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: ListenerSet
    name: deckhouse-tools
    namespace: d8-system
    sectionName: tools-redirect
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
    matches:
    - path:
        type: PathPrefix
        value: /
{{- end }}
{{- end }}
