---
apiVersion: v1
kind: Service
metadata:
  name: webui
  namespace: d8-{{ .Chart.Name }}
{{ include "helm_lib_module_labels" (list . (dict "app" "webui")) | indent 2 }}
spec:
  ports:
    - name: webui
      port: 80
      targetPort: webui
  selector:
    app: webui
