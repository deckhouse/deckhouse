---
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: main-access-log-format
  {{- include "helm_lib_module_labels" (list .) | nindent 2 }}
spec:
  accessLogging:
  - disabled: false
    providers:
    - name: d8-main-access-log-format
