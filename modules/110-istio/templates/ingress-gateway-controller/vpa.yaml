{{- range $controller := .Values.istio.internal.ingressControllers }}
{{- if ($.Values.global.enabledModules | has "vertical-pod-autoscaler") }}
{{- $resourcesRequests := default (dict) $controller.spec.resourcesRequests }}
{{- $mode := ($resourcesRequests.mode | default "VPA") }}
{{- if eq $mode "VPA" }}
{{- $resourcesManagement := include "istio_ingress_gateway_resources_management" $controller | fromYaml }}
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: ingress-gateway-controller-{{ $controller.name }}
  namespace: d8-ingress-{{ $.Chart.Name }}
  {{- include "helm_lib_module_labels" (list $ (dict
      "app" "ingress-gateway-controller"
      "instance" $controller.name
  )) | nindent 2 }}
spec:
{{- include "helm_lib_resources_management_vpa_spec" (list
  "apps/v1"
  "DaemonSet"
  (printf "ingress-gateway-controller-%s" $controller.name)
  "istio-proxy"
  $resourcesManagement
) | nindent 2 }}
{{- end }}
{{- end }}
{{- end }}
