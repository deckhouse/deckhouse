diff --git a/controllers/istiorevision/istiorevision_controller.go b/controllers/istiorevision/istiorevision_controller.go
index 5e97cde4..40455cec 100644
--- a/controllers/istiorevision/istiorevision_controller.go
+++ b/controllers/istiorevision/istiorevision_controller.go
@@ -21,6 +21,8 @@ import (
 	"path"
 	"reflect"
 	"regexp"
+	"strings"
+	"time"
 
 	"github.com/go-logr/logr"
 	v1 "github.com/istio-ecosystem/sail-operator/api/v1"
@@ -99,18 +101,26 @@ func NewReconciler(cfg config.ReconcilerConfig, client client.Client, scheme *ru
 func (r *Reconciler) Reconcile(ctx context.Context, rev *v1.IstioRevision) (ctrl.Result, error) {
 	log := logf.FromContext(ctx)
 
-	reconcileErr := r.doReconcile(ctx, rev)
+	result, reconcileErr := r.doReconcile(ctx, rev)
 
 	log.Info("Reconciliation done. Updating status.")
 	statusErr := r.updateStatus(ctx, rev, reconcileErr)
 
-	return ctrl.Result{}, errors.Join(reconcileErr, statusErr)
+	if reconcileErr != nil {
+		// If we got a requeue result, preserve it
+		if result.Requeue || result.RequeueAfter > 0 {
+			return result, statusErr
+		}
+		return ctrl.Result{}, errors.Join(reconcileErr, statusErr)
+	}
+	return result, statusErr
 }
 
-func (r *Reconciler) doReconcile(ctx context.Context, rev *v1.IstioRevision) error {
+// Updated doReconcile to return both result and error
+func (r *Reconciler) doReconcile(ctx context.Context, rev *v1.IstioRevision) (ctrl.Result, error) {
 	log := logf.FromContext(ctx)
 	if err := r.validate(ctx, rev); err != nil {
-		return err
+		return ctrl.Result{}, err
 	}
 
 	log.Info("Installing Helm chart")
@@ -154,7 +164,17 @@ func (r *Reconciler) validate(ctx context.Context, rev *v1.IstioRevision) error
 	return nil
 }
 
-func (r *Reconciler) installHelmCharts(ctx context.Context, rev *v1.IstioRevision) error {
+func (r *Reconciler) installHelmCharts(ctx context.Context, rev *v1.IstioRevision) (ctrl.Result, error) {
+	log := logf.FromContext(ctx)
+	releaseName := getReleaseName(rev)
+	targetNamespace := rev.Spec.Namespace
+	chartDir := r.getChartDir(rev)
+
+	log.Info("Installing Helm chart",
+		"release", releaseName,
+		"namespace", targetNamespace,
+		"chart", constants.IstiodChartName)
+
 	ownerReference := metav1.OwnerReference{
 		APIVersion:         v1.GroupVersion.String(),
 		Kind:               v1.IstioRevisionKind,
@@ -165,12 +185,39 @@ func (r *Reconciler) installHelmCharts(ctx context.Context, rev *v1.IstioRevisio
 	}
 
 	values := helm.FromValues(rev.Spec.Values)
-	_, err := r.ChartManager.UpgradeOrInstallChart(ctx, r.getChartDir(rev),
-		values, rev.Spec.Namespace, getReleaseName(rev), ownerReference)
-	if err != nil {
-		return fmt.Errorf("failed to install/update Helm chart %q: %w", constants.IstiodChartName, err)
+
+	// First installation attempt
+	_, err := r.ChartManager.UpgradeOrInstallChart(ctx, chartDir,
+		values, targetNamespace, releaseName, ownerReference)
+
+	if err == nil {
+		return ctrl.Result{}, nil
+	}
+
+	// Handle specific error cases
+	switch {
+	case strings.Contains(err.Error(), "unexpected helm release status uninstalling"):
+		log.Info("Release is uninstalling, will retry", "release", releaseName)
+		return ctrl.Result{RequeueAfter: time.Minute}, nil
+
+	case strings.Contains(err.Error(), "invalid ownership metadata"):
+		log.Info("Ownership conflict detected, attempting force install")
+
+		forcedValues := make(map[string]interface{})
+		for k, v := range values {
+			forcedValues[k] = v
+		}
+		forcedValues["force"] = true
+
+		if _, forceErr := r.ChartManager.UpgradeOrInstallChart(ctx, chartDir,
+			forcedValues, targetNamespace, releaseName, ownerReference); forceErr != nil {
+			return ctrl.Result{}, fmt.Errorf("force install failed: %w", forceErr)
+		}
+		return ctrl.Result{}, nil
+
+	default:
+		return ctrl.Result{}, fmt.Errorf("installation failed: %w", err)
 	}
-	return nil
 }
 
 func getReleaseName(rev *v1.IstioRevision) string {
