x-extend:
  schema: config-values.yaml
type: object
properties:
  internal:
    type: object
    default: {}
    properties:
      namespace:
        type: string
        default: d8-observability
      prometheus:
        type: object
        default: {}
        properties:
          token:
            type: string
