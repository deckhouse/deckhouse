apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusterobservabilitypropagateddashboards.observability.deckhouse.io
spec:
  group: observability.deckhouse.io
  scope: Cluster
  names:
    plural: clusterobservabilitypropagateddashboards
    singular: clusterobservabilitypropagateddashboard
    kind: ClusterObservabilityPropagatedDashboard
  preserveUnknownFields: false
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: |
            The resource for storing and managing observability platform dashboards.
          required:
            - spec
          properties:
            spec:
              type: object
              required:
                - definition
              properties:
                definition:
                  type: string
                  description: |
                    The dashboard's JSON manifest.
