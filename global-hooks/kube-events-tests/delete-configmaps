#!/bin/bash

source /antiopa/shell_lib.sh

function __config__() {
  jo -p onDelete="$(jo -a "$(jo kind=configmaps selector=$(jo matchLabels=$(jo hello=world)))")"
}

function __main__() {
    echo "KUBE EVENT delete-configmaps"
}

hook::run $@
