#!/bin/bash

source /antiopa/shell_lib.sh

function __config__() {
  jo -p onStartup=20
}

function __main__() {
  values::set global.cluster.type $(cluster::type)
}

function __main_old__() {
  cat > $RETURN_VALUES_PATH <<END
global:
  cluster:
    type: $(cluster::type)
END
}

hook::run $@
