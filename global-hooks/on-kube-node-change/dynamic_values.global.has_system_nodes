#!/bin/bash -e

if $(kubectl get nodes -l node-role/system -o name | grep . > /dev/null); then
  cat > $RETURN_VALUES_PATH <<END
global:
  has_system_nodes: true
END
else
  cat > $RETURN_VALUES_PATH <<END
global:
  has_system_nodes: false
END
fi
