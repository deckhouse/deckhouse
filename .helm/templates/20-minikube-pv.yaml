{{- if eq .Values.global.env "minikube" }}
kind: PersistentVolume
apiVersion: v1
metadata:
  name: antiopa
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 512M
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/antiopa"
{{- end }}
