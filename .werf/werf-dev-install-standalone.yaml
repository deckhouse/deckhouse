---
image: install-standalone-builder
final: false
from: {{ index $.Images "builder/alpine" }}
import:
- image: dhctl
  add: /dhctl/bin/dhctl
  to: /image/dhctl
  before: setup
shell:
  setup:
  - tar -czf /dhctl-{{- .CI_COMMIT_TAG }}.x86_64.tar.gz -C /image dhctl

---
{{ include "installer_standalone_image" (dict "CI_COMMIT_TAG" .CI_COMMIT_TAG "Images" .Images )}}
---
