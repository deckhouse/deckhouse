giterminismConfigVersion: 1
config:
  goTemplateRendering:
    allowEnvVariables: [ /BASE_.+/, /CI_.+/, "WERF_ENV" , /DOC_API_.+/ ]
  stapel:
    git:
      allowBranch: true
  secrets:
    allowValueIds:
    - GOPROXY
