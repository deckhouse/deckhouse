priorityClassName:
  _default: develop
  production: production-medium

ingressClass:
  _default: nginx
  prod-sel: nginx-tfprod

ingress:
  secret_name:
    _default: ingress-le-tls

resources:
  requests:
    memory:
      _default: 30M
      web-production: 50M

