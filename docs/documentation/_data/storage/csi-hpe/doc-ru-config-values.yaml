type: object
properties:
  nodeSelector:
    description: |
      Определяет, где могут быть смонтированы ресурсы. Работает так же, как параметр `spec.nodeSelector` в Kubernetes Pods.
      Если этот параметр не указан, ресурсы HPE могут быть смонтированы на любом узле кластера с ОС `Linux`.
