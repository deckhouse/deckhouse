apiVersion: v1
kind: Pod
metadata:
  name: nginx-privilege-escalation-allowed
  namespace: testns
  labels:
    app: nginx-privilege-escalation
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        allowPrivilegeEscalation: false
