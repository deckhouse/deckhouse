{{- if ne .Values.csiVsphere.internal.compatibilityFlag "Legacy" }}
{{- range $index, $storageClass := .Values.csiVsphere.internal.storageClasses }}
{{- if eq $storageClass.datastoreType "Datastore" }}
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  {{- include "helm_lib_module_labels" (list $) | nindent 2 }}
  {{- include "helm_lib_module_storage_class_annotations" (list $ $index $storageClass.name) | nindent 2 }}
  name: {{ $storageClass.name | quote }}
provisioner: csi.vsphere.vmware.com
parameters:
  DatastoreURL: {{ $storageClass.datastoreURL | quote }}
allowedTopologies:
- matchLabelExpressions:
  - key: failure-domain.beta.kubernetes.io/region
    values:
    - {{ $.Values.csiVsphere.internal.providerClusterConfiguration.region }}
  - key: failure-domain.beta.kubernetes.io/zone
    values:
  {{- range $storageClass.zones}}
    - {{ . }}
  {{- end }}
allowVolumeExpansion: true
volumeBindingMode: WaitForFirstConsumer
{{- end }}
{{- end }}
{{- end }}
