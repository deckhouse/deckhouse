{{- $podMonitorConfig := dict -}}
{{- $_ := set $podMonitorConfig "targetNamespace" "d8-cloud-provider-zvirt" -}}

{{- include "helm_lib_cloud_data_discoverer_pod_monitor" (list . $podMonitorConfig) }}
