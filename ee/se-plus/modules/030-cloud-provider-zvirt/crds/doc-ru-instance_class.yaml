# This file is auto-generated by openapigen. Do not edit manually.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    heritage: deckhouse
    module: cloud-provider-zvirt
  name: zvirtinstanceclasses.deckhouse.io
spec:
  group: deckhouse.io
  names:
    categories:
    - cloudinstanceclasses
    kind: ZvirtInstanceClass
    listKind: ZvirtInstanceClassList
    plural: zvirtinstanceclasses
    singular: zvirtinstanceclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: NodeGroups which use this instance class.
      jsonPath: .status.nodeGroupConsumers
      name: Node Groups
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: |
              Определяет параметры виртуальной машины zVirt.
            properties:
              customNetworkConfig:
                description: |
                  Включает пользовательскую статическую настройку сети для группы узлов (NodeGroup).

                  Если заполнено, необходимо задать `networkInterfaceName`, `networkInterfaceAddress`, `networkInterfaceGateway` и `networkInterfaceNetmask`.
                properties:
                  dnsServers:
                    description: |
                      Список DNS-серверов.
                    items:
                      type: string
                    type: array
                  networkInterfaceAddress:
                    description: |
                      Список IP-адресов, которые нужно назначить интерфейсам (по адресу для каждого узла).
                    items:
                      type: string
                    type: array
                  networkInterfaceGateway:
                    description: |
                      Шлюз по умолчанию.
                    type: string
                  networkInterfaceName:
                    description: |
                      Имя сетевого интерфейса, для которого будет применена статическая настройка.
                    type: string
                  networkInterfaceNetmask:
                    description: |
                      Маска подсети для интерфейса.
                    type: string
                type: object
              etcdDiskSizeGb:
                description: |
                  Размер диска для etcd в GiB.

                  Устанавливается только для InstanceClass, предназначенного для группы master-узлов.
                nullable: true
                type: integer
              memory:
                description: |
                  Память в MiB для выделения виртуальным машинам.
                type: integer
              numCPUs:
                description: |
                  Количество vCPU, выделяемых виртуальным машинам.
                type: integer
              rootDiskSizeGb:
                description: |
                  Размер корневого диска в GiB для использования в виртуальных машинах.

                  Размер диска будет изменён автоматически, если он отличается от размера диска в шаблоне.
                type: integer
              storageDomainID:
                description: |
                  ID домена хранения.
                type: string
              template:
                description: |
                  Имя шаблона, который будет использован при заказе виртуальных машин.
                type: string
              vnicProfileID:
                description: |
                  ID профиля vNIC.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              memory:
                description: |
                  Память в MiB для выделения виртуальным машинам zVirt VirtualMachines.
                type: integer
              numCPUs:
                description: |
                  Количество vCPU, выделяемых виртуальным машинам zVirt.
                type: integer
              rootDiskSizeGb:
                description: |
                  Размер корневого диска в GiB для использования в виртуальных машинах zVirt.

                  Размер диска будет изменён автоматически, если он отличается от размера диска в шаблоне.
                type: integer
              template:
                description: |
                  Имя шаблона, из которого будут клонированы ВМ.
                type: string
              vnicProfileID:
                description: |
                  vNIC профиль id.
                type: string
            type: object
        type: object
    served: true
    storage: false
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
