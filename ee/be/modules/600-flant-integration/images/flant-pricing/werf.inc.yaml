---
image: {{ .ModuleName }}/{{ $.ImageName }}
fromImage: common/shell-operator
git:
- add: /{{ $.ModulePath }}/modules/600-{{ $.ModuleName }}/images/{{ $.ImageName }}/src
  to: /src
  stageDependencies:
    install:
    - '**/*'
- add: /{{ $.ModulePath }}/modules/600-{{ $.ModuleName }}/images/{{ $.ImageName }}/hooks
  to: /hooks
  excludePaths:
    - '*_test.*'
  stageDependencies:
    install:
      - '**/*'
shell:
  install:
  - apt-get update
  - apt-get install -y python3 python3-module-pip-run ca-certificates curl
  - pip3 install -r /src/requirements.txt
  - apt-get clean
docker:
  ENV:
    SHELL_OPERATOR_HOOKS_DIR: "/hooks"
    LOG_TYPE: json
    PYTHONPATH: "/hooks"
  ENTRYPOINT: ["tini", "--", "/shell-operator"]
  CMD: ["start"]
