spec:
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: |
            Ресурс для настройки удаленного кластера как доверенного.
          properties:
            spec:
              properties:
                trustDomain:
                  description: |
                    TrustDomain удаленного кластера.
                   
                    Указание этого параметра является обязательным, однако на данный момент он не используется, так как текущая версия Istio не поддерживает привязку TrustDomain к корневому сертификату центра сертификации (CA).
                metadata:
                  properties:
                    endpoint:
                      description: |
                       URL формата HTTPS, который используется для публикации метаданных удаленного кластера.
                    insecureSkipVerify:
                      description: |
                        Исключение проверки сертификата HTTPS URL, который используется для публикации метаданных удаленного кластера.
                    ca:
                      description: |
                        Сертификат для проверки HTTPS URL, который используется для публикации метаданных удаленного кластера.
