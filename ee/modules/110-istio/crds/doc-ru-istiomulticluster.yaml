spec:
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: |
            CR for setting remote cluster as trusted one.
          properties:
            spec:
              properties:
                metadataEndpoint:
                  description: |
                    HTTPS endpoint with remote cluster metadata.
