type: object
properties:
  debug:
    type: boolean
    default: false
    description: |
      Option to tell the logger to output stacktraces for warnings and errors.
  numberOfWorkers:
    type: integer
    default: 16
    minimum: 2
    maximum: 128
    description: |
      The number of asynchronous workers in each agent.
