---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    heritage: deckhouse
    module: deckhouse
  name: projects.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: false
      deprecated: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - jsonPath: .status.sync
          name: Ready
          type: boolean
        - jsonPath: .spec.description
          name: Description
          type: string
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                description:
                  description: Arbitrary description of the purpose of the project.
                  type: string
                projectTypeName:
                  description: |
                    The name of the custom resource [ProjectType](cr.html#projecttype) to be used
                    as a template for creating resources for the environment.
                  type: string
                template:
                  additionalProperties:
                    x-kubernetes-preserve-unknown-fields: true
                  description:
                    Values for [resource templates](cr.html#projecttype-v1alpha1-spec-resourcestemplate) from the environment template (CR [ProjectType](cr.html#projecttype) in helm values format that map to the OpenAPI specification (the [openAPI](cr.html#projecttype-v1alpha1-spec-openapi) parameter of the environment template).
                  type: object
            status:
              type: object
              properties:
                message:
                  description: Status message.
                  type: string
                state:
                  description: Summary of the status.
                  type: string
                sync:
                  description: Project definition sync with cluster.
                  type: boolean
    - name: v1alpha2
      served: true
      storage: true
      deprecated: false
      subresources:
        status: {}
      additionalPrinterColumns:
        - jsonPath: .status.sync
          name: Ready
          type: boolean
        - jsonPath: .spec.projectTemplateName
          name: Project template
          type: string
        - jsonPath: .spec.description
          name: Description
          type: string
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                description:
                  description: Arbitrary description of the purpose of the project.
                  type: string
                projectTemplateName:
                  description: |
                    The name of the custom resource [ProjectTemplateName](cr.html#projectTemplateName) to be used
                    as a template for creating resources for the project.
                  type: string
                parameters:
                  additionalProperties:
                    x-kubernetes-preserve-unknown-fields: true
                  description:
                    Parameters for [resources template](cr.html#projecttemplate-v1alpha1-spec-resourcestemplate) 
                    from the related [project template](cr.html#projecttemplate) in helm values format 
                    according to the [OpenAPI specification](cr.html#projecttemplate-v1alpha1-spec-parametersSchema-openAPIV3Schema) 
                    defined in the template.
                  type: object
            status:
              type: object
              properties:
                message:
                  description: Status message.
                  type: string
                state:
                  description: Summary of the status.
                  type: string
                sync:
                  description: Project definition sync with cluster.
                  type: boolean