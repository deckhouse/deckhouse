---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  labels:
    heritage: deckhouse
    module: deckhouse
  name: projects.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    shortNames:
      - project
    singular: project
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.statusSummary.status
          name: Ready
          type: boolean
        - jsonPath: .spec.description
          name: Description
          type: string
        - jsonPath: .status.statusSummary.message
          name: Message
          type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description:
                "APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
              type: string
            kind:
              description:
                "Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
              type: string
            metadata:
              type: object
            spec:
              properties:
                description:
                  description: Description of the Project
                  type: string
                projectTypeName:
                  description: Name of ProjectType to use to create Project
                  type: string
                template:
                  additionalProperties:
                    x-kubernetes-preserve-unknown-fields: true
                  description:
                    Values for resource templates from ProjectType in helm
                    values format that map to the open-api specification from the openAPI
                    ProjectType field
                  type: object
              type: object
            status:
              properties:
                conditions:
                  description: A list of Project conditions
                  items:
                    properties:
                      message:
                        type: string
                      name:
                        type: string
                      status:
                        type: boolean
                    type: object
                  type: array
                statusSummary:
                  description: Summary for the Project status
                  properties:
                    message:
                      description: Status message.
                      type: string
                    status:
                      description: Summary of the status (ready or not ready).
                      type: boolean
                  type: object
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
