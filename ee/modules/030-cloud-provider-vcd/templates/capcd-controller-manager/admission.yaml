# CAPCD v1.2.0 implements only conversion webhooks (/convert path) for API version migration.
# Mutation and validation webhooks are not supported in this version.
# The controller only registers: VCDCluster, VCDMachine, VCDMachineTemplate conversion webhooks.
#
# Do not add MutatingWebhookConfiguration or ValidatingWebhookConfiguration here
# until CAPCD version is upgraded to support them.
