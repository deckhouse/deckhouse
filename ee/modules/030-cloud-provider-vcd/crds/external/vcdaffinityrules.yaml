---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
    heritage: deckhouse
    module: cloud-provider-vcd
  name: vcdaffinityrules.deckhouse.io
spec:
  group: deckhouse.io
  names:
    kind: VCDAffinityRule
    listKind: VCDAffinityRuleList
    plural: vcdaffinityrules
    shortNames:
    - vcdar
    singular: vcdaffinityrule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.nodeCount
      name: NodeCount
      type: integer
    - jsonPath: .status.message
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VCDAffinityRule is the Schema for the vcdaffinityrules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VCDAffinityRuleSpec defines the desired state of VCDAffinityRule.
            properties:
              nodeLabelSelector:
                additionalProperties:
                  type: string
                description: NodeLabelSelector is TODO
                type: object
              polarity:
                description: Polarity is TODO
                type: string
              required:
                description: Required is TODO
                type: boolean
            type: object
          status:
            description: VCDAffinityRuleStatus defines the observed state of VCDAffinityRule.
            properties:
              message:
                description: Message is TODO
                type: string
              nodeCount:
                description: NodeCount is TODO
                type: integer
              nodes:
                description: Nodes is TODO
                items:
                  properties:
                    ID:
                      description: ID is TODO
                      type: string
                    name:
                      description: Name is TODO
                      type: string
                  type: object
                type: array
              ruleID:
                description: Rule ID is TODO
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
