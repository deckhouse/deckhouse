apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: network-gateway
    rbac.deckhouse.io/aggregate-to-networking-as: manager
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/capability: "system-capability.network-gateway.edit"
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: d8-network-gateway
  name: d8:system-capability:network-gateway:edit
  annotations:
    en.meta.deckhouse.io/title: "Module network-gateway: edit configuration"
    ru.meta.deckhouse.io/title: "Модуль network-gateway: управление конфигурацией"
    en.meta.deckhouse.io/description: "Manage the network-gateway module configuration."
    ru.meta.deckhouse.io/description: "Управление конфигурацией модуля network-gateway."
rules:
- apiGroups:
  - deckhouse.io
  resourceNames:
  - network-gateway
  resources:
  - moduleconfigs
  verbs:
  - create
  - update
  - patch
  - delete
