apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: network-gateway
    rbac.deckhouse.io/aggregate-to-networking-as: viewer
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/capability: "system-capability.network-gateway.view"
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: d8-network-gateway
  name: d8:system-capability:network-gateway:view
  annotations:
    en.meta.deckhouse.io/title: "Module network-gateway: view configuration"
    ru.meta.deckhouse.io/title: "Модуль network-gateway: просмотр конфигурации"
    en.meta.deckhouse.io/description: "Read-only access to the network-gateway module configuration."
    ru.meta.deckhouse.io/description: "Доступ только на чтение к конфигурации модуля network-gateway."
rules:
- apiGroups:
  - deckhouse.io
  resourceNames:
  - network-gateway
  resources:
  - moduleconfigs
  verbs:
  - get
  - list
  - watch
