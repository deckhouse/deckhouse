apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    heritage: deckhouse
    module: cloud-provider-baremetal
  name: baremetalinstanceclasses.deckhouse.io
spec:
  group: deckhouse.io
  names:
    categories:
    - cloudinstanceclasses
    kind: BareMetalInstanceClass
    listKind: BareMetalInstanceClassList
    plural: baremetalinstanceclasses
    singular: baremetalinstanceclass
  scope: Cluster
  versions:
  - name: v1
    served: true
    storage: true
    additionalPrinterColumns:
    - name: Node Groups
      type: string
      description: NodeGroups which use this instance class.
      jsonPath: .status.nodeGroupConsumers
    - name: Age
      type: date
      jsonPath: .metadata.creationTimestamp
    schema:
      openAPIV3Schema:
        type: object
        required:
        - spec
        properties:
          apiVersion:
            type: string
            x-doc-skip: true
          kind:
            type: string
            x-doc-skip: true
          metadata:
            type: object
          spec:
            type: object
            required:
            - imageRef
            properties:
              imageRef:
                type: object
                description: Reference to the disk image used for physical host provisioning.
                required:
                - kind
                - name
                properties:
                  kind:
                    type: string
                    enum:
                    - BareMetalImage
                  name:
                    type: string
                    minLength: 1
              hostSelector:
                type: object
                description: Label selector used to select physical hosts.
                properties:
                  matchLabels:
                    type: object
                    additionalProperties:
                      type: string
                  matchExpressions:
                    type: array
                    x-kubernetes-list-type: atomic
                    items:
                      type: object
                      required:
                      - key
                      - operator
                      properties:
                        key:
                          type: string
                        operator:
                          type: string
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                        values:
                          type: array
                          x-kubernetes-list-type: set
                          items:
                            type: string
              disableAutomatedCleaning:
                type: boolean
                default: false
                description: Disables automated cleaning for claimed physical hosts. By default metadata cleaning is enabled.
              nodeReuse:
                type: boolean
                default: false
                description: Enables physical host reuse during upgrades.
              capacity:
                type: object
                description: Node capacity used by cluster-autoscaler when scaling from zero.
                required:
                - cpu
                - memory
                properties:
                  cpu:
                    pattern: '^[0-9]+m?$'
                    x-kubernetes-int-or-string: true
                    x-doc-example: "4000m"
                  memory:
                    pattern: '^[0-9]+(\.[0-9]+)?(E|P|T|G|M|k|Ei|Pi|Ti|Gi|Mi|Ki)?$'
                    x-kubernetes-int-or-string: true
                    x-doc-example: "8Gi"
          status:
            type: object
            properties:
              nodeGroupConsumers:
                type: array
                x-kubernetes-list-type: set
                items:
                  type: string
