apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: keepalived
    rbac.deckhouse.io/aggregate-to-infrastructure-as: viewer
    rbac.deckhouse.io/kind: capability
    rbac.deckhouse.io/capability: "system-capability.keepalived.view"
    rbac.deckhouse.io/scope: system
    rbac.deckhouse.io/namespace: d8-keepalived
  name: d8:system-capability:keepalived:view
  annotations:
    en.meta.deckhouse.io/title: "Module keepalived: view configuration"
    ru.meta.deckhouse.io/title: "Модуль keepalived: просмотр конфигурации"
    en.meta.deckhouse.io/description: "Read-only access to the keepalived module configuration."
    ru.meta.deckhouse.io/description: "Доступ только на чтение к конфигурации модуля keepalived."
rules:
- apiGroups:
  - deckhouse.io
  resources:
  - keepalivedinstances
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - deckhouse.io
  resourceNames:
  - keepalived
  resources:
  - moduleconfigs
  verbs:
  - get
  - list
  - watch
