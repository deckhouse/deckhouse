---
apiVersion: v1
kind: Secret
metadata:
  name: registry-deckhouse-state
  namespace: d8-system
  annotations:
    helm.sh/resource-policy: keep
    version: "1"
  labels:
    app.kubernetes.io/managed-by: system-registry
    heritage: deckhouse
    module: system-registry
    type: system-registry-state
type: system-registry/state
data:
  state: {{ .Values.systemRegistry.internal.state | toYaml | b64enc }}
