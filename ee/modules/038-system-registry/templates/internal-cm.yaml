{{- with .Values.systemRegistry.internal }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-internal
  namespace: d8-system
  {{- include "helm_lib_module_labels" (list . (dict "app" "system-registry-manager")) | nindent 2 }}
data:
  value: |-
    {{- . | toYaml | nindent 4 }}
{{- end }}
