positive:
  values:
    - {}
  configValues:
    - mode: Direct
    - mode: Proxy
      proxy:
        imagesRepo: dev-registry.deckhouse.io/sys/deckhouse-oss
        ca: ""
        password: "password"
        scheme: HTTP
        username: "user"
    - mode: Detached
negative:
  values:
    - somethingInConfig: yes
  configValues:
    - somethingInConfig: yes
